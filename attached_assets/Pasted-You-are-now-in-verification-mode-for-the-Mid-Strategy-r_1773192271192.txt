You are now in verification mode for the Mid-Strategy report work you just completed.

Do not summarize completion.
Do not restate what you built.
Do not give me confidence language.
Do not tell me it is “clean,” “done,” “working,” or “stable” unless you prove it with direct evidence.

Your job is to produce an evidence-backed QA validation report for Mid-Strategy.

VALIDATION GOAL

I need proof that the Mid-Strategy report:
1. renders correctly in preview,
2. exports correctly to PDF,
3. exports correctly to PPTX,
4. uploads or hands off correctly to Google Drive / Slides path,
5. supports the new slide types accurately,
6. does not clip, truncate, mis-map, or silently omit data,
7. uses the correct report data rather than stale cached data,
8. matches the intended narrative and section logic from the source deck,
9. behaves honestly when data is missing,
10. is ready for Olivia review tomorrow.

NON-NEGOTIABLE RULES

- No vague summaries.
- No “looks good.”
- No “appears to work.”
- No “should be fine.”
- No “completed successfully” without showing exact evidence.
- If something was not directly tested, say NOT TESTED.
- If something failed, say FAILED.
- If something is only partially supported, say PARTIAL.
- If something is mocked, hardcoded, sample-based, or fallback-driven, say so explicitly.
- Show exact files, exact routes, exact slide types, exact outcomes.

PHASE 1 — FILE AND ROUTE PROOF

Return:
1. exact files changed in T005 and T006
2. exact route registrations added
3. exact export route handlers added or modified
4. exact UI button wiring added
5. exact print/export path constants changed
6. exact cache mechanism used for PDF print data
7. exact cleanup behavior after PDF generation

For each file:
- give file path
- give purpose
- give exact feature covered

PHASE 2 — SLIDE-TYPE COVERAGE MATRIX

Build a full coverage matrix for every slide type currently supported by Mid-Strategy preview, PDF print page, PPTX export, and Google Drive upload/export path.

Required slide types to validate:
- title
- divider
- metrics
- table
- chart-bar
- chart-line
- bullets
- two-col
- scorecard
- decision-card
- ia-comparison
- cluster-map

Return a table with these columns:
- slide type
- preview support
- PDF support
- PPTX support
- Drive/Slides support
- exact renderer/export function handling it
- status: PASS / PARTIAL / FAIL / NOT TESTED
- notes

Do not skip any type.

PHASE 3 — LIVE RENDER VALIDATION

You must actually validate rendering behavior, not infer it.

For Mid-Strategy preview:
- confirm the page loads
- confirm the report renders with actual section sequence
- confirm no React/runtime errors
- confirm each slide type renders visibly
- confirm long text does not overflow containers
- confirm tables render all rows
- confirm charts render with visible labels/legends where applicable
- confirm IA comparison renders current/future distinctly
- confirm cluster-map renders all hubs/pages expected
- confirm decision-card renders recommendation line and option blocks correctly

For each of the above, give:
- what was tested
- how it was tested
- result
- PASS / FAIL / PARTIAL / NOT TESTED

PHASE 4 — PDF EXPORT VALIDATION

Actually generate a Mid-Strategy PDF and verify it.

Validate:
- export route responds successfully
- PDF opens successfully
- page count is sensible
- no blank pages unless intentionally inserted
- no clipped sections
- no missing charts
- no missing tables
- no missing IA comparison content
- no missing cluster-map content
- no missing decision-card content
- footer/header behavior if applicable
- no sidebar/theme chrome contamination
- no obvious stale data bleed from previous report generations
- no print-cache leak after generation

Required evidence:
- exact route tested
- payload shape used
- whether print cache entry was created
- whether cache entry was cleaned up
- resulting PDF file size or equivalent proof
- page count or equivalent proof
- pass/fail per slide type in PDF

PHASE 5 — PPTX EXPORT VALIDATION

Actually generate a PPTX and verify it.

Validate:
- PPTX route responds successfully
- file opens successfully
- all expected slides exist
- no unsupported slide type is dropped
- decision-card is formatted correctly
- ia-comparison is readable and mapped correctly
- cluster-map is readable and not collapsed into garbage
- bullet hierarchy is preserved where intended
- no malformed slides
- no empty placeholders
- no stale content bleed

Required evidence:
- exact route tested
- output file produced
- slide count
- pass/fail per slide type
- any formatting degradation vs preview/PDF

PHASE 6 — GOOGLE DRIVE / SLIDES PATH VALIDATION

Do not hand-wave this.
If true Google Slides export is not real and the system is only uploading a PPTX to Drive, say that exactly.

Validate:
- what the route actually does
- whether it uploads a PPTX file to Drive only
- whether it creates a native Google Slides deck
- whether formatting is preserved
- whether the output is usable for review
- whether any slide type degrades in that pathway

Return one explicit line:
- “Native Google Slides export: YES/NO”
- “Drive upload of PPTX: YES/NO”

If native Slides is not implemented, say so clearly. Do not blur those two together.

PHASE 7 — STALE DATA / CACHE INTEGRITY TEST

I specifically want proof that Mid-Strategy is not exporting stale data.

Run a controlled validation:
1. generate report version A with a distinctive text marker in one editable field
2. export PDF/PPTX
3. change that marker to a clearly different value in report version B
4. export again
5. prove version B exports contain the updated value and not the old one

Return:
- exact field changed
- value A
- value B
- whether preview updated
- whether PDF updated
- whether PPTX updated
- status: PASS / FAIL

PHASE 8 — DESIGN AND NARRATIVE FIDELITY CHECK

Compare the implemented Mid-Strategy against the intended source-deck structure.

Validate whether the final product still clearly supports these narrative blocks:
- cover
- agenda
- next checkpoint
- domain strategy recommendation
- redirect/migration logic
- competitive baseline
- AI visibility baseline
- competitive summary
- first focus
- current vs future IA
- scalable blueprint / cluster map
- credibility layer
- what’s next
- next steps / ownership

For each one:
- is it supported in generator
- is it supported in preview
- is it supported in PDF
- is it supported in PPTX
- status: PASS / PARTIAL / FAIL
- notes

PHASE 9 — MISSING-DATA HONESTY CHECK

Validate behavior when data is missing.

Specifically test:
- missing benchmark dataset
- missing Asana connection
- missing call-tracking source
- missing Screaming Frog
- missing logo/brand asset
- missing website scan details

For each:
- what the UI says
- what the export says
- whether the system fabricates content
- status: PASS / FAIL

PHASE 10 — FINAL QA SCORECARD

Return a final scorecard with these rows:
- Preview rendering
- PDF export
- PPTX export
- Drive/Slides path
- Slide-type coverage
- Stale-data protection
- Missing-data honesty
- Narrative fidelity
- Olivia-review readiness

Columns:
- status
- blocking issues
- non-blocking issues
- exact next fix if needed

FINAL OUTPUT FORMAT

Return in this order:

1. Validation summary table
2. File and route proof
3. Slide-type coverage matrix
4. Preview validation results
5. PDF validation results
6. PPTX validation results
7. Drive/Slides validation results
8. Stale-data test results
9. Missing-data honesty results
10. Narrative fidelity results
11. Blocking issues
12. Non-blocking issues
13. Final verdict:
   - READY FOR OLIVIA REVIEW
   - READY WITH CAVEATS
   - NOT READY

FINAL WARNING

If you did not directly test something, mark it NOT TESTED.
Do not substitute confidence for evidence.
Do not protect previous work by softening failures.
I want the truth, not morale.