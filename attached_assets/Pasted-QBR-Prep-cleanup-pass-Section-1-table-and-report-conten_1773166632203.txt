QBR Prep cleanup pass — Section 1 table and report-content correction.

There are 3 required fixes here. Implement them exactly.

==================================================
1. PRIORITY CHECKS MUST NOT APPEAR IN THE REPORT
==================================================

Priority Checks are background-only instructions for generation.
They must influence drafting, but they must never render anywhere in the final report.

Current problem:
Priority Checks text is leaking into the Section 1 Reason / report copy.

Required fix:
- Remove Priority Checks content from all rendered report output
- Do not inject Priority Checks text into Section 1 Reason
- Do not show Priority Checks anywhere in preview, print, PDF, DOCX, or saved report display
- Priority Checks may still be used internally by generation logic, but only as hidden background guidance

Important:
- AM’s Hypothesis, Previous Quarter Assessment, Client Insights, and Client Sentiment may appear in the report where intended
- Priority Checks must not appear, quote, paraphrase, or leak into visible report language

==================================================
2. CLEAN UP SECTION 1 TABLE LAYOUT
==================================================

Current Section 1 table is too messy and the Reason column is overly wide.

Required fixes:
A. Rename column header:
- “Measurement Source” → “Source”

B. Slim down the Reason column content
- Rewrite Section 1 Reason output so it is tighter and more concise
- Keep the comparative logic, but reduce verbosity substantially
- Aim for 1–3 tight sentences max
- Do not let it become a long paragraph
- Preserve strategic clarity while making the table more readable

C. Widen the functional columns by reducing Reason bloat
- Goal Type, Goal, Source, and Goal Shift vs Last Quarter should have enough room to read cleanly
- Keep the table readable in preview and print/PDF
- Avoid cramped wrapping where possible

==================================================
3. STANDARDIZE “GOAL SHIFT VS LAST QUARTER”
==================================================

Current problem:
The column mixes formats like “0%” and text like “Maintain”.

Required fix:
- Standardize Goal Shift vs Last Quarter to percentage-only presentation
- No words like “Maintain”
- Every row should use a consistent numeric percentage format

Rules:
- Use percentage strings only, such as:
  - 0%
  - +12%
  - -8%
- If the shift is flat, show 0%
- If there is no reliable comparison basis, use a consistent neutral fallback such as:
  - —
Choose one fallback and apply it consistently, but do not use words

Important:
- This applies to all rows in Section 1 where Goal Shift vs Last Quarter is shown
- Keep the calculation logic truthful
- Do not invent a percent if comparison data truly does not exist

==================================================
4. TESTING / ACCEPTANCE CRITERIA
==================================================

1. Priority Checks text no longer appears anywhere in visible report output
2. Section 1 Reason no longer contains leaked Priority Checks language
3. “Measurement Source” is renamed to “Source”
4. Section 1 Reason is materially slimmer and easier to scan
5. Goal Shift vs Last Quarter uses standardized percentage-only formatting
6. No “Maintain” text appears in that column
7. Preview and print/PDF still render correctly
8. Export flow still works

==================================================
5. AFTER IMPLEMENTATION, REPORT BACK WITH:
==================================================

- files changed
- exact change made to prevent Priority Checks from leaking into visible report copy
- exact final Section 1 Reason generation rules
- exact final Goal Shift vs Last Quarter formatting rules
- confirmation that “Measurement Source” is now “Source”
- confirmation that preview and export still work