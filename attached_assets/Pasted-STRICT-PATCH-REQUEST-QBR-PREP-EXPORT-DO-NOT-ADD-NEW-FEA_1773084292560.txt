STRICT PATCH REQUEST — QBR PREP EXPORT
DO NOT ADD NEW FEATURES.
DO NOT REFACTOR UNRELATED LOGIC.
DO NOT MARK THIS COMPLETE UNTIL EVERY ACCEPTANCE TEST BELOW PASSES.

You already implemented the first round. This patch is ONLY for the tasks that still failed or only partially passed in the latest exported PDF.

FILES TO FOCUS ON
- server/qbrPrepSectionGenerator.ts
- server/qbrPrepHelpers.ts
- server/qbrPrepDocxGenerator.ts
- any shared sanitization/util layer used during final export
- preview/print only if needed to keep rendered output consistent with the generator

NON-NEGOTIABLE RULE
The PDF output is the source of truth for success.
If the exported PDF still contains any of the broken strings, contradictory labels, or wrong statuses listed below, the patch is a failure.

==================================================
P0 — HARD FAILURES TO FIX
==================================================

1) FIX GA4 CONTRADICTION GLOBALLY
Problem:
The latest PDF still shows:
Sources used: Google Sheets NSM Tracker, Google Search Console, Multi-source, CallRail · Missing: GA4
even though GA4 is actively used in:
- Section 1 tertiary goal: Organic Sessions = GA4 / GSC
- Section 7 rows that cite GA4
This is an absolute contradiction.

Required behavior:
- If GA4 contributes to ANY rendered section in the report, GA4 must NEVER appear in missingData, the source legend, or any "Missing:" output.
- This rule must run after all sections are assembled and after all source labels are finalized.
- This must be enforced globally, not just per section.

Implementation requirement:
- Build a final usedSources set by recursively scanning the fully assembled report payload immediately before render/export.
- Normalize source names before comparison:
  - "GA4"
  - "Google Analytics 4"
  - "Google Analytics"
  must all normalize to "GA4"
- Remove any normalized used source from missingData before footer/source legend render.

Acceptance tests:
- If any field anywhere in the report includes GA4 as a source, footer/source legend must not contain "Missing: GA4".
- Same rule should also work for GSC, CallRail, CTM, GBP, Screaming Frog, Multi-source.

Hard fail strings that must not appear:
- Missing: GA4

2) FIX FINAL SANITIZATION FOR REAL, NOT PARTIALLY
Problem:
The current sanitization pass is still leaking malformed unicode, broken slugs, stitched phrases, and truncated copy into the PDF.

Broken output currently visible includes examples like:
- /insurance￾verification/
- Substance￾Specific Education
- late￾stage
- /am-i-an-alcoholic-can-you-be￾an-alcoholic-and-not-drink￾every-day/
- /what-never-leaving-your￾hometown-does-to-your-brain/
- Source pending confirmation via
- not.
- already unde.
- Women's Men's Detox Addiction Treatment & Dual Diagnosis

Required behavior:
Run a final, aggressive export sanitization pass over EVERY string in the fully assembled report payload immediately before render.

Must fix all of the following:
A) Unicode cleanup
- strip invisible replacement/control chars such as:
  - U+FFFE / U+FFFF-like junk
  - object replacement chars
  - zero-width non-printing separators when they break words/slugs
- repair slug separators so URLs render with normal hyphens only

B) Broken stitched phrases
- remove dangling fragments like:
  - "via" at the end of a sentence
  - "not."
  - "not ."
  - "already unde."
- if a stitched sentence is incomplete, trim it back to the last complete clause instead of leaving broken text

C) Label repair
- fix malformed category names like:
  - "Substance￾Specific Education" -> "Substance-Specific Education"
- fix malformed prose joins like:
  - "late￾stage" -> "late-stage"
- fix malformed program labels such as:
  - "Women's Men's Detox Addiction Treatment & Dual Diagnosis"
  to a readable, deterministic format

D) Whitespace/punctuation normalization
- collapse duplicate spaces
- remove orphan punctuation
- normalize spaces around slashes, hyphens, commas, and periods
- preserve valid URL punctuation

Implementation requirement:
- sanitize recursively across:
  - strings
  - arrays
  - nested objects
  - edit-resolved content before final render
- do not sanitize only raw generator content; sanitize final output content
- add targeted replacement rules for known recurring bad patterns
- add a fallback regex that removes non-printing junk characters anywhere in the string

Acceptance tests:
The exported PDF must contain NONE of these strings:
- ￾
- /insurance￾verification/
- Substance￾Specific
- late￾stage
- Source pending confirmation via
- not.
- not .
- already unde.
- what-never-leaving-your￾hometown
- am-i-an-alcoholic-can-you-be￾an-alcoholic

Additional acceptance rule:
No sentence in the export may end in:
- via
- not
- unde
unless those are valid complete words in normal prose

3) FIX SECTION 1 ADMITS LOGIC CONTRADICTION
Problem:
Section 1 currently shows a precise admits number and QoQ shift while also saying the source is "Source pending confirmation".
That is logically inconsistent.

Required behavior:
For the Primary Goal = Admits, there are ONLY two valid states:

STATE A — CONFIRMED ADMITS SOURCE
Use this only if a real admits source exists and a numeric admits value is actually supported.
Allowed output:
- admits value shown
- source shown as confirmed system/source
- shift vs last quarter shown
- reason text may reference actual admits trend

STATE B — SOURCE PENDING
Use this when admits source is not confirmed.
Required output:
- Goal label still = Admits
- Measurement source = Source pending confirmation
- NO numeric admits value
- NO QoQ admits shift
- NO pacing math based on admits
- Reason text must explicitly say admits is the strategic primary KPI but reporting source is not yet confirmed

Do not mix the two states.

Implementation requirement:
- Gate all admits numeric rendering behind hasConfirmedAdmitsSource && hasNumericAdmitsValue
- If either is false:
  - goal value becomes "Source pending"
    OR blank/"—" depending on your schema
  - shift becomes "—"
  - reason becomes a static explanatory sentence
- Calls remains the measurable operational proxy underneath admits
- Organic sessions remains tertiary

Acceptance tests:
INVALID:
- "178 admits | Source pending confirmation | -5%"
VALID:
- "Admits | Source pending confirmation | —"
VALID:
- "178 admits | [confirmed source] | -5%"

==================================================
P1 — PARTIAL PASSES THAT MUST BECOME FULL PASSES
==================================================

4) FIX SECTION 7 STATUS LOGIC
Problem:
Section 7 status is still wrong in at least one important case.
"Organic Sessions" shows source = GA4 but status = Missing Setup, even though Section 1 already reports organic sessions from GA4 / GSC.
That means status derivation is not aligned with actual data availability.

Required behavior:
Status must be derived from metric-level evidence, not vague source assumptions.

Canonical statuses:
- Live
- Needs Verification
- Missing Setup
- Inferred Only

Metric-level rules:
A) Organic Sessions
- If GA4 or GSC successfully produced the organic sessions metric used in Section 1 or Section 7, status = Live
- It cannot be Missing Setup if the report already shows a real sessions number from GA4/GSC

B) Qualified Calls / Calls
- If CallRail/CTM data successfully populated the metric, status = Live

C) VOB submissions + qualified organic calls
- If the metric is partially measured from real sources but not fully instrumented end-to-end, status = Needs Verification or Inferred Only depending on actual logic
- Use Missing Setup only when there is truly no usable tracking source

D) Conversion Rate / Internal Link Effectiveness
- If GA4 events/paths needed to support the metric are not confirmed, Missing Setup is acceptable
- If partially derived from indirect data, use Inferred Only

Implementation requirement:
- derive status from the actual metric payload and data provenance used to generate that row
- do not derive status from a generic source-name match alone
- add a helper like deriveTrackingStatus(metricKey, availableEvidence)

Acceptance tests:
- Organic Sessions row must be Live if Section 1 shows a real sessions number from GA4/GSC
- A row cannot have source = GA4 and status = Missing Setup when that same metric was already populated from GA4
- Status logic must be deterministic and explainable

5) TIGHTEN TOPIC CLUSTERING FURTHER
Problem:
Topic clustering improved, but "Other" is still too large at 19% of clicks.
That means the fallback bucket is still swallowing too much useful intent.

Required behavior:
- "Other" must be a last-resort remainder bucket, not a major strategic bucket
- Target: "Other" should be under 10% of clicks whenever enough query patterns exist to classify more specifically
- Queries currently falling into "Other" must be re-evaluated before accepting fallback

Implementation requirement:
- Add a second-pass classifier for currently unbucketed queries before assigning "Other"
- Add/expand deterministic patterns for:
  - substance/drug duration queries
  - alcohol screening/self-assessment queries
  - rehab/treatment-intent queries
  - mental health/condition queries
  - shame/recovery/self-forgiveness style recovery education queries
  - women/female rehab queries
- Keep local intent strict; do not throw informational queries into Local Treatment Intent
- Use a consistent fallback order:
  1. Branded Navigation
  2. Treatment / Service Intent
  3. Detox
  4. Residential Treatment
  5. Women's Rehab
  6. Mental Health / Conditions
  7. Substance-Specific Education
  8. Informational / Education
  9. Trust / Evaluation
  10. Other

Acceptance tests:
- "Other" should be reduced materially from current 19% in this export if queries can be classified by existing words/patterns
- obvious educational or substance-related queries must not remain in Other if a named bucket fits
- bucket names must render cleanly and consistently

6) MAKE SECTION 1 / SECTION 7 SOURCE-STATUS-LEGEND LOGIC INTERNALLY CONSISTENT
Problem:
The report is still behaving like different sections do not know what other sections already proved.

Required behavior:
- Section 1, Section 7, and source legend must all agree on whether a source was used
- A metric shown from a source in Section 1 should inform status in Section 7
- Used sources in either section must suppress that source from missingData
- This must be based on shared normalized evidence, not separate local guesses

Implementation requirement:
Create one normalized evidence map for the final assembled report, something like:
{
  GA4: { used: true, metrics: ["organicSessions", ...] },
  GSC: { used: true, metrics: [...] },
  CallRail: { used: true, metrics: [...] },
  ...
}
All footer/status/source decisions must reference the same final evidence map.

Acceptance tests:
- source legend, Section 1, and Section 7 must agree
- no source can be both used and missing in the same report
- no metric can be shown from a source and simultaneously marked missing setup unless the status refers to a different missing event layer with explicit logic

==================================================
P2 — OPTIONAL BUT RECOMMENDED TO CLOSE THE LOOP
==================================================

7) MAKE CONNECTION LABEL DEFINITIONS VISIBLE ONLY IF YOU CAN DO IT CLEANLY
This was previously a partial.
Only implement if it can be done without clutter.

Required behavior:
If connection labels appear in Section 2 or Section 3 and tracking is incomplete, show a compact note under the relevant table:
- Direct = closest to admission action
- Assisted = supports later conversion
- Informational = low direct admit intent
- Trust / Evaluation = credibility/research stage before conversion

Do NOT add a bulky legend if it makes the page messy.
This is optional in this patch.
Prioritize the hard failures above first.

==================================================
IMPLEMENTATION CONSTRAINTS
==================================================

- Do not break existing custom row support
- Do not regress the Section 6 Tracking & Attribution Setup injection
- Do not regress PPC vs organic call labeling
- Do not remove the existing tracking disclaimer
- Do not change the internal section title "Where Conversions Actually Happen"
- Do not fabricate admits from calls
- Do not hide problems by deleting sections; fix the logic

==================================================
DONE MEANS ALL OF THIS IS TRUE
==================================================

1. Exported PDF no longer says "Missing: GA4" if GA4 is used anywhere
2. Exported PDF contains zero malformed unicode separators or broken slug characters
3. No truncated stitched phrases remain
4. Primary Goal "Admits" is either fully confirmed with source + value + shift, or clearly pending with no fake number/trend
5. Organic Sessions in Section 7 is not marked "Missing Setup" if GA4/GSC already populated it
6. "Other" is materially reduced and only contains true leftovers
7. No regressions to already-fixed items

FINAL INSTRUCTION
Do not reply with a summary of what you intend to do.
Make the patch, generate a fresh PDF, and verify the output against the exact acceptance tests above before calling it done.