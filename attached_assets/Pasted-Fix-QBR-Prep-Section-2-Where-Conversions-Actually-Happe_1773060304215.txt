Fix QBR Prep Section 2: “Where Conversions Actually Happen.”

Problem:
Section 2 is falling back to “Manual entry needed” even though the connected sources are available and this section should be auto-filled.

Expected behavior:
Section 2 should automatically build:
1. Top Converting Pages
2. Top Converting Sources

using the best available combination of:
- GA4
- Call tracking (CTM / CallRail / Nimbata / manual call tracking if present)
- GBP
- page classification from site structure / URL patterns

Do not wait for one perfect source. Build the section from the strongest available combined signals.

==================================================
REQUIRED FIX
==================================================

Rework Section 2 generation logic so it assembles a composite conversion picture from available sources.

Top Converting Pages should infer rows like:
- Type
- Page / Pattern
- Conversion Source
- Notes / What We’re Learning

Top Converting Sources should infer rows like:
- Source
- What’s Converting
- Notes / What We’re Learning

==================================================
SOURCE PRIORITY FOR SECTION 2
==================================================

Use this order, but combine sources when useful:

1. GA4 conversion-linked landing pages / page patterns
2. Call tracking source + landing page data
3. GBP calls / website clicks
4. known admissions-path pages from site classification
5. AM/client-confirmed notes only if already available in the report context

Do NOT force manual fallback just because one source is incomplete.

==================================================
GA4 LOGIC
==================================================

Use GA4 to identify:
- pages with form starts / form submissions / click-to-call events / key conversion events
- page patterns tied to those events
- pre-call and pre-form behavior when strong enough

Normalize common useful outputs into patterns like:
- /contact/ — VOB Form Start
- / — Phone Clicks
- /faqs/ — Phone Clicks
- /service-page/ — Phone Clicks
- /meet-our-staff/ — Phone Clicks

If event naming varies by client, infer from available GA4 event names instead of failing the section.

==================================================
CALL TRACKING LOGIC
==================================================

Use CTM / CallRail / Nimbata to identify:
- top landing pages driving calls
- source labels
- organic vs GBP vs brand where possible
- qualified calls / admits proxies if present

Normalize source names where possible:
- Google Business Profile / GMB
- Organic Search
- Brand & SSL Search
- VOB Form (On-Site)
- Homepage / direct local intent
- etc.

If attribution is partial, still create the row and note the attribution gap instead of dropping to manual fallback.

==================================================
GBP LOGIC
==================================================

If GBP is connected, use:
- calls
- website clicks
- direction requests when relevant

GBP should inform the “Top Converting Sources” table even if it does not map perfectly to a page row.

==================================================
PAGE CLASSIFICATION LOGIC
==================================================

Classify pages into useful types using URL and site structure, for example:
- Form Entry
- Homepage
- FAQ Page
- Service Page
- Staff Page
- Contact / Admissions
- Verify Insurance
- Blog / Resource
- Location Page

Use this classification to label Section 2 rows.

==================================================
NOTES / WHAT WE’RE LEARNING
==================================================

Generate notes from the actual pattern, not generic filler.

Examples of acceptable note logic:
- strongest trackable conversion on the site
- dominant call driver but attribution gap exists
- only non-homepage service page generating calls
- users vetting before committing
- quality leads are coming from form intent
- brand search converts strongly
- GMB is driving major call volume

==================================================
FALLBACK RULE
==================================================

Only use “Manual entry needed” if ALL of the following are missing or unusable:
- GA4 conversion/page behavior
- call tracking attribution
- GBP source signal
- page classification / admissions-path inference

If even one of those is usable, build the section from it.

==================================================
DEBUG / PROOF REQUIRED
==================================================

After fixing, return:
1. files changed
2. exact Section 2 generator logic
3. what GA4 events / page signals were found for this client
4. what call tracking signals were found for this client
5. what GBP signals were found for this client
6. the final generated Section 2 JSON for this client
7. why the old logic fell back to manual
8. proof that the section now auto-fills instead of showing manual fallback