ADR cleanup pass — fix the remaining mismatches exactly. Implement these items fully. Do not give me a partial UI summary. I want the real backend + frontend changes.

Important clarifications:
- Olivia approved the naming “Client Insights,” so keep that name
- Previous Quarter Assessment should NOT be optional; make it required
- Section 1 reason logic is currently too thin and must be rewritten properly
- The report context block labels before Section 1 must be correct
- If the report says QoQ, the calculation must truly be QoQ
- The system should rely on the NSM sheet / NSM type information to understand what tracking is active; tracking should not be treated as absent if the NSM source already tells us the active tracking type

Do not change:
- current export gating
- Strategy Bank logic
- current Section 2 qualified-source rule for Top Converting Pages
- current PDF/print architecture unless needed for the items below

==================================================
1. SIDEBAR FIELD REQUIREMENTS + ORDER
==================================================

Fix the sidebar field order and requirement status.

Required live sidebar order:
1. AM’s Hypothesis
2. Previous Quarter Assessment
3. Priority Checks
4. Client Insights
5. Client Sentiment

Requirement status:
- AM’s Hypothesis = required
- Previous Quarter Assessment = required
- Priority Checks = required
- Client Insights = optional
- Client Sentiment = required if that is the current intended workflow, otherwise keep existing validation only if already approved; do not silently change business rules without reporting it

Implementation rules:
- preserve existing persistence and restore logic
- preserve existing payload wiring
- only change order / labels / validation status as needed
- Previous Quarter Assessment must now validate like a required field in the initial generation flow

==================================================
2. REPORT CONTEXT BLOCK BEFORE SECTION 1
==================================================

Before Section 1, render the context block with these labels exactly:
- AM’s Hypothesis
- Previous Quarter Assessment
- Client Insights
- Client Sentiment

Rules:
- Priority Checks must NOT render anywhere in the final report
- If all 4 visible context fields are empty, the block may hide entirely only if that state is still possible after validation; otherwise render it normally
- Keep preview and print/PDF behavior aligned

==================================================
3. SECTION 1 REASON LOGIC — COMPLETE REWRITE
==================================================

Current implementation is too thin because it only prepends:
“Prior quarter: [Previous Quarter Assessment] — [existing admits reason]”

That is not sufficient.

Rewrite the Section 1 Reason so it functions as a true comparative explanation.

Required behavior:
The Reason under “What Matters Most This Quarter” must explicitly explain:
1. what happened last quarter
2. what the previous focus was
3. what changed
4. why this quarter’s goal is now different by comparison

Inputs that must be used where available:
- Previous Quarter Assessment
- AM’s Hypothesis
- Client Insights
- Client Sentiment
- current quarter performance data
- NSM goal/actuals
- tracking type from the NSM sheet / active NSM source
- call tracking provider or GA4 source where applicable

Important:
- do not make this fully manual
- automate as much of the reasoning as possible
- use the Previous Quarter Assessment as one input, not the whole explanation
- the generated sentence(s) should synthesize the AM context and the account data

Tracking clarification:
- the NSM sheet already indicates the NSM type / active tracking setup
- use that to understand whether admits, calls, forms, GA4, or another measure is the active tracked source
- do not write reason logic as if tracking is missing when the NSM sheet already provides the active tracking type
- if direct admits data from call tracking is unavailable but another approved NSM tracking type is active, use that tracked source in the reasoning
- only fall back to manual-entry-style caveats when the system truly lacks the relevant tracked signal

Desired output style:
- concise
- direct
- comparative
- easy for an ADR or AM to understand quickly
- should read like a strategic explanation, not a stitched-together prepend

==================================================
4. GOAL / NSM SOURCE LOGIC
==================================================

Keep:
- Primary Goal/MVP NSM
- Tertiary Goal/Secondary NSM

But confirm the measurement-source logic is aligned with the NSM sheet.

Required:
- use the NSM sheet / NSM type as the source of truth for what is actively tracked
- do not assume missing tracking if the NSM configuration already identifies the active source
- make sure report language reflects the actual configured tracking type

Tertiary Goal/Secondary NSM:
- keep GA4-only if that is still the approved business rule
- if there is any dependency on the NSM sheet for this labeling/source logic, wire it correctly and report it back clearly

==================================================
5. QOQ LABEL / CALCULATION MISMATCH
==================================================

Current problem:
- the report says QoQ
- the backend calculation is still matched-duration prior-period logic
- that is not acceptable

Required fix:
Preferred path:
- change the backend calculation to true quarter-over-quarter logic using real prior-quarter boundaries
- keep the QoQ labels

Meaning:
- compare the current quarter period against the actual prior quarter period, not a same-length rolling matched window
- align report labels, helper text, and logs with the real calculation

If for some reason you cannot safely make the calculation truly QoQ in this pass, do NOT leave fake QoQ labels in place. In that case revert labels to truthful wording and explain why.

But preferred implementation is:
- true QoQ math
- truthful QoQ labels
- no pseudo-QoQ wording anywhere in report-facing output

Also:
- apply this consistently to clicks / queries / impressions where the report labels claim QoQ
- confirm exact current-quarter and prior-quarter date-window logic after the change

==================================================
6. NSM / TRACKING AWARENESS IN SECTION 1
==================================================

Strengthen Section 1 generation so it is aware of the actual active tracking type from the NSM sheet.

Required behavior:
- if NSM type says admits tracking is active through a call tracking provider, use that
- if NSM type indicates another valid tracked source, use that instead of implying no tracking
- report copy should reflect what is actually being measured
- avoid generic “tracking gap” language inside the Section 1 reason when the NSM configuration already identifies the active tracked source

This is not asking for a new UI field. This is asking the generator to use the existing NSM metadata correctly.

==================================================
7. TESTING / ACCEPTANCE CRITERIA
==================================================

1. Sidebar order is exactly:
- AM’s Hypothesis
- Previous Quarter Assessment
- Priority Checks
- Client Insights
- Client Sentiment

2. Previous Quarter Assessment is required in the initial generation flow

3. Report context block before Section 1 uses exactly:
- AM’s Hypothesis
- Previous Quarter Assessment
- Client Insights
- Client Sentiment

4. Priority Checks does not render in the final report

5. Section 1 Reason is a true comparative explanation, not a prepended string

6. Section 1 Reason uses:
- AM context
- current account data
- NSM tracking type / active source
as available

7. If labels say QoQ, backend math is truly quarter-over-quarter

8. No report-facing label claims QoQ while using matched prior-period logic

9. Existing export flow still works

==================================================
8. AFTER IMPLEMENTATION, REPORT BACK WITH:
==================================================

- files changed
- exact final sidebar field order
- exact validation rules for each sidebar field
- exact report-context labels before Section 1
- exact new Section 1 reason-generation logic
- exact way NSM type / tracking source is being used in Section 1
- exact final QoQ calculation logic and date windows
- confirmation that Previous Quarter Assessment is now required
- confirmation that Priority Checks still does not render
- confirmation that export still works
- any unresolved edge cases