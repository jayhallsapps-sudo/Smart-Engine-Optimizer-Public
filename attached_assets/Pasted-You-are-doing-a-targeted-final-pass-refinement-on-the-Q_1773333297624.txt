You are doing a targeted final-pass refinement on the QBS generator and preview experience. This is not a broad rebuild. This is a practical, deadline-aware cleanup pass focused only on the issues listed below.

Do not reopen solved logic outside this scope.
Do not redesign the report.
Do not change the red QBS design language.
Do not change the table design system except where explicitly requested below.
Do not change working Sections 2, 4, 5, 8, or 9 beyond the exact fixes requested here.

PRIMARY OBJECTIVE

Make the last practical fixes needed so the QBS is more usable right now:
- tighten a few remaining copy issues
- simplify Section 7 summary copy
- cap cluster display length
- enforce a better content-mix in keyword recommendations
- add preview-only row deletion controls to all tables
- fix Primary Location so it is not falling back to “Manual entry needed” when the live site clearly provides location evidence

This pass is intentionally narrow. Do not drift beyond it.

==================================================
ONLY FIX THE FOLLOWING
==================================================

1) SECTION 5 — TIGHTEN THE “WHY IT MATTERS” LANGUAGE

Current problem:
Section 5’s “Why It Matters” language is still too broad / overstated in cases where the report already confirms some Tier 1 elements are present.

Required fix:
- tighten the Section 5 “Why It Matters” copy so it matches the actual findings more precisely
- if Tier 1 is Partial, the language must sound Partial
- do not imply total blockage when the report has already confirmed real live utilities / Levels of Care pages
- keep it concise and defensible

Acceptance requirement:
- Section 5 “Why It Matters” language matches the actual tier status and does not overstate blockage

2) SECTION 7 — REMOVE THE CREDIT-CAP MATH PHRASE

Current problem:
The Section 7 summary text is overcomplicated and the count/cap phrasing has caused confusion.

Required fix:
Replace the current Section 7 summary phrasing with a simpler format:
- “Showing X keyword opportunities, grounded in GSC query data.”

Rules:
- do not include the “2× monthly credit capacity” phrase
- do not include bracketed cap math
- keep the box concise and clean
- preserve the current box style and source logic

Acceptance requirement:
- Section 7 summary uses the simplified sentence only
- no count/cap multiplier math remains in that summary copy

3) SECTION 7 — CAP DISPLAYED VARIANTS PER KEYWORD CLUSTER

Current problem:
Some keyword clusters are too long and visually ridiculous.

Required fix:
- cap the number of displayed variants in any single keyword cluster row to 10 maximum
- keep the best / most relevant / most representative variants
- do not let one row turn into an unwieldy wall of terms
- preserve cluster logic; only cap display length

Acceptance requirement:
- no keyword row displays more than 10 variants
- cluster output remains readable

4) SECTION 7 — ENFORCE A MINIMUM NET-NEW CONTENT COUNT

Current problem:
The current recommendation mix can underproduce create-new content recommendations for a quarter.

Required fix:
- ensure the keyword recommendation output includes at least 3 create-new / new content recommendations per quarterly QBS output
- do this without breaking logic around existing live-page detection
- if strong existing-page matches exist, keep those as CRO / optimize / refresh where appropriate
- but the final recommendation mix must still include at least 3 create-new rows

Important:
Do not recommend obviously redundant net-new content where a strong existing page already fully covers the intent.
This should be a smart minimum mix rule, not a dumb override.

Acceptance requirement:
- final keyword recommendation set contains at least 3 create-new recommendations
- existing-page recommendations still remain sensible

5) PREVIEW ONLY — ADD ROW DELETION CONTROL TO ALL TABLES

Current problem:
The preview needs manual cleanup control so rows can be removed quickly before export.

Required fix:
- add an X control beside every row in every editable preview table
- when clicked, that row disappears from the preview state
- deleted rows must not appear in export outputs generated from that edited preview state
- this is preview/editor behavior only, not something shown in the final PDF/DOCX/HTML export artifact

Rules:
- the X should be visible and easy to use in preview
- it should not break table layout
- it should work consistently across all row-based tables where manual row deletion is useful
- preserve existing styling as much as possible

Acceptance requirement:
- every relevant preview table row has an X delete control
- deleting a row removes it from preview and downstream export state
- no X appears in final exported report artifacts

6) PRIMARY LOCATION — STOP FALLING BACK TO “MANUAL ENTRY NEEDED” WHEN THE LIVE SITE PROVIDES LOCATION EVIDENCE

Current problem:
Primary Location should not be landing on “Manual entry needed” when the website itself clearly provides location information.

Required fix:
- improve Primary Location resolution so the system can derive the primary location from the live website when possible
- use live website signals first, including:
  - contact/location pages
  - visible address/location language on the site
  - nav/footer location links
  - title/H1/location wording
  - other live page identity signals
- if the site clearly has an “Our Locations” or similar page and that is the best available location asset, use that consistently instead of “Manual entry needed”
- make sure the same Primary Location resolution is used consistently across header, Section 4, Section 5, and any other dependent sections

Important:
Do not just patch Bliss manually.
Fix the underlying Primary Location resolution logic so it works generally across clients.

Acceptance requirement:
- Primary Location no longer falls back to “Manual entry needed” when live website evidence clearly supports a location page or location asset
- Primary Location handling is consistent across all sections

==================================================
DO NOT CHANGE THESE THINGS
==================================================

Leave these alone unless absolutely required by the fixes above:
- current Section 4 mapping logic otherwise
- Section 6 Critical Observations tone
- major tier logic
- source hierarchy
- resolved live-page inventory architecture
- Anchored Tides-specific solved behaviors
- SF abbreviation behavior
- current red design language
- row count / keyword count logic outside the simplified Section 7 summary sentence
- existing correct CRO / refresh / optimize decisioning except where needed to satisfy the 3-create-new minimum

==================================================
NON-NEGOTIABLE PRESERVATION RULES
==================================================

Do not break any of the following already-solved behaviors:
- no fabricated generic paths
- no Anchored Tides residue
- real live page resolution in Sections 2 and 4
- honest call-tracking behavior
- no user-facing “service page” leakage where Levels of Care framing is required
- clean keyword clustering logic, aside from the new 10-variant display cap
- IOP and other confirmed live pages staying mapped correctly
- Section 8 terminology
- Section 9 authority framing
- source-badge no-wrap behavior

==================================================
FINAL VALIDATION REQUIREMENT
==================================================

Use the same validation flow already established.

Required steps:
1. Generate a fresh QBS using live connected data.
2. Validate the fixes above in preview and in exported output where relevant.
3. Export the actual PDF.
4. Provide a direct link/path to the PDF.
5. Provide a short QA note that covers only:
   - Section 5 “Why It Matters” tightening
   - Section 7 simplified summary sentence
   - Section 7 10-variant display cap
   - Section 7 minimum 3 create-new rows
   - preview row deletion controls across tables
   - Primary Location resolution fix
   - confirmation that no previously solved issue regressed

==================================================
FAILURE RULE
==================================================

Do not declare completion if any of the following remain:
- Section 5 “Why It Matters” still overstates blockage
- Section 7 still contains cap math phrasing
- any keyword row displays more than 10 variants
- fewer than 3 create-new keyword recommendations appear
- preview tables do not have functioning row delete controls
- Primary Location still says “Manual entry needed” despite clear live site evidence

Instead, list:
- exact section / feature
- exact remaining issue
- exact fix still required

FINAL INSTRUCTION

This is a narrow practical cleanup pass.
Fix only the requested items above so the QBS is more usable right now, preserve the rest, and provide the updated PDF artifact plus QA note.