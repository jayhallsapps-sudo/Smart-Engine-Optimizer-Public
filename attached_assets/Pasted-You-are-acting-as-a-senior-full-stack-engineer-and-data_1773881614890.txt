You are acting as a senior full-stack engineer and data reliability engineer.

I need you to FIX my reporting app, not give me a surface-level explanation.

There are two core problems:
1. Manual edits in reports are not being persisted and restored.
2. Integration-driven data is disappearing, not mapping correctly, or not rehydrating correctly.

Your job is to perform a full root-cause analysis, implement fixes, and return a thorough engineering report with evidence.

IMPORTANT OPERATING RULES
- Do not give me generic advice.
- Do not give me a list of possibilities without verifying them.
- Inspect the actual codebase, schemas, API routes, DB models, sync jobs, save/load flows, and UI state handling.
- Trace the data end-to-end.
- If something is broken, fix it.
- If something is ambiguous, make the safest durable architectural choice and explain it.
- Do not mark this complete until you have reproduced the issues, implemented a fix, and verified the fix with test evidence.
- Preserve existing working behavior wherever possible.
- No silent assumptions. Show me exactly what you changed and why.

==================================================
PART 1 — CURRENT FAILURE MODES TO DIAGNOSE
==================================================

A) BI-WEEKLY REPORT MANUAL ENTRIES ARE NOT SAVING

Problem:
When I manually edit sections like:
- Content
- Optimization
- Technical SEO
- Local SEO

...those edits do not persist into saved historical reports.

Observed behavior:
I edited the report manually, saved it, then reloaded it later.
The report reverted to default filler text like:
- “No content published this period”
- “No optimization work completed this period”
- “Add technical maintenance completed during this period”
- “No local SEO GBP work completed this period”

That is false data. The app is either:
- not saving manual edits,
- saving them to the wrong place,
- reloading generated defaults on top of saved data,
- or discarding the persisted manual state during report regeneration/hydration.

You must find the exact cause.

B) BI-WEEKLY CONTENT DATA IS NOT PULLING CORRECTLY

Expected source logic for bi-weekly reports:

1. “What We Did / Learned” for content-related items
- should pull from Airtable PUBLISHED view for the client
- should detect items published during the report period

For Sol Women’s Treatment Center, examples that should have appeared:
- levels of care hub page
- my wife drinks a bottle of wine a night

Classification:
- Fresh net-new article goes under Content
- Existing page improvement / refresh / page optimization goes under Optimization

So for example:
- “my wife drinks a bottle of wine a night” = Content
- “levels of care hub page” = Optimization

2. “What’s Next”
- should pull from Airtable PRODUCTION table/view for the client
- specifically for the next two weeks for bi-weekly reporting
- should identify upcoming scheduled items based on date

For Sol, the next two weeks should have shown 3 scale articles/content refreshes.

The app is not doing this correctly.

You must inspect:
- Airtable base/table/view mapping
- client-specific config
- field mapping
- date filtering logic
- classification logic
- timezone/date boundary handling
- whether the integration token/config is vanishing
- whether data is fetched but dropped before render
- whether data is rendered but overwritten by fallback text

C) TECHNICAL SEO SECTION LOGIC IS NOT WORKING

Expected “What We Did” logic:
- compare old Screaming Frog upload vs newest Screaming Frog upload
- items present in old and resolved/absent in new should count as completed/fixed technical work
- also pull completed Asana tasks containing “technical” in the task name

Expected “What’s Next” logic:
- highest-impact unresolved technical issues from the newest Screaming Frog upload

Current problem:
- comparative technical data is not showing
- old/new Screaming Frog comparison does not appear to exist in the report
- may not be exposed in the sidebar / report UI
- Asana technical task pulling may also be failing or incomplete

You must inspect:
- file ingestion for Screaming Frog uploads
- storage of historical crawl snapshots
- diff engine between prior and current snapshots
- Asana sync logic for completed tasks
- task-name matching and categorization
- priority scoring for unresolved technical issues
- report assembly logic

D) EVALUATION SHEETS ARE LOSING DATA / MAPPINGS

The evaluation sheet mappings were already defined clearly, but some values are now missing.

Expected mappings:

Manual / pulled fields:
- Whois Registry = from WHOIS lookup, registration date
- First Archive = from Wayback Machine, first archived date
- DR = Ahrefs batch analysis
- Backlinks = Ahrefs batch analysis
- Index Pages = Google site:domain result count
- AI Visibility Score = SEMrush AI tool
- Mentions = SEMrush AI tool
- Cited Sources = SEMrush AI tool
- Organic Traffic = Ahrefs batch analysis
- Organic Keywords = Ahrefs batch analysis
- Top 10 Keywords = Ahrefs top 1-3 + top 4-10
- Informational Keywords, US only = SEMrush organic rankings
- Featured Snippet / AIO = Ahrefs Organic Keywords report with SERP feature filters:
  Current + Include + Target In + Featured Snippet + AI Overview + Any

Formula-driven fields:
- everything after the pulled metrics that is supposed to be a formula
- all rank columns are formulas

Problems to diagnose:
- integrations appear to “disappear”
- mapped values are missing later
- formula columns may be overwritten or not regenerated
- source-to-column mapping may not be persisted
- evaluation sheet config may not be stable across reloads/deploys

You must find the exact reason.

==================================================
PART 2 — NON-NEGOTIABLE ARCHITECTURAL FIXES
==================================================

You must implement the following principles.

1. MANUAL REPORT CONTENT MUST BE FIRST-CLASS PERSISTED DATA
Manual edits cannot live only in frontend state.
They must be stored durably in the database with:
- report_id
- client_id
- report_type
- reporting_period_start
- reporting_period_end
- section_key
- raw_text / structured_content
- last_edited_by
- updated_at
- source = manual

If there is also generated content for the same section, store it separately.
Do NOT overwrite manual content with regenerated defaults.

Required rule:
If manual content exists for a report section, the UI must load manual content as the authoritative displayed value unless the user explicitly resets/regenerates it.

2. GENERATED CONTENT AND MANUAL CONTENT MUST BE SEPARATE LAYERS
Implement a layered content model for report sections:
- generated_value
- manual_override_value
- effective_value

Effective value logic:
- if manual_override_value exists and is non-empty, use it
- otherwise use generated_value
- fallback copy only if both are empty

3. NO SILENT DEFAULTS ON LOAD
The report loader must never inject fake fallback copy if saved data exists.
Fallback strings like “No content published this period” must only appear if:
- generation ran successfully,
- no matching source data existed,
- and no manual override exists.

4. INTEGRATION CONFIGURATION MUST BE PERSISTENT AND AUDITABLE
If an integration exists, I need to know:
- where the credentials/config are stored
- whether the mapping is per workspace, per client, or global
- when it was last validated
- whether it is active/inactive/broken
- the last successful sync timestamp
- the last failure reason

Create or fix an integration_status table/store with fields like:
- integration_name
- workspace_id
- client_id
- status
- last_success_at
- last_failure_at
- last_error
- config_hash or mapping fingerprint
- connection_validated_at

5. REPORT GENERATION MUST BE DETERMINISTIC AND TRACEABLE
For every generated report section, store provenance:
- source integration(s)
- query/filter used
- matched source records count
- source record IDs
- generation timestamp

I need to be able to inspect why a section was populated or left blank.

6. HISTORICAL SNAPSHOTS MUST EXIST FOR COMPARATIVE DATA
For things like Screaming Frog comparison, historical snapshots must be retained and queryable.
Do not treat uploads as temporary UI artifacts.
Store them as versioned crawl snapshots tied to:
- client
- uploaded_at
- source_type
- parsed_summary
- raw file reference

7. FORMULA/MAPPING CONFIG FOR EVALUATION SHEETS MUST BE IMMUTABLE UNLESS EXPLICITLY EDITED
Do not let deploys, migrations, or sync refreshes silently wipe mappings.
Persist:
- column definitions
- source mapping
- formula definitions
- rank formula definitions
- sort logic
- evaluation sheet version history

==================================================
PART 3 — EXACT BEHAVIOR TO IMPLEMENT
==================================================

A) BI-WEEKLY REPORT SAVE / LOAD REPAIR

Inspect and repair the full lifecycle:
- create report
- edit report manually
- save report
- reopen report
- load historical reports
- duplicate report if applicable
- regenerate report if applicable

Required result:
Manual edits remain intact across:
- refresh
- navigation away/back
- reopening historical report
- app restart
- deploy restart

Also:
- historical reports must preserve the exact text as it existed when saved
- unless an explicit edit is later made

B) BI-WEEKLY CONTENT PULL LOGIC

Implement/fix data assembly logic for:
1. Content — fresh net-new published articles in period
2. Optimization — updated/refreshed/optimized existing pages in period
3. What’s Next — upcoming scheduled production items in next two weeks

Use Airtable as follows:
- published data = “what we did / learned”
- production data = “what’s next”

You must identify the actual Airtable tables/views/fields currently configured in the codebase and fix the query logic rather than hardcoding nonsense.

Add classification logic that can distinguish:
- new content
- content refresh
- page optimization

If classification data is absent, implement a sane fallback using record type / status / tags / title conventions, and document it.

C) TECHNICAL SEO ASSEMBLY LOGIC

Implement/fix:
1. Completed technical work:
- diff previous Screaming Frog snapshot vs current snapshot
- identify resolved issues
- surface these in the “what we did” section

2. Completed technical Asana work:
- pull completed Asana tasks containing “technical” in the task name
- within the reporting period
- dedupe against Screaming Frog-resolved issues if needed

3. Upcoming technical priorities:
- analyze the newest Screaming Frog snapshot
- rank issues by likely impact
- surface highest-priority unresolved items under “what’s next”

Document the impact-ranking logic.

D) EVALUATION SHEET PERSISTENCE AND MAPPING REPAIR

Find why mapped values disappear.
Then fix:
- source field mappings
- integration fetch logic
- persistence
- hydration into the UI
- formula regeneration
- rank column formulas

Implement validation for each mapped metric:
- WHOIS registration date
- first archive date
- Ahrefs DR
- Ahrefs backlinks
- Google site index count
- SEMrush AI visibility
- SEMrush mentions
- SEMrush cited sources
- Ahrefs organic traffic
- Ahrefs organic keywords
- Ahrefs top 10 keywords
- SEMrush informational keywords US-only
- Ahrefs featured snippet / AI overview count

If any source requires user action, surface it explicitly in status, not by silently blanking fields.

==================================================
PART 4 — DEBUGGING AND TESTING REQUIREMENTS
==================================================

You must create and run a verification checklist.

At minimum include:

1. Manual report persistence test
- create report
- manually edit all four sections
- save
- reload
- confirm exact text persists

2. Historical report integrity test
- open prior saved report
- confirm previously saved text is preserved

3. Generated + manual layering test
- generate content
- manually override one section
- regenerate report
- confirm manual override is preserved unless explicitly reset

4. Airtable sync test
- fetch published items for period
- fetch production items for next two weeks
- show raw matched records and final categorized output

5. Screaming Frog diff test
- load two snapshots
- show identified resolved issues
- show identified unresolved high-priority issues

6. Asana technical task test
- fetch completed technical tasks in range
- show inclusion/exclusion logic

7. Evaluation sheet mapping test
- fetch each mapped source
- show value returned
- show value written to sheet/store
- show value rendered in UI
- confirm formula columns still work

8. Integration durability test
- restart app / simulate cold boot
- verify integration config still present
- verify report still loads with persisted data

==================================================
PART 5 — DELIVERABLE FORMAT FOR YOUR RESPONSE
==================================================

Return your work in this exact format:

1. EXECUTIVE SUMMARY
- What was actually broken
- What you fixed
- What remains blocked, if anything

2. ROOT CAUSE ANALYSIS
For each issue:
- symptom
- exact root cause
- affected files/modules
- why it happened

3. CODE CHANGES
List every changed file and what changed in each file.

4. DATA MODEL CHANGES
List any DB/schema/storage changes.
Include migrations if added.

5. INTEGRATION STATUS REVIEW
For each integration:
- Airtable
- Asana
- Screaming Frog
- Ahrefs
- SEMrush
- evaluation sheet sources

Report:
- whether it is connected
- where config lives
- whether mapping persists
- last known failure point
- what you changed

6. TEST EVIDENCE
Show results of the required tests above.
I want concrete evidence, not “should now work.”

7. OPEN RISKS / FOLLOW-UPS
Anything still fragile or requiring product decisions.

8. NO-FLuff HANDOFF
Tell me exactly what I should test next in the UI.

==================================================
PART 6 — EXTRA IMPLEMENTATION GUARDRAILS
==================================================

- Add logging around report save/load paths.
- Add logging around integration hydrate/sync paths.
- Add defensive checks so fallback copy cannot overwrite non-empty saved content.
- Add a visible debug/provenance panel in admin/dev mode for each report section showing:
  - source
  - generated at
  - manual override present yes/no
  - matched record count
  - source IDs
- Add a visible integration health panel if one does not already exist.

==================================================
PART 7 — PRODUCT LOGIC TO RESPECT
==================================================

The app must reflect the actual reporting philosophy:
- bi-weeklies are momentum and relationship reporting, not fake filler
- monthlies are strategy and insight, not noise
- technical work must be planned, tracked, and visible
- documentation is part of the job, not optional

Do not reduce missing data to generic placeholder copy if real data exists or manual edits were made.

Proceed now:
1. inspect codebase
2. reproduce issues
3. fix them
4. run verification
5. return the full report in the required format