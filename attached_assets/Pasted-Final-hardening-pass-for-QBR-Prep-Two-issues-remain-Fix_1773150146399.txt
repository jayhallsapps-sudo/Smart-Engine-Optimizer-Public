Final hardening pass for QBR Prep. Two issues remain. Fix both cleanly without changing the current report structure, preview layout, or PDF formatting.

Goals:
1. Move the export/finalization gate from frontend-only behavior to true server-side enforcement as well
2. Make the monthly content credit map a true single source of truth instead of redefining/copying it in routes.ts

Do not change:
- Section 2 structure
- Section 6 structure
- preview rendering
- print/PDF layout
- Strategy Bank logic
- existing AM cross-sell classification UX, unless needed to support the server-side gate

ISSUE 1 — SERVER-SIDE ENFORCEMENT FOR FINALIZATION / EXPORT

Current state:
- Frontend correctly disables Download DOCX, Download PDF, and Save to Google Drive when:
  - auditMissing === true
  - unresolved crossSellPreview items still exist
- That is good for normal use, but it is still only a UI gate

Required change:
Add true server-side enforcement so the report cannot be finalized/exported if required prerequisites are not satisfied, even if someone bypasses the UI.

What must be enforced server-side:
A. Audit gate
- If audit notes are missing/blank, export/finalization must be blocked

B. Cross-sell classification gate
- If Section 6 returned crossSellPreview items and the AM has not resolved them yet, export/finalization must be blocked
- “Resolved” means the saved edits map includes s6_crossSells_confirmed, even if that value is "[]"

Routes / actions that must be protected:
- any PDF export endpoint / action
- any DOCX export endpoint / action
- any Google Drive save/export action tied to this report
- any other route that finalizes or persists the report as a completed export artifact

Implementation rules:
1. Do not trust frontend state alone
2. Recompute or validate the gating conditions from server-side inputs / stored report state
3. Return a clear validation error payload when blocked
4. Error messaging should distinguish:
   - missing audit input
   - unresolved cross-sell classification
   - both missing at once

Suggested response shape:
{
  ok: false,
  code: "QBR_PREP_EXPORT_BLOCKED",
  reasons: [
    "Audit input is required before export.",
    "Cross-sell / upsell preview items must be classified before export."
  ]
}

Behavior requirements:
- Frontend gate stays in place
- Server now enforces the same logic independently
- If blocked, no PDF/DOCX/Drive artifact should be created
- Do not silently continue or generate partial export artifacts

State / validation requirements:
- Use the report’s saved reportData + edits map as the source of truth when validating export readiness
- Do not require the AM to regenerate if the missing condition has already been satisfied in saved state
- If crossSellPreview was never generated or is empty, no classification gate should apply
- If s6_crossSells_confirmed exists, treat the preview as resolved even if it contains an empty array

ISSUE 2 — TRUE SINGLE SOURCE OF TRUTH FOR CONTENT CREDIT CAPACITY

Current state:
- The generator-local credit map was removed, which was good
- routes.ts now defines/resolves QBR_CLIENT_CREDIT_MAP before passing monthlyCredits into the generator
- Better than before, but still not ideal if the same map already exists elsewhere

Required change:
Use one shared canonical credit map / resolver instead of redefining or copying it in routes.ts.

Implementation rules:
1. Identify the existing canonical client credit source already used elsewhere in the project
2. Import and use that exact shared source in the QBR Prep route flow
3. Do not duplicate the map values in routes.ts
4. If a shared resolver/helper already exists, use that instead of importing raw constants
5. Keep the explicit fallback for unknown clients, but make it secondary only

Required behavior:
- routes.ts should no longer redefine a local QBR_CLIENT_CREDIT_MAP copy
- monthlyCredits passed into generateQbrPrepReport should come from the shared canonical source
- generator should continue using input.monthlyCredits ?? fallback
- fallback should remain explicit and documented

ARCHITECTURE / CLEANLINESS REQUIREMENTS

- Keep the change as small and surgical as possible
- Avoid broad refactors
- Preserve current report generation behavior
- Preserve current PDF output
- Preserve existing preview behavior
- Do not introduce duplicate validation logic in multiple places if a shared helper can be used cleanly

RECOMMENDED IMPLEMENTATION APPROACH

For server-side export gating:
- Create a small shared validator/helper for QBR Prep export readiness
- Input should be enough to evaluate:
  - auditMissing
  - crossSellPreview existence
  - s6_crossSells_confirmed resolution state
- Use that helper in all QBR Prep export/finalization routes

Suggested helper behavior:
validateQbrPrepExportReadiness(reportData, edits) -> {
  canExport: boolean,
  reasons: string[]
}

Suggested logic:
- auditMissing = reportData?.section6Priorities?.auditMissing === true
- crossSellPreview = reportData?.section6Priorities?.crossSellPreview ?? []
- crossSellPending = crossSellPreview.length > 0 && edits["s6_crossSells_confirmed"] === undefined
- canExport = !auditMissing && !crossSellPending

For canonical credit sourcing:
- Find the shared client credit config already used elsewhere
- Import it instead of recreating it
- If the existing project has a canonical resolver, prefer:
  resolveClientMonthlyCredits(clientName)
  over manual map lookup inside routes.ts

TESTING / ACCEPTANCE CRITERIA

1. Frontend still disables export buttons when blocked
2. Server also blocks export/finalization when blocked, even if the route is hit directly
3. Blocked export returns a clear error payload with specific reasons
4. No PDF is created when audit input is missing
5. No PDF is created when cross-sell preview exists but has not been classified
6. Export succeeds when:
   - audit notes exist
   - cross-sell preview is empty, or resolved via s6_crossSells_confirmed
7. routes.ts no longer contains a duplicated/local copy of the credit map
8. monthlyCredits now comes from the real shared canonical source
9. Existing PDF export still works end to end after these changes

After implementation, give me:
- files changed
- exact server-side validation flow
- exact routes/actions now protected by the validator
- exact blocked error response shape
- exact canonical credit source now being imported/used
- confirmation that routes.ts no longer duplicates the credit map
- confirmation that PDF/DOCX/Drive export still work when requirements are satisfied
- any edge cases or remaining limitations