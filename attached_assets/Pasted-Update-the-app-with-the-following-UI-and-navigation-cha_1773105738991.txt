Update the app with the following UI and navigation changes.

Main change:
Do not build the new client info panel on the Clients page anymore.
Instead, add it to the Dashboard view for each client.

Dashboard changes:
Keep the existing top section as-is:
- title
- refreshed
- connected services
- graph

Directly below the graph, add a tabbed section with:
1. Stats
2. Client Info

Behavior:
- Stats should be the default open tab
- Client Info should be the tab to the right of Stats
- Switching tabs should not affect the graph or top summary area
- Keep the UI clean and consistent with the existing app styling

Stats tab:
- This should contain whatever currently lives below the graph now
- Do not remove existing stats behavior; just place it under the Stats tab

Client Info tab:
Move the business/account info feature here instead of the Clients page.

Show the following fields in Client Info:
Top section:
- Website attached to the client
- Credits allocated to the client
- NSM type

Quarter sections:
Create two clearly labeled subsections:
1. Current Quarter
2. Next Quarter

For each quarter, show:
- Organic Sessions Actual
- Organic Sessions Goal
- MVP NSMs Actual
- MVP NSMs Goal
- % to Organic Sessions Target
- % to MVP NSM Target

Data source:
- Pull quarter-based data from the “SEO NSM Tracker 2026”
- Pull:
  - actual organic sessions
  - goal organic sessions
  - actual MVP NSMs
  - goal MVP NSMs
  - NSM type
- Pull website and credits from the correct existing source of truth
  - if already stored on the client record, use that
  - if stored in SEO NSM Tracker 2026, map it from there
- Do not use fake placeholder values

Quarter logic:
- Determine Current Quarter dynamically from the current date
- Determine Next Quarter dynamically from the current quarter
- Do not hardcode quarter labels

Fallback behavior:
- If any Next Quarter values are not available yet, display an em dash: —
- If any value is missing, display —
- If a goal is missing or 0, the related percentage should display — rather than erroring

Calculations:
- % to Organic Sessions Target = Organic Sessions Actual / Organic Sessions Goal * 100
- % to MVP NSM Target = MVP NSMs Actual / MVP NSMs Goal * 100
- Format percentages cleanly
- Handle divide-by-zero safely

Important:
- This feature should no longer change the Clients page UI at all
- Leave the Clients page behavior alone unless required for shared data plumbing only

Navigation and naming updates:
Update the sidebar / navigation labels and ordering as follows.

Reports section order:
1. Mid-Strategy
2. Bi-Weekly
3. Monthly
4. QBR Prep
5. QBR

Naming rules:
- Keep “Bi-Weekly” named exactly that
- Keep “Monthly” named exactly that
- Rename “QBR Full” to “QBR”
- Keep “QBR Prep” named exactly that
- Keep “Mid-Strategy” named exactly that

Tools section:
- Keep Clients
- Remove History entirely
- Keep Sample Exports
- Keep Template Builder
- Rename Setup to Integrations
- Add a new page called Security

Recommended Tools order:
1. Clients
2. Sample Exports
3. Template Builder
4. Integrations
5. Security

Integrations page improvements:
- Rename any “Setup” references in the UI to “Integrations”
- Add a short explanation on the Integrations page that explains what commands/actions are available there
- Make it obvious what each integration can do and what is currently connected
- Keep existing integrations, including Google Docs, QSSB, and Notion SEO StrategyBank

Notion SEO StrategyBank fix / validation:
- Investigate why “Notion SEO StrategyBank requires pages to be added” is showing
- If I already authorized all the relevant pages, the integration should not appear empty
- Notion should show the connected pages/databases it has access to
- Confirm whether the issue is:
  1. a bad connection state in the UI
  2. a page discovery issue
  3. an OAuth scope / token issue
  4. a mapping issue after authorization
- Fix the display if the data is actually available
- If pages truly are not accessible, surface a clear reason rather than a generic empty state
- I need this confirmed before we move forward, so do not ignore this piece

Security page:
Create a new Tools page called Security.

Purpose:
Give a quick overview of the app’s security posture in a simple card-based layout.

Add security status cards for the security measures already implemented.
Examples of card types:
- Encryption status
- Secrets / environment variable handling
- Auth protection status
- API key storage status
- Access control status
- Secure transport / HTTPS status
- Any other relevant security protections already in the app

Behavior:
- Each card should show:
  - security item name
  - current status
  - short explanation
- Do not invent protections that do not exist
- If a security measure is not implemented, show that honestly
- If a status cannot yet be verified programmatically, label it clearly as unverified instead of pretending it is active

Engineering requirements:
- Reuse existing data-fetching and dashboard structures where possible
- Keep the dashboard tab state stable and responsive
- Avoid layout jumpiness
- Add loading states for Client Info if needed
- Add empty states where appropriate
- Keep the implementation scalable across many client dashboards

Acceptance criteria:
1. Dashboard now has two tabs below the graph: Stats and Client Info
2. Stats is the default open tab
3. Client Info shows website, credits, NSM type, current quarter metrics, and next quarter metrics
4. Client Info values pull from the correct real sources, including SEO NSM Tracker 2026 where applicable
5. Missing values safely display as —
6. Percentages calculate correctly and fail safely
7. Clients page is no longer changed by this feature
8. Reports are reordered and renamed correctly
9. History is removed from Tools
10. Setup is renamed to Integrations
11. Integrations page explains available commands/actions
12. Notion SEO StrategyBank connection state is investigated and corrected if the data is already authorized
13. New Security page exists under Tools
14. Security page shows honest card-based status for implemented protections
15. Existing functionality outside these changes remains intact

After implementation, report back with:
- exact files changed
- exact nav/order changes made
- how Client Info data was mapped
- how quarter logic was implemented
- what was found with the Notion SEO StrategyBank connection issue
- what the Security page is reading from vs what is hardcoded
- any assumptions made because of inconsistent source data