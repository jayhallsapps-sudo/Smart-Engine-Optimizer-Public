You are doing a final, evidence-first implementation pass on SmartEO.

Your goal is not to redesign the product from scratch.
Your goal is to make the platform functionally match the attached decks, spreadsheets, and report samples as closely as possible, while removing fake completeness and fabricated data.

==================================================
PRIMARY SUCCESS CONDITION
==================================================

The next pass is successful if the reports and evaluation sheets allow the team to move forward, meaning:
1. the reports and decks work,
2. the system has done everything it can to minimize missing values,
3. any remaining missing values display as `—`,
4. every `—` has a clear explanation of why the data is missing,
5. no numeric KPI or source-backed metric is fabricated by AI.

==================================================
SOURCE-OF-TRUTH PRIORITY
==================================================

Use this priority order exactly:

1. Attached decks and spreadsheets
2. Attached SOPs / report docs / samples
3. Current live app behavior
4. AI interpretation

If current app behavior conflicts with the decks or spreadsheets, the decks and spreadsheets win.

==================================================
OVERALL SCOPE
==================================================

Do all of the following in one pass:

1. finish the remaining high-priority fixes
2. produce a production-readiness report
3. produce a metric integrity matrix
4. produce a fake-completeness audit
5. implement or correct code where needed

==================================================
PROTECTED MODULES
==================================================

Bi-Weekly and QBS are protected design/template anchors.

DO NOT materially redesign:
- Bi-Weekly
- QBS

That means:
- do not change their overall structure
- do not change their visual language unless required for shared platform consistency
- do not reimagine their layout

You MAY still change:
- source routing
- evidence / provenance handling
- readiness validation
- naming cleanup
- shared cross-report functions
- shared upload flows
- shared tooltip behavior
- shared integration logic
- shared report infrastructure

Bi-Weekly and QBS are also design references for:
- reds
- line styles
- fonts
- table styling
- overall visual language

Other report/deck modules should build from that design language unless the attached sample deck for that module clearly overrides it.

==================================================
MODULE PRIORITY
==================================================

Highest priority:
- Monthly
- QBR
- Mid-Strategy
- evaluation-sheet support
- fake completeness cleanup
- metric integrity

Lower priority:
- ACA

Discoverability scope:
- primarily a keyword ideation tool
- must stop fabricating numbers
- can use real enrichment where available
- should not be expanded into a broader analytics system unless needed to support current functionality

Nimbata:
- do not spend time changing Nimbata right now
- do not prioritize Nimbata work in this pass
- if Nimbata remains incomplete, just report it honestly

==================================================
TARGET STATE BY MODULE
==================================================

1. Bi-Weekly
Protected.
Keep structure and look intact.
Only fix shared/report-infrastructure issues and routing/provenance problems.

2. QBS
Protected.
Keep structure and look intact.
QBS is the internal strategy/evidence layer.
It should feed QBR.
Fix routing, evidence quality, provenance, and naming only.

3. QBR
QBR should match the attached QBR sample deck as closely as possible.
It should do this by using the information from QBS and rendering that information into the client-facing deck structure shown in the attached QBR sample.

Strong rule:
- the attached QBR sample deck is the rendering source of truth

4. Monthly
The attached Monthly deck is both:
- the rendering source of truth
- the function source of truth

Make Monthly as close to the attached sample deck as possible.
If the current generator has extra sections, extra slides, or invented structure not present in the sample, remove them, hide them, or make them optional internal-only sections.
Default rendered output should match the sample.

Build its design using the visual language of Bi-Weekly and QBS unless the Monthly sample explicitly overrides something.

5. Mid-Strategy
The evaluation sheets are the prerequisite and data source for Mid-Strategy.
The Mid-Strategy deck depends on the evaluation sheets for its data.

Strong rule:
- the evaluation sheets are the data source of truth
- the AI writes the narrative from the evaluation-sheet data
- the AI does not create an alternate data model
- if code output conflicts with the spreadsheets, the spreadsheets win

The attached Mid-Strategy deck is the rendering target.
The attached evaluation sheets/spreadsheet logic are the functional target.

==================================================
MISSING DATA RULE
==================================================

If a field has no real data, display:
`—`

Do not use:
- fake placeholders
- invented values
- soft bluff text pretending data exists

Every `—` must have an explanation.

Required behavior:
- in UI: hover tooltip explaining why the value is `—`
- in generated report/deck output: include a report-visible explanation or source note so the missing value is not unexplained

Examples:
- `—` because GSC not connected
- `—` because evaluation sheet column not populated
- `—` because source exists but no rows returned for this period
- `—` because this provider does not expose that field
- `—` because integration is configured but not implemented

==================================================
VALIDATION / UI INSPECTION RULE
==================================================

Do NOT use UI navigation as the primary way to infer routing or backend truth.
Use code, integrations, field maps, and actual generator logic for that.

You MAY use current UI/routes/components/pages as a validation layer only:
- verify what users actually see
- verify visible badges/tooltips/placeholders
- verify whether rendered reports match the attached decks
- verify whether a module falsely appears complete

==================================================
HARD DATA RULES
==================================================

AI may:
- summarize
- prioritize
- synthesize
- write narratives
- explain missing data
- turn evaluation-sheet data into report copy

AI may NOT:
- fabricate KPI values
- fabricate keyword volumes
- fabricate difficulty
- fabricate rankings
- fabricate call counts
- fabricate crawl metrics
- fabricate benchmark metrics
- fabricate evaluation-sheet values

If a metric cannot be sourced, it must be `—` with explanation.

==================================================
DISCOVERABILITY RULES
==================================================

Treat Discoverability as primarily a keyword ideation tool.

Required:
- stop fabricating numbers completely
- if search volume is unavailable, show `—`
- if KD is unavailable, show `—`
- if position is unavailable, show `—`
- every missing numeric value needs a tooltip explanation
- keep provenance visible where appropriate

Do not expand Discoverability into a broader analytics engine unless that is required for current output integrity.

==================================================
MID-STRATEGY / EVALUATION RULES
==================================================

The evaluation sheets are the functional truth.
The Mid-Strategy deck is downstream of those sheets.

Required:
- preserve evaluation-sheet column logic
- preserve formula logic
- preserve ranking logic
- preserve category rollup logic
- preserve benchmark logic
- preserve sheet-to-deck dependency

The system should:
1. read / compute the evaluation-sheet style data
2. use that as the sole data foundation
3. let AI write the explanatory narrative from that data

No alternate data story.

If formulas or ranks in code disagree with the evaluation-sheet model, fix code to match the evaluation-sheet model.

==================================================
REQUIRED DELIVERABLES
==================================================

Return all of the following:

A. Production readiness table
For each module:
- module_name
- status: Ready / Partially Ready / Blocked
- reason
- blocker files/services
- exact next step

Modules:
- ACA
- Discoverability
- Bi-Weekly
- Monthly
- QBS
- QBR
- Mid-Strategy
- Dashboard
- Clients
- Setup Integrations

B. Required integrity fixes before production trust
Only include issues that materially affect data correctness or create fake completeness.
For each:
- issue
- user impact
- affected modules
- files/services involved
- exact implementation step
- severity

C. Metric integrity matrix
For every major user-facing metric across:
- Dashboard
- Monthly
- QBS
- QBR
- Mid-Strategy
- Discoverability

Return:
- metric_name
- module
- source_of_truth
- fallback_source
- null_behavior (`—` rule)
- provenance_shown
- tooltip_explains_missing? yes/no
- validator_dependency
- AI_allowed_to_fill? yes/no

D. Fake-completeness cleanup list
Identify every place where the UI or generated output implies live support or completeness that does not truly exist.

Return:
- screen/module
- misleading UI element
- why it is misleading
- recommended fix
- blocking or non-blocking

Examples to check for:
- badges without real backend support
- dead columns that always show blanks without explanation
- partially rendered slides that look complete
- categories driven only by availability flags
- source labels that imply stronger data than actually exists

E. File-by-file implementation report
For every file created or modified in this pass:
- file path
- why changed
- functions added/changed
- modules impacted
- complete or partial

F. Exact modules fixed
List exactly what was fixed in:
- Monthly
- QBR
- Mid-Strategy
- Dashboard
- Discoverability
- Setup Integrations
- Clients
- Bi-Weekly shared infrastructure
- QBS shared infrastructure

G. Remaining blockers
For each blocker:
- blocker
- why still blocked
- required credentials / scope / data / implementation
- affected module
- what the UI should show until fixed

==================================================
SPECIAL IMPLEMENTATION DIRECTIVES
==================================================

1. Monthly
Make it match the attached sample deck as closely as possible.
The attached Monthly deck is the rendering and function source of truth.

Rules:
- remove or hide extra slides/sections not in the target deck
- default output should conform to the sample
- use `—` instead of “manual entry needed” where data is missing
- explain `—` with report note and tooltip behavior where applicable

2. QBR
Make QBR match the attached QBR sample deck as closely as possible.
QBR should transform QBS evidence into the sample QBR deck format.

Rules:
- QBS is the evidence/input layer
- QBR is the rendered/client-facing deck
- do not invent deck sections beyond the sample unless optional and clearly marked

3. Mid-Strategy
Use the evaluation sheets as the required data layer.
Deck text should be written from the evaluation data.
No extra AI-generated “evidence.”

Rules:
- the evaluation sheets are prerequisite
- if evaluation data is missing, show `—` with explanation
- do not fake benchmark fields
- do not fake AI visibility fields
- if a field is not implemented or not available, show `—` and explain why

==================================================
DO-NOT-TOUCH RULES
==================================================

Do not materially redesign:
- Bi-Weekly format
- QBS format

Do not treat:
- sample decks
- spreadsheets
- evaluation sheets
as “reference inspiration.”

They are the target.

Do not return another fluffy audit summary.
Do not over-explain already-settled items.
Do not prioritize cosmetic cleanup over data integrity.
Do not make unfinished support look complete.

==================================================
FINAL OUTPUT STYLE
==================================================

Use:
- markdown tables where useful
- short direct notes where needed
- explicit field-level detail
- implementation-level evidence

Do not give broad prose.
Do not give generic reassurances.
Do not say something is implemented unless it is truly functional.

Execute the fixes and return the deliverables above.