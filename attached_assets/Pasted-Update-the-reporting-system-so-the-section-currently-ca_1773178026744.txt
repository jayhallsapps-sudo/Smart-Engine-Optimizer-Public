Update the reporting system so the section currently called “How Credits Are Used Each Month” is renamed to “Possible Credit Usage Each Month” and behaves differently between preview and export.

Requirements:

Rename only

Rename the section label everywhere in the app preview from:

How Credits Are Used Each Month

To:

Possible Credit Usage Each Month

Preview behavior

Keep this section visible in the in-app preview.

It should continue rendering exactly as it does now in preview, aside from the new title.

Do not remove any existing preview functionality tied to this section.

Export behavior

Exclude this entire section from every export output.

That means it must not appear in:

PDF exports

print exports

any downloadable/exported report format used by QBR Prep, QBR Full, Monthly, Biweekly, Mid-Strategy, or any other reporting export path

The section should still exist in the underlying report data if needed for preview, but it must not render in export output.

Scope

Apply this change consistently across all report types that currently support this section.

Do not remove or alter any other sections.

Do not change the logic used to calculate or populate the credit usage rows unless required for hiding it in exports.

Implementation expectation

Use a preview-only render condition or an export-hide condition so the section remains available in preview but is fully suppressed in exported versions.

Keep naming and logic clean so future team members can clearly understand that this is an intentionally preview-only section.

QA checklist
Confirm all of the following:

In preview, the section appears with the new title: “Possible Credit Usage Each Month”

In preview, the section content still works as before

In export, the section does not appear at all

No blank gap, empty card, or leftover heading appears where the section used to be

No other report sections are affected

Return:

files changed

whether this was presentation-only or required logic changes

which report/export paths were verified

confirmation that the section is still visible in preview and fully hidden in exports

Reasoning for the change:
The section is useful internally for planning and review, but it should not be client-facing in exported deliverables. That aligns with the broader expectation that reporting should stay focused on business outcomes, clarity, and client-safe presentation. 

SEO Account Manager Job Descrip…

 

How We Operate (READ FIRST)

If you want, I can also make this stricter by adding language that it must be hidden at the component/render layer, not just via CSS.

Sources
Another small change Olivia asked for, the QBR prep should be called the QBR snapshot. So, let's make a prompt for the credits thing as well as that.
Thought for a couple of seconds

Use this as the combined prompt for Replit:

Update the reporting system so two presentation changes are applied consistently:

The section currently called “How Credits Are Used Each Month” should be renamed to “Possible Credit Usage Each Month,” remain visible in preview, and be excluded from all exports.

The report currently called “QBR Prep” should be renamed to “QBR Snapshot” anywhere user-facing.

Requirements

A. Credits section: preview-only, renamed

Rename the section label everywhere in the app preview from:

How Credits Are Used Each Month

To:

Possible Credit Usage Each Month

Keep this section visible in the in-app preview.

It should continue rendering exactly as it does now in preview, aside from the new title.

Do not remove any existing preview functionality tied to this section.

Do not change the logic used to calculate or populate the credit usage rows unless required for hiding it in exports.

B. Credits section: remove from exports

Exclude this entire section from every export output.

It must not appear in:

PDF exports

print exports

any downloadable/exported report format used by QBR Snapshot, QBR Full, Monthly, Biweekly, Mid-Strategy, or any other reporting export path

The section can still exist in underlying report data if needed for preview, but it must not render in export output.

No blank gap, empty card, spacer, or orphaned heading should remain where the section would have appeared.

C. Rename QBR Prep to QBR Snapshot

Change all user-facing references of “QBR Prep” to “QBR Snapshot”

Apply this consistently across:

page titles

buttons

dropdowns

saved report selectors

report headers

export labels

preview labels

navigation or tabs

empty states

toasts or success messages

any visible UI copy shown to users

Do not rename internal identifiers, route names, database values, reportType enums, or API contracts unless absolutely necessary.

Prefer a presentation-layer rename only, so the system can still function on the existing QBR Prep wiring if that is how it is currently built.

If there are places where the visible label is derived from a shared report-type mapping, update that mapping cleanly so “QBR Snapshot” becomes the default user-facing label.

D. Scope and guardrails

Apply both changes consistently across all affected report types and export paths.

Do not alter unrelated report logic.

Do not change report scoring, thresholds, data selection, or generation behavior.

Keep implementation clean and obvious so future team members can tell:

the credits section is intentionally preview-only

QBR Snapshot is the user-facing name for QBR Prep

E. QA checklist
Confirm all of the following:

Credits section

In preview, the section appears with the new title: “Possible Credit Usage Each Month”

In preview, the section content still works exactly as before

In exports, the section does not appear at all

No blank gap, empty card, or leftover heading appears in export output

No other report sections are affected

QBR Snapshot rename

“QBR Prep” no longer appears anywhere user-facing

“QBR Snapshot” appears correctly in all visible UI locations where QBR Prep used to appear

Existing generation, preview, save/load, and export flows still work

Saved reports that were previously created under the old QBR Prep system still load correctly if that flow already existed

No internal breakage was introduced by the label rename

Return

files changed

whether the work was presentation-only or required any logic changes

which preview and export paths were verified

confirmation that:

“Possible Credit Usage Each Month” is preview-only

“QBR Snapshot” is now the user-facing label

internal wiring for the former QBR Prep flow still works correctly

If you want, I can also make a second version of this prompt that is more forceful and QA-heavy, so Replit has less room to interpret it loosely.

Yes, give me a prompt that is more forceful and QA-heavy so that Replit has less room to interpret it loosely.
Thought for a second

Use this version:

Make the following changes exactly as specified. Do not reinterpret the request, do not partially apply it, and do not make unrelated “cleanup” changes.

Two changes are required:

The section currently labeled “How Credits Are Used Each Month” must be renamed to “Possible Credit Usage Each Month,” kept visible in preview, and fully removed from all exports.

The report currently labeled “QBR Prep” must be relabeled everywhere user-facing as “QBR Snapshot.”

Non-negotiable implementation rules

Treat these as controlled presentation changes unless a minimal render-path adjustment is required to satisfy export suppression.

Do not change report logic, scoring, thresholds, classification behavior, source selection, or data generation unless absolutely required to hide the credits section in exports.

Do not rename internal route names, reportType enums, database values, saved-report types, API request/response contracts, or any other internal identifiers unless doing so is truly unavoidable. Default assumption: internal wiring stays as-is, user-facing labels change.

Do not leave any mixed terminology behind. “QBR Prep” should not remain visible anywhere user-facing after this change.

Do not hide the credits section with a fragile CSS-only trick unless the export renderer is guaranteed to respect it. The requirement is functional suppression from export output, not visual approximation.

Do not create empty wrappers, blank cards, stray headings, or spacing artifacts in exported output.

Change 1: Credits section must become preview-only

Current label:

How Credits Are Used Each Month

New label:

Possible Credit Usage Each Month

Required behavior:

In the in-app preview, the section must still render.

In the in-app preview, the section title must now read exactly:

Possible Credit Usage Each Month

The section content, rows, editability, and existing preview behavior must remain intact unless a separate known issue already required change.

In every export output, the entire section must be absent.

“Absent” means:

no section heading

no table

no wrapper/card/container

no spacer

no reserved vertical gap

no page-break artifact

no placeholder text

Apply export suppression to every export path, including but not limited to:

PDF export

print export

downloadable export views

any export/render path used by:

QBR Snapshot

QBR Full

Monthly

Biweekly

Mid-Strategy

any other report type that renders this section

Implementation expectation:

Use an explicit export-mode guard at the render layer, or an equivalent robust condition that prevents the section from being rendered in export output.

Preview must still receive and render the section.

Export output must not render it at all.

Change 2: QBR Prep must become QBR Snapshot everywhere user-facing

Current user-facing label:

QBR Prep

New user-facing label:

QBR Snapshot

Required behavior:

Replace all visible UI references to “QBR Prep” with “QBR Snapshot”

Apply everywhere a user can see the label, including but not limited to:

page titles

report titles

buttons

dropdowns

saved report selectors

report headers

export labels

preview labels

navigation

tabs

empty states

toasts

modals

confirmation text

status copy

helper copy

any other visible interface text

Internal compatibility requirement:

Existing internal QBR Prep wiring must continue working.

Existing saved reports generated under the former QBR Prep flow must still load if they already loaded before.

Existing generation flow must still function.

Existing export flow must still function.

Existing preview flow must still function.

Existing save/load flow must still function.

What must not happen:

No user-facing screen should show “QBR Prep”

No export should show “QBR Prep”

No mixed state where some surfaces say “QBR Prep” and others say “QBR Snapshot”

No breaking changes to existing QBR Prep internals if the system currently depends on those internals

Acceptance criteria

A. Credits section

In preview, the section is visible

In preview, the section title reads exactly “Possible Credit Usage Each Month”

In preview, the section behaves the same as before unless explicitly required otherwise

In export, the section is completely absent

In export, there is no leftover heading, gap, card, wrapper, or spacing artifact

No other report sections are removed or changed unintentionally

B. QBR Snapshot rename

“QBR Prep” no longer appears anywhere user-facing

“QBR Snapshot” appears everywhere the old user-facing label used to appear

Existing generation still works

Existing preview still works

Existing save/load still works

Existing export still works

Existing saved reports remain compatible

Internal wiring remains intact unless a minimal necessary change was required

Required QA process

Do not return a vague “done” message. Perform verification and report it explicitly.

Verify at minimum:

Preview render for the former QBR Prep report

Export render for the former QBR Prep report

At least one additional export/report path where the credits section could appear

Saved report selector / saved report load path

At least one visible UI surface each for:

navigation or tab label

page/report header

action button or dropdown

export label or output title

Required return format

Return the final report in this structure:

Files changed

List every file changed

Change type

State clearly whether the work was:

presentation-only

presentation plus render-path changes

or anything else

State clearly whether any internal identifier had to be changed

Credits section verification

Confirm exactly where it still appears

Confirm exactly where it no longer appears

Confirm no export artifact remains

Name the preview and export paths tested

QBR Snapshot rename verification

Confirm which visible surfaces were checked

Confirm “QBR Prep” no longer appears anywhere user-facing

Confirm saved report compatibility status

Confirm generation/preview/export flow status

Guardrail confirmation

Confirm no report logic, scoring, thresholds, or signal behavior were changed

Confirm no unrelated UI was modified

Any unavoidable deviations

If anything could not be done exactly as requested, state it plainly and explain why

Definition of success

The work is only complete if both conditions are true:

The credits section remains available in preview under the exact new title and is fully absent from all exports

“QBR Prep” has been fully replaced by “QBR Snapshot” everywhere user-facing without breaking the underlying flow