We are making a strict QBS product update pass.

Do not treat this as a light UI cleanup.
Do not partially implement.
Do not improvise around the request.
Do not keep legacy behavior “for now.”
This is a hard logic + presentation + QA task.

==================================================
OBJECTIVE

Implement the following QBS changes exactly:

Remove “Possible Credit Usage Each Month”

Replace it with a new section:
Suggested Keywords for Next Quarter

Rebuild Additional Opportunities styling and content formatting

Standardize source-tag behavior across all QBS tables/sections

Remove the Source column from Section 1 and replace it with source tags inside Goal Type

Remove any table columns that are redundant once source tags are applied

Eliminate vague “e.g.” / “EG” style examples in recommendations and replace them with actual specific pages/keywords/paths whenever the system has enough data

Force the generator to pull crawl, sitemap, page inventory, and performance data from the integrated app sources instead of treating them as optional

==================================================
PART 1 — REPLACE “POSSIBLE CREDIT USAGE EACH MONTH”

Delete the existing section:

Possible Credit Usage Each Month

Replace it with a new QBS section:

Suggested Keywords for Next Quarter

This is not a cosmetic rename.
This is a new recommendation engine section.

A. PURPOSE OF THE NEW SECTION

The new section must:

query the connected integrations

identify the most strategically relevant keywords to focus on next quarter

connect those keywords to existing relevant pages where possible

recommend new content where no suitable page exists

stay grounded in the client’s available quarterly credits

give enough options to be useful, but not so many that it becomes unrealistic

B. INPUTS THE SECTION MUST USE

The keyword recommendation logic must use available data from relevant integrations/sources, including:

GSC

GA4

Ahrefs

SEMrush

Screaming Frog

Airtable / work tracking if helpful

NSM / QBS context

existing page inventory

sitemap / URL inventory

crawl data

Do not fabricate “strategic keywords” from thin air.
Recommendations must be grounded in actual site/search/opportunity data.

==================================================
HARD DATA-ACCESS RULE

Do not treat site crawl, sitemap, page inventory, or performance data as optional or conditional.

For QBS generation, the system must assume it has access to these through the app’s integrations and internal data pipeline.

That means the generator is required to explicitly pull and use:

site crawl data

sitemap / URL inventory

page inventory

performance data

relevant connected integration outputs

Do not use language like:

“if available”

“if the system has access”

“where possible”
for these core inputs.

The app already has access to them.
The generator must use them.

If one of those sources genuinely fails to return data at runtime, that should be treated as a data retrieval issue or integration issue, not as a reason to fall back to vague recommendations.

C. CREDIT-AWARE RECOMMENDATION LOGIC

This section must respect the client’s quarterly credit reality.

Credit count must be derived from the client’s monthly credits × 3 months in the quarter.

Example:
Anchored Tides = 4 credits/month
Quarter total = 12 credits

Recommendation count rule:

Maximum keyword recommendations = no more than 2x total quarterly credits

This is meant to provide options, not an infinite wish list

Example:
12 quarterly credits → max 24 recommended keyword opportunities

Do not exceed that cap.

Also:
The recommendation engine must be mindful of approximate implementation weight based on Skill.md / existing internal credit logic.

That means it should understand that opportunities may map to different work types such as:

new blog content

content refresh

service page refresh

CRO/content update

new service/supporting page

internal linking improvement tied to an existing page

other approved QBS-relevant work types

The list should not be naive.
It should reflect what is actually realistic to execute within the client’s quarterly credit budget.

D. OUTPUT STRUCTURE FOR “SUGGESTED KEYWORDS FOR NEXT QUARTER”

For each recommendation row, include at minimum:

Keyword

Recommendation Type

optimize existing page

refresh existing page

create new content

CRO/supporting update

internal linking support
or similar grounded action type

Target Page / Suggested Asset

if an associated page exists, show the actual page

if no strong page exists, explicitly say:
Suggest new content for this keyword

Why It’s Recommended

concise but meaningful

grounded in available search/performance/site data

Source Tags

use source tags, not a raw source column if avoidable

tag based on the actual source(s) used for the recommendation

E. REVISED PAGE MATCHING RULE

For every keyword recommendation, the generator must explicitly query:

crawl data

sitemap / page inventory

relevant performance data

Then it must determine whether a relevant page already exists.

Required behavior:

Pull the actual existing page candidates from the integrated crawl/page inventory data

Match the keyword against real existing pages

If a relevant page exists, list the actual page

If no relevant page exists, explicitly output:
Suggest new content for this keyword

Do not skip the page-matching step.
Do not use example pages.
Do not use placeholder paths.
Do not use “e.g.” or “EG.”
Do not act as if page inventory is unavailable.

The system already has access to the site inventory and must use it.

F. DO NOT OUTPUT GENERIC FILLER

Do not output:

vague keyword lists with no action

“example” pages

“EG” or “e.g.”

fake optionality

recommendations disconnected from credits

recommendations disconnected from real page inventory

==================================================
PART 2 — ADDITIONAL OPPORTUNITIES SECTION REBUILD

The Additional Opportunities section needs both design and content cleanup.

A. HEADER / STYLE FIX

The gray bar styling currently used for upsell/cross-sell header treatment is wrong.

Change it so the section visually matches the red-led QBS design system used elsewhere.
It must feel like part of the same report family.

Do not keep a random gray header band if the rest of the report uses the red system.

B. TAG / TOKEN FORMATTING

Any keywords, domains, paths, URL slugs, subdomains, extensions, or similar structured values inside Additional Opportunities must render as tags.

By “tags,” use the same visual tag treatment already used elsewhere in QBS, such as the colored square-ish source tags like:

CallRail

GA4

GSC

Screaming Frog

Multi-source

Do not render these values as raw comma-separated inline text.

Examples of values that should be tags:

keywords

domains

/meet-the-team/

/verify-insurance/

subdomain suggestions

extension/path references

page slugs

The tags should visually alternate so they are distinguishable.
Do not dump them as one plain comma list.

C. REMOVE “EG” / “EXAMPLE” STYLE LANGUAGE

If the opportunity says something like:

add internal links for high-traffic pages with low Admit connection to primary service and VOB pages

Then the system must list the actual pages it wants acted on.
No “EG”
No “e.g.”
No generic example placeholders

D. REVISED ADDITIONAL OPPORTUNITIES RULE

For Additional Opportunities, whenever the recommendation refers to:

pages

paths

slugs

domains

subdomains

internal linking targets

VOB pages

service pages

trust pages

high-traffic pages

the generator must explicitly pull the real URLs / paths from the integrated crawl, sitemap, and page inventory data.

It must not write:

EG

e.g.

example pages

placeholder slugs

generic illustrative paths

It must output the actual pages from the site.

Since the app already has access to:

the site crawl

the sitemap

the page inventory

the performance data

there is no acceptable reason to leave these recommendations generic.

If a recommendation says:
“add internal links from high-traffic pages to primary service and VOB pages”

the output must name the actual pages as tags.

==================================================
PART 3 — SOURCE TAG STANDARDIZATION ACROSS QBS

We need one consistent source-tag rule across the report.

A. CORE RULE

If an entire table/section is drawn from a single source, place the source tag next to the table/section heading.

Do not repeat the same source tag redundantly in every row if the whole table shares that source.

If individual rows in a table come from different sources, then render source tags at the row level.

This rule applies across the entire QBS.

B. EXAMPLES

If Top Traffic Pages is entirely GA4:

show GA4 tag beside the “Top Traffic Pages” heading

do not repeat GA4 in every row

If a table is truly multi-source by row:

each row can have its own source tags

If a table is uniformly multi-source as a whole:

one Multi-source tag at the heading level may be acceptable, but only if truthful

C. ENFORCEMENT RULE

There should be nothing in QBS that lacks source identification.

Every meaningful table or section must either have:

heading-level source tag(s), or

row-level source tag(s)

No orphaned unattributed sections.

==================================================
PART 4 — SECTION 1 SOURCE COLUMN REMOVAL

In Section 1:
What Matters Most This Quarter

Remove the Source column entirely.

Current desired columns:

Goal Type

Goal

Goal Shift vs Last Quarter

Reason

Source must instead appear as tag(s) inside the Goal Type cell.

Example:
Primary Goal / MVP NSM
[CallRail]

Secondary Goal / NSM
[GA4]

Do not keep a separate Source column if the source is already clear from the row and can be rendered as a tag under Goal Type.

Also:
any other table columns that become redundant once source tags are applied should be removed where appropriate.

==================================================
PART 5 — SECTION 2 SOURCE COLUMN CLEANUP

Review Section 2:
Where Conversions Actually Happen

The current source-column behavior needs cleanup.

If a subsection/table is coming from one source:

move that source indication to the heading-level tag

remove redundant source column if that is cleaner

Example:
If Top Converting Pages is from one source, tag the heading and remove redundant source column if present.

But:
if row-level source differences are real, preserve them via row tags.

Do not mechanically remove source columns without checking truthfulness.
Apply the heading-level vs row-level source-tag rule properly.

==================================================
PART 6 — TABLE-BY-TABLE SOURCE TAG RULE

Apply this rule to every QBS table:

Determine whether the table’s data source is:

single-source for entire table

multi-source by row

multi-source for entire table as one combined output

Render tags accordingly:

single-source whole table → heading-level tag

multi-source by row → row-level tags

whole-table multi-source → heading-level Multi-source or multiple tags if clearer

Remove redundant source columns where the source can be conveyed more cleanly by tags.

Keep the report visually cleaner while preserving truth.

==================================================
PART 7 — QA ACCEPTANCE CRITERIA

Do not mark complete until every item below passes.

NEW KEYWORD SECTION

“Possible Credit Usage Each Month” is fully removed

“Suggested Keywords for Next Quarter” is present

keyword recommendations are grounded in real integration/site data

recommendations respect quarterly credit cap

total recommendations do not exceed 2x quarterly credits

each recommendation includes an actual page if a suitable page exists

when no page exists, output explicitly says to suggest new content

no “EG” / “e.g.” / fake examples appear

recommendation types are meaningful and actionable

generator explicitly pulls crawl, sitemap, page inventory, and performance data

no recommendation uses placeholder/example pages when real page inventory exists

ADDITIONAL OPPORTUNITIES
12. gray header styling is replaced with red-led QBS styling
13. keywords/domains/paths/slugs render as tags, not comma-dump text
14. no “EG” / “e.g.” placeholders remain
15. internal-linking or page-based recommendations list actual pages from integrated site data

SOURCE TAG STANDARDIZATION
16. every meaningful table/section has source identification
17. single-source tables use heading-level tags
18. multi-source-by-row tables use row-level tags
19. redundant source columns are removed where appropriate
20. no unattributed table remains

SECTION 1
21. Source column is removed
22. source tag appears inside Goal Type cell for each row
23. table remains readable and visually clean after column removal

SECTION 2
24. source behavior is cleaned up according to heading-level vs row-level rule
25. no redundant source columns remain where heading-level tag is more truthful and cleaner

==================================================
PART 8 — REQUIRED OUTPUT

Return a formal proof report with these exact sections:

Files changed

New keyword section logic

exact integrations queried

exact credit-aware cap logic

exact page-matching logic

exact output structure

Additional Opportunities rebuild

styling changes

tag rendering changes

removal of EG/example behavior

proof that actual site pages are being used

Source-tag system

exact table-by-table source-tag decisions

which tables now use heading-level tags

which tables now use row-level tags

which source columns were removed

QA scenarios tested

one line per scenario

pass/fail

observed result

Before/after proof
Show at minimum:

old credit-usage section vs new keyword section

old Additional Opportunities formatting vs new formatting

old source-column behavior vs new source-tag behavior

Final status

explicitly state whether the QBS update is fully complete

==================================================
FINAL HARD RULE

If the new keyword section is still generic, if recommendations are not grounded in real site/search data, if Additional Opportunities still uses placeholder/example language, or if source tags are inconsistently applied, the task is not complete.