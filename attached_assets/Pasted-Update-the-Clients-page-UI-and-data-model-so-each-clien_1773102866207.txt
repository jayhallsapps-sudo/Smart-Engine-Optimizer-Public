Update the Clients page UI and data model so each client row is compact by default and can expand into one of two detail panels.

Goal:
Make the Clients page easier to scan while still exposing two kinds of detail on demand:
1. Client Info
2. Connected Sources

Core UI changes:
- On initial page load, each client entry should be collapsed.
- In the collapsed state, show only:
  - client name
  - existing small icon next to the name
  - pencil/edit icon
  - delete icon
- Do not show the connected integrations panel by default anymore.
- Under the main client row, add two buttons:
  - Client Info
  - Connected Sources

Expand / collapse behavior:
- Clicking Client Info opens a slide-down panel directly beneath that client.
- Clicking Connected Sources opens a slide-down panel directly beneath that client.
- Only one of those two panels can be open at a time for a given client.
- If Client Info is open and the user clicks Connected Sources, close Client Info and open Connected Sources.
- If the user clicks the currently open button again, collapse that panel.
- Keep the interaction smooth and obvious. Use the existing styling language of the app.
- Preserve the edit and delete actions exactly as they work now.

Connected Sources panel:
- This panel should display the current integrations / connected sources UI that already exists today.
- Functionally, it should be the same panel we already have, just hidden behind the Connected Sources button instead of always visible.

Client Info panel:
Create a new panel called Client Info with the following fields.

Top section:
- Website attached to the client
- Credits allocated to that client
- NSM type

Quarter sections:
Show two separate subsections:
1. Current Quarter
2. Next Quarter

For each quarter, show:
- Organic Sessions Actual
- Organic Sessions Goal
- MVP NSMs Actual
- MVP NSMs Goal
- % to Organic Sessions Target
- % to MVP NSM Target

Data source:
- Pull all quarter-based business metric data from the “SEO NSM Tracker 2026”
- Pull:
  - actual organic sessions
  - goal organic sessions
  - actual MVP NSMs
  - goal MVP NSMs
  - NSM type
- Also surface the website attached to the client and the client’s credit entitlement in this panel
- If those values already live in the client record, use the client record as the source of truth
- If any of them are stored in the SEO NSM Tracker 2026, map them from there cleanly
- Do not create fake placeholder data

Quarter logic:
- Determine Current Quarter dynamically from the current date
- Determine Next Quarter dynamically based on the current quarter
- Example:
  - if current date is in Q1, show Q1 as Current Quarter and Q2 as Next Quarter
- Do not hardcode quarter names

Fallback rules:
- If any Next Quarter values do not exist yet, show an em dash: —
- If goal values are missing or zero, show — for the corresponding percentage field rather than breaking the UI
- If any mapped metric is unavailable, show —
- Make missing data obvious but clean, not alarming

Percentage calculations:
- % to Organic Sessions Target = Organic Sessions Actual / Organic Sessions Goal * 100
- % to MVP NSM Target = MVP NSMs Actual / MVP NSMs Goal * 100
- Format percentages cleanly
- If the denominator is missing or 0, return —

Display requirements:
- Keep the panel readable and structured
- Use labels that are plain and client-safe
- “MVP NSMs” is an acceptable label
- Group current quarter and next quarter clearly so I can quickly compare what exists and what does not
- Make it easy to tell what is connected, what is working, and what data is available

Engineering requirements:
- Reuse existing client list and integration rendering where possible
- Do not duplicate the integrations logic; move it behind the Connected Sources panel
- Add proper loading states for Client Info if the NSM data needs to be fetched
- Add empty states where appropriate
- Avoid layout jumpiness
- Keep this scalable for many clients on the page

Acceptance criteria:
1. Clients page loads with all client cards collapsed
2. Each client card shows only name, icon, edit, delete, and two buttons: Client Info / Connected Sources
3. Connected Sources opens the current integrations panel for that client
4. Client Info opens a new slide-down panel with:
   - website
   - credits
   - NSM type
   - Current Quarter metrics
   - Next Quarter metrics
5. Current and next quarter values are pulled from SEO NSM Tracker 2026 where applicable
6. Missing next quarter data displays as —
7. Percentages calculate correctly and fail safely
8. Only one detail panel is open per client at a time
9. Existing edit/delete functionality remains unchanged
10. UI feels cleaner and easier to scan than the current always-expanded version

After implementing:
- Tell me exactly which files were changed
- Explain how the SEO NSM Tracker 2026 fields were mapped
- Note any assumptions you had to make about the tracker schema
- Flag anything that still needs manual field mapping if the sheet structure is inconsistent