Fix source label / badge rendering in QBR Prep only.

Problem:
QBR Prep is still not showing source labels/badges in the places where they should appear, even though the approved design system expects them.

I am not asking you to change the wording or fallback text right now.
Do not touch the internal/client-facing language yet.
Focus only on getting the labels/badges to render correctly.

==================================================
GOAL
==================================================

QBR Prep should display source labels/badges in the approved style, similar to the examples I attached.

Examples of source labels:
- Airtable
- Asana
- Screaming Frog
- GA4
- GSC
- GBP
- CallRail
- CTM
- Nimbata
- SEMrush
- Ahrefs
- Manual entry needed

These should appear inline where source attribution is meaningful.

==================================================
SCOPE
==================================================

Fix this only for QBR Prep.

Check at minimum:
- Section 2: Where Conversions Actually Happen
- Section 3: Top Organic Traffic Drivers
- Section 6: What We Need to Do Next
- Section 7: What We Track

If other QBR Prep sections also have source attribution fields, fix them too.

==================================================
REQUIRED FIX
==================================================

1. Identify every QBR Prep row/cell/field where source attribution exists or should exist.
2. Render those sources as badges/labels, not plain strings.
3. Use the shared source badge component/style system already established or intended in the app.
4. Preserve the existing report content and editability.
5. Apply this in preview and export where reasonably supported.

==================================================
IMPLEMENTATION RULES
==================================================

- Do not redesign the report.
- Do not change the text wording yet.
- Do not remove or rewrite fallback logic.
- Do not patch just one row manually.
- Fix the rendering path so QBR Prep consistently displays source badges anywhere source metadata is present.

Likely causes to check:
- QBR Prep may still be outputting plain text strings instead of structured source metadata
- preview components may not be calling the shared badge renderer
- export render may not be using the same source display logic
- some fields may need a small schema adjustment so source data is passed separately from display text

==================================================
EXPECTED BEHAVIOR
==================================================

Examples:
- a row driven by Airtable should show an Airtable badge
- a row driven by Asana should show an Asana badge
- a technical recommendation should show a Screaming Frog badge
- a conversion note from GA4 should show a GA4 badge
- a visibility note from GSC should show a GSC badge
- a source-driven call row should show CallRail / CTM / Nimbata / GBP badge where applicable

If multiple sources contribute to a row, render multiple badges cleanly if the design supports it.

==================================================
PROOF REQUIRED
==================================================

Return:
1. files changed
2. which QBR Prep sections/fields now support source badge rendering
3. whether you had to change the data structure or only the renderer
4. proof from at least:
   - one Section 2 row
   - one Section 3 row
   - one Section 6 row
showing the labels/badges visible
5. confirmation whether preview and export both now show the labels correctly