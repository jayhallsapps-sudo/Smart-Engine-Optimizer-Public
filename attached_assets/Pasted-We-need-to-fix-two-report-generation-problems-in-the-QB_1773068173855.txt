We need to fix two report-generation problems in the QBR / CBR prep flow.

Problem 1: Wrong client location is being extracted
Problem 2: “What Matters Most This Quarter” is using the wrong primary and secondary goals and the wrong measurement sources

Make the following changes.

PART 1 — FIX LOCATION EXTRACTION

Issue:
The report is incorrectly identifying Anchored Tides as “Blue Shield, CA” instead of Huntington Beach, CA. The parser appears to be grabbing insurer/logo text like “Blue Shield CA” and treating it as a city/state pair.

Required fix:
Replace any loose “first City, State match” or “first X CA match” logic with a stricter business-location extraction system.

Use this location extraction hierarchy, in this exact order:

Structured schema / JSON-LD business address fields

addressLocality

addressRegion

streetAddress

postalCode

Visible NAP block or footer business address

Explicit location phrases in body copy such as:

“Located in Huntington Beach”

“in Huntington Beach, CA”

“facility in Huntington Beach”

Canonical client location stored in config / client settings

Only then, fallback text heuristics

Add a hard exclusion list so insurer / payer / logo text can never be interpreted as location data.

Blacklist at minimum:

Blue Shield

Blue Cross

Blue Cross Blue Shield

Anthem

Aetna

Cigna

GEHA

Highmark

Horizon

Humana

Kaiser

Magellan

Optum

Tricare

UnitedHealthcare

UHC

Health Net

Molina

Medicare

Medicaid

Validation rules:

If a candidate contains a blacklisted insurance / payer term, reject it

If the candidate looks like a brand or insurer instead of a city, reject it

If multiple location candidates exist, prefer the one repeated in schema, footer, and contact page copy

Service area pages must not override the real facility location

Add a client-level canonical location override

For Anchored Tides, set canonical location to:

city: Huntington Beach

state: CA

Add debug logging for location extraction:

all location candidates found

source of each candidate

rejected candidates

reason for rejection

final selected location

Acceptance criteria:

Anchored Tides resolves to Huntington Beach, CA

“Blue Shield, CA” can never appear in any report as the client location

insurance/logo text is ignored for location extraction

service area pages do not override the facility city

PART 2 — FIX “WHAT MATTERS MOST THIS QUARTER”

Issue:
Section 1 is currently surfacing the wrong priorities and the wrong measurement sources.

Current incorrect behavior:

Primary goal is showing as Organic Sessions

Secondary goal is showing as Organic + GMB + AI / LLM Calls

Measurement source is showing as Google Sheets NSM + GA4

Required business logic:
The primary goal should always be admits.
The secondary goal should always be organic sessions.

Measurement source logic must be:
Primary goal: admits

source of truth = active call tracking platform for that client

use whichever is active:

CallTrackingMetrics

CallRail

Nimbata

do not show Google Sheets NSM as the client-facing measurement source

do not show GA4 / GSC as the measurement source for admits

Secondary goal: organic sessions

source of truth = GA4, with GSC as the supporting SEO source

client-facing display should show:

“GA4 / GSC”

do not show Google Sheets NSM in the client-facing source label

Important rule:
Google Sheets NSM can still be used internally for targets, pacing, or reconciliation, but it should never appear in the client-facing report language as the measurement source.

Required output format for Section 1:
Primary goal:

label: Admits

measurement source: CallTrackingMetrics, CallRail, or Nimbata, depending on active integration for the client

Secondary goal:

label: Organic Sessions

measurement source: GA4 / GSC

Implementation details:

Update the section-one goal mapping logic

Add a data-source priority system:

admits → call tracking provider only

organic sessions → GA4 / GSC

Add a client integration lookup:

determine which call tracking provider is active per client

display that provider as the source label

Remove Google Sheets NSM from client-facing measurement source text

Keep internal sheet values available for benchmarking only if needed behind the scenes

Add debug logging for Section 1:

selected primary goal

selected secondary goal

selected source label for each

raw data source used behind the scenes

active call tracking provider detected for the client

Acceptance criteria:

Section 1 primary goal always displays as Admits

Section 1 secondary goal always displays as Organic Sessions

Admits measurement source shows only the active call tracking platform

Organic Sessions measurement source shows GA4 / GSC

Google Sheets NSM never appears in client-facing source labels

Anchored Tides should not show Organic Sessions as the primary goal

Anchored Tides should not show Google Sheets NSM + GA4 as the measurement source for admits

If helpful, add unit tests for:

location extraction with both “Huntington Beach” and “Blue Shield CA” present in the same page text

section-one rendering where:

active call tracking provider = CallRail

admits exists

GA4 sessions exist

NSM sheet values exist
and confirm the display is:

Primary Goal: Admits | Source: CallRail

Secondary Goal: Organic Sessions | Source: GA4 / GSC

Expected Anchored Tides result:

Location: Huntington Beach, CA

Primary Goal: Admits

Primary Measurement Source: whichever call tracking platform is active for AT

Secondary Goal: Organic Sessions

Secondary Measurement Source: GA4 / GSC

And here’s a shorter version if you want something more punchy for Replit:

Fix two report issues.

Location extraction is wrong.
The parser is reading insurer text like “Blue Shield CA” as a city/state and outputting “Blue Shield, CA.” Replace loose text matching with a strict hierarchy:
schema address → visible NAP/footer → explicit location copy → client canonical location → fallback heuristic.
Blacklist insurer/payer names from ever being used as locations.
Set Anchored Tides canonical location to Huntington Beach, CA.
Log all candidates, rejections, and final source.

Section 1 “What Matters Most This Quarter” is wrong.
Primary goal must always be Admits.
Secondary goal must always be Organic Sessions.
Admits must use the active call tracking provider as the client-facing measurement source:
CallTrackingMetrics, CallRail, or Nimbata.
Organic Sessions must use GA4 / GSC as the client-facing measurement source.
Google Sheets NSM can be used internally, but must never appear in client-facing source labels.

Acceptance criteria:

Anchored Tides location = Huntington Beach, CA

“Blue Shield, CA” never appears

Primary goal = Admits

Secondary goal = Organic Sessions

Admits source = active call tracking provider

Organic Sessions source = GA4 / GSC

Google Sheets NSM never appears client-facing in Section 1