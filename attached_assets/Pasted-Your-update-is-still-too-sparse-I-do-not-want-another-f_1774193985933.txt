Your update is still too sparse.

I do not want another feature summary. I want an evidence-based validation report with concrete examples, exact file-level detail, exact data flow, and proof that the implemented behavior actually works.

From this point forward, do not give me short summaries like “fully delivered” or “all test steps passed” unless you also show exactly what passed and how.

For your next response, use this exact structure and fill every section in detail.

==================================================
1. EXECUTIVE STATUS
==================================================

Start with one of these only:
- FULLY COMPLETE AND VERIFIED
- COMPLETE WITH KNOWN GAPS
- PARTIALLY COMPLETE
- NOT COMPLETE

Then explain in 5–10 sentences:
- what is truly finished
- what is still missing
- what is proven versus assumed
- whether the current implementation is safe for production right now

==================================================
2. FILE-BY-FILE IMPLEMENTATION REPORT
==================================================

For every file added or changed, provide a table with these columns:

- File path
- New or modified
- Exact purpose
- Main functions/components/classes added
- Routes/endpoints touched
- Shared dependencies used
- Regression risk level (low / medium / high)
- Why it should not affect other report types

Do not summarize vaguely.
I want every file listed.

==================================================
3. ROUTES AND DATA FLOW
==================================================

List every frontend route and backend API route involved in the Templates system and the Quarterly Content Roadmap flow.

For each one, explain:
- route path
- request payload shape
- response shape
- who calls it
- what happens next in the flow

Then give me two end-to-end flow maps:

A. Template editing flow
Example:
Sidebar > Templates > Template Card > Visual Editor > Edit Element > Commit Template > Save API > Persisted Layout > Reload

B. Quarterly Content Roadmap generation flow
Example:
Creator > Client/Quarter/Year select > Generate > QBS lookup > Airtable lookup > Build report JSON > Preview > Save > PPTX export

Spell out the exact steps, not a summary.

==================================================
4. TEMPLATES SYSTEM — PROOF OF CAPABILITIES
==================================================

For the QCR visual editor, I need proof of each capability.

For each capability below, provide:
- where it lives in the UI
- how it works technically
- what object/state is updated
- whether it persists after reload
- whether it affects future generated decks

Capabilities to prove:
- template list visible in sidebar
- template cards visible on /templates
- open visual editor
- select element
- deselect element
- drag element
- resize element
- edit x/y/width/height numerically
- change fill color
- change text color
- change font size
- change font weight
- change font style
- change alignment
- duplicate element
- delete element
- save template
- reload saved template
- reset active slide to defaults

For each one, be concrete. Do not say “supported” without describing how.

==================================================
5. EXACT TEMPLATE DATA MODEL
==================================================

Show me the actual template data model used for QCR layout persistence.

I want:
- the structure of qcr_layout
- how slide types are keyed
- how elements are represented
- what properties exist per element
- how text versus shapes differ
- how deleted/duplicated elements are represented
- where template_config.json lives
- whether the save format is versioned
- what happens if the JSON is malformed or missing

If useful, include a real sanitized JSON example.

==================================================
6. QUARTERLY CONTENT ROADMAP — EXACT IMPLEMENTATION
==================================================

Do not just say “11-slide deck.”
List every slide in order and explain:

- slide number
- slide purpose
- content source
- editable fields
- whether layout comes from template/default/customized template
- whether the slide is affected by template edits

I want the exact slide inventory.

==================================================
7. QBS MAPPING — EXACT LOGIC
==================================================

I do not want “same logic as QBR.”

I want exact logic.

Explain:
- how selected client is resolved to the correct QBS record
- how quarter/year is matched
- what happens if multiple QBS records exist
- what happens if no QBS record exists
- which exact fields/sections/blocks are read for each month
- how month-specific strategy bullets are created
- how reasoning is transformed into client-facing copy
- what fallback logic is used when a field is empty

Also provide one real sample mapping for Anchored Tides:
- which QBS record was selected
- what April strategy content came from
- what May strategy content came from
- what June strategy content came from

If anything is inferred rather than explicitly mapped, say so.

==================================================
8. AIRTABLE MAPPING — EXACT LOGIC
==================================================

I do not want “fetches Airtable deliverables.”

I want exact logic.

Explain:
- which Airtable table is queried
- which fields are read
- which fields are displayed
- how client matching works
- how month/year filtering works
- how sorting works
- how deduping works if applicable
- what fields are excluded from client-facing output
- what happens if records are incomplete
- what happens if a month has zero records

Also provide one real sample mapping for Anchored Tides:
- April rows returned
- May rows returned
- June rows returned

If anything is fallback-generated, say so.

==================================================
9. SAVE / LOAD / EDIT PERSISTENCE
==================================================

Show exact proof for both:
A. Template editing persistence
B. Generated report editing persistence

For each, explain:
- what is stored
- where it is stored
- what key/identifier is used
- what reload path restores it
- whether edits survive page refresh
- whether edits survive leaving and returning
- whether edits flow into PPTX export

Be precise.

==================================================
10. PPTX EXPORT — PROOF
==================================================

I need a detailed export report.

Explain:
- which route creates the PPTX
- what payload it receives
- how report JSON is transformed into PPTX sections
- how template layout is applied during export
- whether live-edited report content is used
- whether live-edited template layout is used
- whether deleted template elements are absent from export
- whether yellow ADR boxes are excluded from export
- whether export matches preview exactly or approximately

If there are any limitations between browser preview and PPTX export, list them explicitly.

==================================================
11. NON-REGRESSION VALIDATION
==================================================

“All test steps passed” is not enough.

List every validation step performed.

For each validation step, include:
- what was tested
- how it was tested
- expected result
- actual result
- whether it was manual, automated, typecheck, build check, or runtime/browser check

I want explicit checks for:
- existing report creator still works
- existing report routes still work
- existing save/load still works
- existing PPTX exports still work
- existing sidebar items still work
- no admin-only gating issue introduced
- no TypeScript errors
- no Vite/runtime errors
- no broken imports
- no styling regressions on unrelated report pages

Also tell me what was NOT tested.

==================================================
12. SAMPLE ARTIFACT — ANCHORED TIDES
==================================================

Generate a real sample Quarterly Content Roadmap using Anchored Tides.

I need all of the following:
- preview generated successfully or not
- exact client/quarter/year used
- real populated slide content pasted into the response
- a direct download link to the PPTX file
- whether the sample used true live data, partial live data, or fallback data
- exact list of missing fields or fallback sections if any

Paste the sample content in this order:
- title slide
- quarter overview
- April strategy bullets
- April production table
- May strategy bullets
- May production table
- June strategy bullets
- June production table

==================================================
13. SCREENSHOT / VISUAL PROOF
==================================================

If possible, include or generate visual proof for:
- /templates list page
- /templates/:id editor with selected element
- Anchored Tides QCR preview
- exported PPTX opened/rendered if your environment allows it

If screenshots are not possible, say exactly why and what visual proof you can provide instead.

==================================================
14. KNOWN GAPS / RISKS
==================================================

End with a strict list of:
- known gaps
- known limitations
- unverified assumptions
- biggest regression risks
- what I should inspect first
- whether this is ready for production right now

Do not give me shallow language.
Do not compress your answer.
Do not say “working as expected” without evidence.
Do not hide behind summaries.

I want an audit-quality report.