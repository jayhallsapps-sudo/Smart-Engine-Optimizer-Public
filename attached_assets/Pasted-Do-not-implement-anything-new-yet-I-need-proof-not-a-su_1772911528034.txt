Do not implement anything new yet. I need proof, not a summary.

Show me concrete evidence for the shared report persistence and crawl asset system.

Return your answer in this exact structure:

1. Files changed
List every file modified for the shared save/load/crawl infrastructure and what changed in each file.

2. Schema / migration proof
Show the exact schema and migration changes for:
- unified saved_reports table
- sf_reports extensions
Include:
- table definitions
- indexes
- new columns
- how existing data was preserved or migrated

3. Shared service proof
Show the full API/service surface for:
- savedReportService.ts
- crawlAssetService.ts

For each service, show:
- exported functions
- what tables they read/write
- how they normalize report types
- how they handle current_crawl_asset_id and comparison_crawl_asset_id

4. API route proof
Show the exact routes added/changed for:
- /api/saved-reports/*
- /api/crawl-assets/*
- existing SF upload route

For each route, show:
- method
- request shape
- response shape
- one real example request/response

5. Shared hook proof
Show the code for useReportSave and explain:
- autosave debounce logic
- create vs update behavior
- save status states
- how stale closure problems are avoided
- how reload restores report data + edits

6. Shared component proof
Show the code or key props/contracts for:
- SaveStatusIndicator
- ReportSaveSelector
- CrawlAssetSelector

For each one, explain:
- required props
- loading states
- empty states
- error states
- how the UI differs when no saved reports or no crawls exist

7. Report integration proof
For each report type:
- Biweekly
- Monthly
- QBR Full
- QBR Prep

Show:
- what old custom logic was removed or replaced
- how the report now uses the shared save system
- how the report now uses the shared crawl selector
- whether current and comparison crawl IDs are persisted
- whether existing preview and export behavior still works

8. End-to-end proof by report type
Actually exercise each report type and show the full flow:
- generate
- autosave after edit
- manual save
- reload prior saved report
- select crawl
- select comparison crawl if supported
- export still works

I need one real proof sequence for each of the 4 report types.

9. Crawl asset library proof
Show:
- a real list of stored SF crawl assets for one client
- asset_name values in the required format
- uploaded_at values
- how a new upload gets named automatically
- how a previous crawl is selected
- how comparison crawl is selected

10. Persistence proof
Show database rows proving:
- multiple saved reports exist for the same client/report type
- edits are persisted
- crawl asset references are persisted
- last_saved_at updates after autosave/manual save

11. Regression proof
Show that the shared system did not break:
- Monthly generation
- Monthly export
- Biweekly generation
- Biweekly export
- QBR Prep generation
- QBR Prep export
- QBR Full generation/export if implemented

12. Known issues
List any remaining issues, compromises, or gaps.
Be honest. I do not want “everything complete” unless there are truly no known issues.

13. Final verification table
Return PASS / PARTIAL / FAIL for each:
- unified saved_reports works
- shared autosave works
- shared manual save works
- saved report selector works
- crawl asset selector works
- new SF uploads are named correctly
- previous crawls remain selectable
- comparison crawl selection works
- Biweekly integrated
- Monthly integrated
- QBR Full integrated
- QBR Prep integrated
- exports still work across report types

Important:
Do not summarize.
Show code, DB proof, request/response proof, and actual outputs.