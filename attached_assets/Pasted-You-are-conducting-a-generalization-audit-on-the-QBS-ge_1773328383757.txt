You are conducting a generalization audit on the QBS generator. This is not a feature-build pass. This is a vetting pass to determine which of the fixes made during the Anchored Tides debugging process are truly generalized across all clients, which are pattern-based but reusable, and which are still client-specific.

Do not give me a vague reassurance.
Do not tell me “it should work.”
Do not tell me “the logic is cleaner now” unless you prove how.
I need a direct audit of implementation scope and risk.

PRIMARY OBJECTIVE

Audit every meaningful fix made during the Anchored Tides QBS debugging process and classify whether it now works:

1. universally across all clients
2. pattern-based but generalizable
3. client-specific / hardcoded / account-dependent

The point of this audit is to determine whether the fixes now improve the QBS system as a whole, or whether we only made Anchored Tides look good.

==================================================
CORE AUDIT INSTRUCTIONS
==================================================

You must review the actual implementation logic and report back on whether each fix is:

- Universal
  Meaning: applies cleanly across all clients without relying on Anchored Tides-specific assumptions

- Pattern-based but generalizable
  Meaning: based on rules that can work across many clients, but still rely on recognizable patterns or page structures

- Client-specific
  Meaning: depends on Anchored Tides-specific URLs, labels, patterns, inventory assumptions, or fallback logic that would not safely generalize

For every fix you audit, you must provide:
- classification: Universal / Pattern-based but generalizable / Client-specific
- exact reason for that classification
- implementation location(s) in code
- risk level for other clients: Low / Medium / High
- what would need to change if it is not truly universal

==================================================
FIXES TO AUDIT
==================================================

Audit each of the following areas individually.

1) REAL LIVE PAGE RESOLUTION
Audit whether the report now resolves actual live pages from:
- live site
- sitemap
- crawl
- resolved page inventory

Questions to answer:
- Is this now generalized for all clients?
- Or does it rely on Anchored Tides-specific money-page assumptions?
- Are any URLs, slugs, or page-slot mappings hardcoded around Anchored Tides?
- Does the resolver work for unusual nested slugs generally, or only because Anchored Tides patterns were added?

2) DETOX / DETOXIFICATION AND OTHER CARE-CONCEPT NORMALIZATION
Audit whether care-concept normalization is now generalized.

Questions to answer:
- Does the system handle detox vs detoxification generically?
- Does it handle residential / inpatient, PHP / IOP, outpatient, dual diagnosis, verify insurance, contact/admissions in a reusable way?
- Are these mappings general pattern rules, or account-specific assumptions?

3) VERIFY INSURANCE / ADMISSIONS / HYBRID PAGE CLASSIFICATION
Audit the logic that correctly reclassified:
- /admissions-and-alcohol-rehab-insurance/

Questions to answer:
- Is this implemented as a general admissions/insurance pattern rule?
- Or is it effectively tailored to this one account?
- Would other hybrid slugs across clients classify correctly?

4) TOP CONVERTING PAGES SOURCE HIERARCHY
Audit whether Top Converting Pages now generally uses:
- GA4
- call-tracking integration
as primary proof, with GSC / SF as support

Questions to answer:
- Is that hierarchy now baked into the generator for all clients?
- Or was it only made to behave this way for Anchored Tides?
- Does it work across CallRail, CallTrackingMetrics, and Nimbata clients?

5) SOURCE LABELING / SHARED-SOURCE LOGIC
Audit whether:
- shared-source header labels
- row-level mixed-source labels
- SF abbreviation
are now generalized across the report system

Questions to answer:
- Is this global?
- Are there any sections or renderers where old behavior still exists?
- Is “SF” applied consistently anywhere compact source labels are used?

6) LEVELS OF CARE TERMINOLOGY STANDARDIZATION
Audit whether “Levels of Care” terminology is now generalized across all QBS outputs where appropriate.

Questions to answer:
- Is this a real system-wide terminology cleanup?
- Or were only Anchored Tides cases patched?
- Are there still client types or branches that will emit “service page(s)” when they should not?

7) TIER DIAGNOSIS FRAMEWORK
Audit whether the Tier logic now truly follows the QSSB-connected Webserv framework generally.

Questions to answer:
- Is the report now using the mapped Webserv tiers across all clients?
- Or are there still fallback branches using improvised logic?
- Are the fixes to Tier 1 / Tier 2 / Tier 4 narrative general, or were they just cleaned for Anchored Tides?

8) MANUAL QA NEEDED / FALSE NEGATIVE HANDLING
Audit whether false-negative handling is generalized.

Questions to answer:
- When the system cannot confirm something, does it now handle that consistently across all clients?
- Or are certain “not found” / “manual entry needed” states still special-cased?
- Is the system broadly less likely to mark real pages missing now, or only on this one client?

9) SECTION 4 PAGE SLOT MAPPING
Audit the logic that maps live pages into Section 4 rows like:
- Contact / Admissions
- Detox
- Residential / Inpatient
- PHP / IOP
- Outpatient
- Dual Diagnosis
- Verify Insurance
- Primary Location

Questions to answer:
- Is this generalized slot mapping logic?
- Or does it depend on Anchored Tides-specific verifiedMoneyPages or page patterns?
- Will it work for other treatment-center clients with custom nested URLs?

10) KEYWORD CLUSTERING
Audit whether the cleaned clustering behavior is generalized.

Questions to answer:
- Is the molly/tramadol/alcohol fix a generalized clustering improvement?
- Or was it achieved through Anchored Tides-specific stopwords and anchor logic?
- Could the current changes accidentally hurt other clients’ keyword grouping?

11) CREATE-NEW VS CRO / REFRESH DECISIONING
Audit whether the system now generally checks confirmed live inventory before recommending:
- create-new
vs
- CRO update / supporting update / refresh

Questions to answer:
- Is this now generalized?
- Or does it only behave correctly on Anchored Tides because specific pages were manually fed into inventory resolution?
- Will it correctly avoid recommending net-new content when an existing page already covers the intent on other clients?

12) SUMMARY BOX COUNTS / CLIPPED COPY / TEXT SANITIZATION
Audit whether these artifact-level fixes are generalized:
- summary counts matching actual row counts
- clipped copy in Critical Observations
- sanitizeString no longer stripping valid sentence endings

Questions to answer:
- Are these now system-safe improvements?
- Are there any remaining renderers or sections that still use old truncation logic?

13) PDF / RENDERER CONSISTENCY
Audit whether the latest fixes were implemented consistently across:
- preview
- print
- HTML
- DOCX / PDF generation path

Questions to answer:
- Are all renderers aligned now?
- Or are there still fixes that only exist in preview or only in one output path?

==================================================
REQUIRED OUTPUT FORMAT
==================================================

For each audited area above, output:

AREA NAME

Classification:
Universal / Pattern-based but generalizable / Client-specific

Implementation:
(list exact files, functions, helpers, or branches involved)

Why this classification:
(explain directly and specifically)

Risk for other clients:
Low / Medium / High

What would break or behave differently on another client:
(be honest and concrete)

What should be done next:
- leave as-is
- generalize further
- remove client-specific handling
- replace with reusable resolver logic
- add more test coverage
(or similar)

==================================================
MANDATORY FINAL SUMMARY
==================================================

After auditing all areas, give me 3 final sections:

1. SAFE TO GENERALIZE NOW
List the fixes that are truly system-wide and safe to trust across clients.

2. NEEDS GENERALIZATION
List the fixes that currently work but are only pattern-based or partially portable.

3. CLIENT-SPECIFIC RISK
List any fixes that appear Anchored Tides-specific, hardcoded, or risky for broader use.

Then give one final verdict:

FINAL VERDICT
Choose one:
- Mostly generalized
- Mixed: some generalized, some client-specific
- Still too Anchored Tides-specific

You must justify that verdict in plain language.

==================================================
VALIDATION REQUIREMENT
==================================================

Do not answer from memory or with assumptions.
Trace the actual implementation.
If you are not sure whether a fix is generalized or client-specific, say so and explain exactly why.
Honesty is more important than optimism.