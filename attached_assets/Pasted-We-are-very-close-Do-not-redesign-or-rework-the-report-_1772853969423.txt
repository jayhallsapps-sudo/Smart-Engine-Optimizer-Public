We are very close. Do not redesign or rework the report. Do not change the successful parts of the current export. Only make the specific fixes below so the exported PDF matches the preview cleanly.

Current required fixes:

Remove the header gap

Remove the extra blank page

Render the red X status icons correctly

Restore the footer on every page

Important guardrails:

Do not rescale the report

Do not shrink content to fit

Do not change fonts unless required for missing icon rendering

Do not alter table structure except where needed to keep icons rendering correctly

Do not rework page layout, spacing, or the overall report design

Do not switch export strategies again

Keep using the current fixed PDF layout that is now close

Use the following implementation requirements exactly.

Header must bleed flush to the page edges

The red header/background art must touch the top edge and both side edges of the PDF page.

There should be no white gap on the top, left, or right.

Required checks:

@page margin must be 0

html margin/padding must be 0

body margin/padding must be 0

.pdf-page must have no padding

header/background art must be positioned against the outer page wrapper, not inside the padded content wrapper

no parent wrapper should apply border, outline, transform, inset shadow, or padding that creates a visible gap

Use this CSS:

@page {
  size: Letter;
  margin: 0;
}

html, body {
  margin: 0;
  padding: 0;
  background: #111;
}

* {
  box-sizing: border-box;
}

.pdf-page {
  position: relative;
  width: 8.5in;
  height: 11in;
  margin: 0 auto;
  padding: 0;
  overflow: hidden;
  background: #fff;
}

.page-bg-header {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  pointer-events: none;
  z-index: 0;
}

.pdf-page-inner {
  position: relative;
  z-index: 1;
  width: 100%;
  height: 100%;
  padding: 0.7in 0.6in 0.5in 0.6in;
}

Important:

The header/background art belongs outside .pdf-page-inner

The content padding belongs inside .pdf-page-inner

Do not apply padding to .pdf-page

Remove the extra blank page

The export must contain only the intended report pages.

Required checks:

there must be exactly 2 .pdf-page elements in the DOM for this report

no empty trailing .pdf-page

no spacer div after the last page

no wrapper after the last page with min-height, height, or page-break properties

only non-final pages should force a page break

Use this CSS:

.pdf-page {
  break-after: page;
  page-break-after: always;
}

.pdf-page:last-child {
  break-after: auto;
  page-break-after: auto;
}

Also verify in the render logic:

no accidental third page node is being created

no conditional footer/header wrapper is creating its own blank page

Puppeteer is printing only the report pages, not outer app wrappers

Fix the red X icons in the status table

The X icons are not rendering reliably in export.

Do not use:

icon fonts

pseudo-elements

JS-hydrated icon components

background-image icons

Required fix:

replace each red X with inline SVG directly in the DOM

if needed, provide a plain text fallback

hard-code the red stroke/fill color

Use HTML like this inside the status cell:

<td class="status-cell">
  <span class="status-icon status-icon--bad" aria-hidden="true">
    <svg viewBox="0 0 24 24" width="14" height="14" xmlns="http://www.w3.org/2000/svg">
      <path d="M18 6L6 18M6 6l12 12" stroke="#E23D28" stroke-width="3" stroke-linecap="round"/>
    </svg>
  </span>
  <span class="status-text">Behind</span>
</td>

Use CSS like this:

.status-cell {
  white-space: nowrap;
}

.status-icon {
  display: inline-flex;
  align-items: center;
  vertical-align: middle;
  margin-right: 4px;
  line-height: 1;
}

.status-icon svg {
  display: block;
  width: 14px;
  height: 14px;
}

.status-text {
  display: inline;
  vertical-align: middle;
}

Fallback if SVG still fails:

render a literal ✕ directly in the HTML before the word “Behind”

color it red with CSS

example:

<td class="status-cell">
  <span class="status-fallback">✕</span>
  <span class="status-text">Behind</span>
</td>
.status-fallback {
  color: #E23D28;
  font-weight: 700;
  margin-right: 4px;
}

Important:

the icon must exist as actual markup in the table cell before export

do not depend on external icon libraries for export rendering

Restore the footer on every page

The footer must be rendered on every page as part of the page layout.

Do not rely on normal document flow, flex-end, sticky, or page-end behavior.

Required structure:

footer must be inside each .pdf-page

.pdf-page must be position: relative

footer must be absolutely positioned near the bottom of each page

footer must sit above the background and remain visible in export

Use this HTML structure:

<div class="pdf-page">
  <div class="page-bg-header">
    <!-- full-width header art -->
  </div>

  <div class="pdf-page-inner">
    <!-- report content -->
  </div>

  <footer class="pdf-footer">
    <div class="pdf-footer-line"></div>
    <div class="pdf-footer-text">
      Webserv | 32 Discovery Suite 130, Irvine, CA 92618 | webserv.io
    </div>
  </footer>
</div>

Use this CSS:

.pdf-footer {
  position: absolute;
  left: 0.6in;
  right: 0.6in;
  bottom: 0.32in;
  z-index: 2;
}

.pdf-footer-line {
  border-top: 1px solid #B8BDC7;
  margin-bottom: 8px;
}

.pdf-footer-text {
  font-size: 10px;
  line-height: 1.2;
  color: #7C828D;
  text-align: center;
}

Important:

footer must not be outside .pdf-page

footer must not be clipped by an incorrect parent wrapper

footer must not depend on auto-flow after content

footer must appear on both intended pages

Keep the fixed PDF page structure

Do not go back to a responsive export layout.

The print/export route should remain fixed-width and fixed-height for Letter pages.

Use exactly two report page containers for this report:

Page 1 = cover/header/top sections

Page 2 = remaining content sections

Page markup should remain intentional, not browser auto-pagination.

Puppeteer export settings

Keep using Puppeteer PDF export, but make sure it is only printing the report route and waiting for assets.

Use:

await page.goto(printUrl, { waitUntil: 'networkidle0' });
await page.waitForSelector('.pdf-page');
await page.emulateMediaType('print');

await page.evaluate(async () => {
  await document.fonts.ready;

  const images = Array.from(document.images);
  await Promise.all(
    images.map(img => {
      if (img.complete) return Promise.resolve();
      return new Promise(resolve => {
        img.onload = resolve;
        img.onerror = resolve;
      });
    })
  );
});

await page.pdf({
  path: outputPath,
  format: 'Letter',
  printBackground: true,
  preferCSSPageSize: true,
  margin: { top: '0', right: '0', bottom: '0', left: '0' }
});

Important:

do not add browser margins

do not fit-to-page

do not screenshot the page into the PDF

keep text as real text

Final acceptance criteria

A correct export means:

no white gap on the top, left, or right of the header art

header art is flush to the page edges

exactly 2 pages in the export

no extra blank page at the end

red X icons render correctly in the status column

footer appears correctly on each page

current layout remains otherwise unchanged

no new scaling, spacing, font, or wrapping regressions

Final warning:
Do not “improve” the report again.
Do not touch any successful layout area unless it is directly required for one of the four fixes above.
We only need surgical corrections now, not another export rewrite.

And here’s a shorter version you can add above it as context:

The current PDF is almost correct. Only fix four issues: header gap, extra blank page, missing red X icons, and missing footer. Do not change layout, scale, spacing, tables, or typography beyond what is required for those fixes.