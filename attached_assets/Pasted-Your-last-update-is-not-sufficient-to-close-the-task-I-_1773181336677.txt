Your last update is not sufficient to close the task. I need a formal proof-oriented completion report, not a summary narrative. Treat this as a QA verification request.

Do not give me a general recap.
Do not tell me it is “done” without proving each required behavior.
Do not omit failed or untested scenarios.

Return the completion report in the exact structure below.

OBJECTIVE

Prove that the final QBR Snapshot hardening pass is fully complete for:
	1.	Section 1 — What Matters Most This Quarter
	2.	Section 2 — Where Conversions Actually Happen

I need implementation proof, hierarchy proof, regression proof, and test-scenario proof.

==================================================
REQUIRED REPORT FORMAT
	1.	FILES CHANGED
List every file changed for this task only.

For each file, include:
	•	file path
	•	short description of what changed in that file

	2.	SECTION 1 — EXACT LOGIC IMPLEMENTED
Explain the exact final logic used for Section 1.

Must include:
	•	exact formula used for Goal Shift vs Last Quarter
	•	exact rule that converts 0% to “Par”
	•	whether “Par” is display-only or stored
	•	all places where “Par” normalization is applied
	•	preview
	•	print/export PDF
	•	DOCX export
	•	saved-report rendering / migration if applicable

Also explain exact reason-generation logic:
	•	how PAR is detected
	•	how UP is detected
	•	how DOWN is detected
	•	what evidence inputs are used
	•	how pacing influences the reasoning
	•	what happens when pacing data is incomplete

	3.	SECTION 2 — EXACT HIERARCHY IMPLEMENTED
Explain the exact final Top Converting Pages hierarchy in order.

I need all layers explicitly stated and confirmed:

P1. direct GA4 conversion landing-page data
P2. call tracking landing-page or source-page data
P3. organic / GA4 sessions proxy or high-intent landing-page inference
P4. money page inference
P5. structural fallback pages

For each layer, explain:
	•	what input data it uses
	•	when that layer is considered usable
	•	when the system moves to the next layer
	•	whether multiple layers can be blended
	•	what overrides what
	•	whether direct evidence always outranks inference

Also explain:
	•	exact note labeling rules
	•	which labels are used for inference
	•	exact dead-end suppression behavior
	•	the only condition where a dead-end row is still allowed

	4.	QA SCENARIOS TESTED
I need one line per scenario with:

	•	scenario name
	•	pass/fail
	•	what rendered
	•	whether it was preview, export, or both

You must include all of these:

SECTION 1 TESTS
	•	unchanged goal renders Par
	•	increased goal renders signed positive percent
	•	decreased goal renders signed negative percent
	•	unchanged goal with weaker pacing
	•	unchanged goal with stronger pacing
	•	incomplete pacing data still produces useful reason

SECTION 2 TESTS
	•	direct GA4 conversion-page data available
	•	no GA4 direct data but call tracking landing/source-page data available
	•	no direct conversion data but high-intent organic landing-page inference available
	•	mixed-source organic + GMB + AI LLM evidence available
	•	only structural fallback available
	•	truly no viable inference path available

REGRESSION TESTS
	•	preview still renders
	•	print/export PDF still renders
	•	DOCX export still renders
	•	saved snapshot loading still renders correctly
	•	unrelated sections untouched
	•	no type/notes mismatch remains in affected tables

	5.	BEFORE / AFTER EXAMPLES
Provide concrete before/after examples for both sections using Anchored Tides-style data.

I need:
A. Section 1 before
B. Section 1 after
C. Section 2 before
D. Section 2 after

Do not summarize. Show representative row-level output.
	6.	PROOF OF PRIORITY ORDER
I need explicit proof that direct evidence outranks inference.

Show at least:
	•	one example where direct GA4 conversion-page data exists and is used instead of inference
	•	one example where call tracking page/source data exists and is used instead of lower-layer inference
	•	one example where inference is used only because higher-priority layers failed

	7.	PROOF THAT “0%” IS GONE
I need explicit proof that raw “0%” no longer appears for unchanged goals.

Show proof for:
	•	preview
	•	print/export PDF
	•	DOCX export
	•	saved report rendering path

If any path still depends on legacy “0%” storage, explain exactly how it is normalized to “Par”.
	8.	FINAL STATUS
End with one of these exact statements:

	•	FULLY COMPLETE
	•	NOT FULLY COMPLETE

If NOT FULLY COMPLETE, list exactly what remains.

==================================================
IMPORTANT RULES
	•	Do not give me a vague summary
	•	Do not skip any required section
	•	Do not say “verified” without showing what was tested
	•	Do not hide untested scenarios
	•	If a scenario was not tested, label it UNTESTED
	•	If a scenario failed, label it FAIL
	•	Be exact and proof-oriented

I am not approving closure of this task without this report.