I have an urgent export bug and I need you to diagnose and fix it properly, not theorize.

Problem:
In the app, the QBR Prep preview shows the correct, current version of the report.
But when I download the PDF, it exports an old, outdated version of the report that has not existed in days.

So somewhere, the preview path and the PDF export path are not using the same source, template, renderer, payload, or version.

Your job:
Find the exact root cause in the codebase and fix it so the downloaded PDF matches the current on-screen preview exactly.

What I need you to do:

Trace the full preview pipeline for QBR Prep

Find the exact frontend component(s), backend route(s), data transformation logic, and template/render path used to generate the preview.

Identify the exact payload and structure being used for the preview.

Trace the full PDF export pipeline for QBR Prep

Find the exact frontend trigger, backend export route, PDF generation function, HTML/template source, serializer, and any intermediate storage/cache layer.

Identify whether the PDF is using:

an old template

a separate legacy renderer

stale cached HTML/data

old database content

a different reportType mapping

a different component or template than preview

an old snapshot or precomputed report body

an older endpoint entirely

server-side code that was not updated when preview changed

Compare the two pipelines side by side
I want you to explicitly show:

preview route/function/component

PDF route/function/component

where they diverge

which exact file(s) are causing the old version to be used

Fix the architecture, not just the symptom
The export should not have its own stale report structure if the preview already has the correct one.
Best fix:
make preview and PDF use the same normalized report payload and the same rendering source wherever possible.

Priority:
single source of truth.
No duplicated report layout logic unless absolutely required.

Check for caching/versioning issues
Specifically inspect for:

stale localStorage/sessionStorage usage

cached API responses

memoized report content

persisted report snapshots in DB

old template IDs

report type aliases or switch/case mismatches

old server build artifacts

separate export workers/functions still pointing to deprecated code

hardcoded fallback templates

old HTML strings or legacy JSX components still being used for export

route mismatches between qbr-prep preview and qbr-prep pdf export

Add verification
After fixing, verify:

preview and PDF now match for QBR Prep

no outdated headings/sections/layout from the old report remain

other report types did not break

export still works end to end

What I want back from you:
Do not give me vague summaries.
Give me a hard report in this exact format:

Root cause

Explain exactly why preview and PDF were different

Files changed

List every file changed

What was changed in each file

One concise bullet per file

Final source of truth

State exactly what now powers both preview and PDF

Validation performed

Explain how you confirmed the PDF now matches preview

Any remaining risk

Only include this if something still needs follow-up

Important constraints:

Do not redesign the report

Do not change the content structure unless required to make PDF match preview

Do not do a large refactor unless necessary

Fix the real divergence fast and cleanly

Prefer reusing existing preview rendering logic or its exact payload

Remove or bypass legacy export logic if that is the problem

Success condition:
When I preview QBR Prep and then download the PDF, the PDF must be the same current report, not some older legacy version.