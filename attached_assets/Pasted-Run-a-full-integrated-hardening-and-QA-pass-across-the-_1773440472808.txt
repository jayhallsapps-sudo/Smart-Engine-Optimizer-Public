Run a full integrated hardening and QA pass across the complete SmartEO Phase 2 system.

IMPORTANT CONTEXT

Phase 2 feature development is effectively complete. Do not add new major features in this prompt unless a small fix is required to resolve a real QA issue.

The goal now is to validate and tighten the entire Phase 2 stack so the product is stable enough for serious internal testing.

CORE OBJECTIVE

Test and harden the full SmartEO Phase 2 flow end to end, focusing on structural consistency, state integrity, handoff behavior, admin safety, and workflow/report continuity.

PHASE 2 SYSTEMS TO VERIFY TOGETHER

- Command Center
- report registry and canonical naming
- report family split
- workflow shell
- finding model
- contextual finding chat
- comments/review
- workflow session persistence
- workflow-to-report handoff
- workflow field mapping
- QBS → QBR bridge
- quarter-aware QBS selection
- admin guidance surfacing
- config overrides
- template controls
- admin auth
- priority engine
- manual priority overrides
- execution context
- Airtable/Asana reference linking
- cross-period finding memory

PRIORITY QA AREAS

1. END-TO-END WORKFLOW CONTINUITY
- verify report type selection → client selection → strategy areas → findings review → assembly → report builder handoff works cleanly
- verify workflow restoration and start-fresh behavior
- verify URL preset behavior still bypasses stale restore correctly
- verify revised findings, priority overrides, execution state, and recurrence context remain coherent across steps

2. REPORT HANDOFF + MAPPING
- verify workflow output still maps into the correct report fields for Bi-Weekly, Monthly, and QBS
- verify QBS → QBR import still works correctly after recent additions
- verify no wrong-client / wrong-report cross-contamination
- verify handoff banners, apply flows, and dismiss flows behave consistently

3. COMMENTS / REVIEW
- verify savedReportId scoping is still correct
- verify anchors still resolve correctly
- verify delete / resolve / reply behavior still works after template/config changes
- verify comments remain stable in read/presentation/focus modes

4. PRIORITY / EXECUTION / RECURRENCE
- verify heuristic priority, manual override, execution state, linked refs, and recurrence context all coexist correctly on the same finding
- verify sorting behavior is stable and sensible
- verify chat panel shows the right context for the active finding
- verify recurrence info displays correctly on cards and in chat

5. ADMIN / GOVERNANCE / TEMPLATE CONTROLS
- verify admin auth still protects all write routes
- verify non-admin users cannot mutate governance/config/template data
- verify config overrides and template controls still affect runtime cleanly
- verify guidance surfacing still reflects active guidance correctly
- verify admin note/override/template edits do not create regressions in report/workflow surfaces

6. CANONICAL NAMING / CONSISTENCY
- verify canonical names are used throughout:
  - Kickoff Deck
  - Mid-Strategy Deck
  - Launch Deck
  - Bi-Weekly Report
  - Monthly Report
  - QBS
  - QBR
- verify no lingering “QBR Full” / “QBR Prep” style visible naming mistakes remain unless intentionally internal-only and documented

7. FAIL-CLOSED / EDGE CASES
- verify missing ADMIN_TOKEN fails closed
- verify missing Airtable/Asana config degrades gracefully
- verify empty guidance / empty comments / empty recurrence cases render cleanly
- verify session expiry and missing saved report contexts fail gracefully

REQUIREMENTS

- fix low-risk issues discovered during this pass
- document medium/high-risk issues if they should be deferred
- do not add unrelated new features
- prefer stabilization over expansion

DELIVERABLES

Provide:
- a hardening pass with fixes applied
- a QA report including:
  - what was tested
  - what was fixed
  - what remains deferred but acceptable
  - any remaining risks before serious internal testing
  - your honest judgment on whether Phase 2 is now stable enough for a dedicated testing round

SUCCESS CONDITION

The complete Phase 2 system works coherently enough end to end that SmartEO can enter serious internal testing without major known structural gaps.