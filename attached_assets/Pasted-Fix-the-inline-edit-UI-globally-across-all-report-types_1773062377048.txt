Fix the inline edit UI globally across all report types.

Problem:
When clicking some editable fields, the edit state shows a black/dark input box. This makes the text hard to read and looks inconsistent. Other fields already show the correct white background edit state.

Desired behavior for ALL editable fields:
- white background
- visible dark text
- clear border around the edit area
- Save and Cancel buttons visible and consistent
- no black box
- no dark-theme inherited styling in edit mode

This must be fixed at the shared editable-field/component level so it applies everywhere, not patched field-by-field.

==================================================
REQUIRED FIX
==================================================

Find the shared inline editing component(s) used across report previews, such as:
- EditableSection
- EditableCell
- any shared edit wrapper / inline input / textarea component

Make the edit state visually consistent everywhere.

When a field enters edit mode, enforce:
- background: white
- text color: dark / near-black
- border: visible neutral border
- border radius: consistent with report UI
- no dark background inherited from app shell or theme
- placeholder text visible if present
- focus state visible but not aggressive

Buttons:
- Save and Cancel should remain visible
- button layout can stay as-is if already acceptable
- do not break working button behavior

==================================================
SCOPE
==================================================

Apply this globally to all editable report fields, including:
- titles
- subtitles
- commentary blocks
- bullets
- table cells
- metric notes
- any inline-editable text in:
  - Bi-Weekly
  - Monthly
  - QBR Prep
  - Full QBR
  - Mid-Strategy SEO Report

==================================================
IMPLEMENTATION RULE
==================================================

Do not patch individual fields.
Fix the shared component or shared edit-state styles so all report fields use the same edit appearance.

Check for:
- dark mode utility classes being inherited
- transparent backgrounds
- input/textarea styles not being explicitly set
- className branching differences between field types

==================================================
PROOF REQUIRED
==================================================

Return:
1. files changed
2. the shared component or style rule that was fixed
3. before/after code for the edit-state styling
4. confirmation of which report types now inherit the fix
5. screenshots or proof from at least:
   - one bullet field
   - one table cell
   - one commentary field
showing white background, visible text, and border in edit mode