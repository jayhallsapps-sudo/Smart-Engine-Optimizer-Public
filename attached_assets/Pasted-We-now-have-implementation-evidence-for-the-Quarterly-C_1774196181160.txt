We now have implementation evidence for the Quarterly Content Roadmap and Templates work. Based on that evidence, the feature is not yet production-ready.

Do not give me another generic progress summary.
Do not tell me what is already wired unless it is directly relevant to fixing the remaining gaps.
Your job now is to close the known functional gaps, validate them properly, and give me a real artifact I can inspect.

==================================================
CURRENT STATUS YOU MUST ACCEPT AS TRUE
==================================================

The following are already implemented and do not need to be re-described at a high level unless they are being changed:

- Quarterly Content Roadmap generator exists
- Quarterly Content Roadmap page exists
- QCR generate route exists
- QCR PPTX route exists
- Templates page exists
- Template editor exists
- Templates is back in the left sidebar
- Anchored Tides Q2 2026 generation returns 11 slides
- Airtable production rows are being pulled successfully for April, May, and June

The following known gaps are real and must be fixed:

1. CRITICAL GAP — QBS strategy extraction is using the wrong schema
The current extractor reads fields like:
- executive_summary
- opportunity_backlog
- future_window_label
- sourceSnapshotJson

But the live QBR Prep / QBS data for Anchored Tides is in the v2 structure using keys like:
- section1Goals
- section2Conversions
- section3Traffic
- section4Services
- section5Diagnosis
- section6Priorities
- section7Tracking
- sectionQssb
- sectionSuggestedKeywords
- sourceSnapshot

Because of that mismatch, all strategy slides are currently falling back to placeholder text.

2. TEMPLATE GAP — template canvas edits do not affect PPTX output
The template editor saves layout JSON, but the PPTX generator still uses hardcoded colors, hardcoded positions, and hardcoded layout logic.
That means moving the header, changing fonts, changing colors, deleting elements, and resizing elements in Templates does not actually change the PPTX export.

3. TEMPLATE SAVE GAP — qcr_layout does not exist until first explicit commit
That is acceptable as a startup state, but once saved, the layout must be used downstream by generation/export where applicable.

4. DATA LABEL GAP — Airtable Credit Type values are all rendering as “Other” for Anchored Tides
That may be a data vocabulary issue instead of a code bug, but I want it investigated and handled cleanly in client-facing output.

==================================================
PRIMARY OBJECTIVE NOW
==================================================

Make the Quarterly Content Roadmap production-ready.

That means:
- real month-specific strategy content must populate from the actual live QBS/QBR Prep schema
- template edits must meaningfully affect the generated deck output, especially PPTX export
- client-facing table output must be clean and useful
- the system must remain non-destructive to all existing reports and decks

==================================================
PART 1 — FIX QBS / STRATEGY MAPPING
==================================================

You must update the QCR strategy extraction so it works with the actual live QBS / QBR Prep schema currently stored in production.

Required behavior:
- inspect the real v2 QBR Prep JSON structure already being used for clients like Anchored Tides
- identify exactly which sections contain the quarter strategy, month priorities, rationale, or prioritization logic needed for the QCR deck
- build a deterministic adapter for the v2 schema
- preserve support for the older schema if older clients still use it
- do not break old schema compatibility if it already works for any clients

Required outcome:
- April, May, and June strategy slides for Anchored Tides Q2 2026 must no longer be placeholder text
- the quarter overview slide must no longer rely on placeholder/fallback strategy language
- each month must have distinct strategy bullets, not three generic repeated lines with the month name swapped

Required implementation detail:
- if the v2 schema does not contain explicitly month-labeled strategy sections, derive month-specific bullets from the most relevant quarter planning sections using a deterministic mapping rule
- if any inference is necessary, document the inference clearly in your response
- do not invent strategy content without tying it to real source fields

I want the adapter logic explained clearly:
- exact fields read
- exact precedence order
- exact fallback order
- exact month distribution logic
- exact handling when some sections are populated and others are empty

==================================================
PART 2 — MAKE TEMPLATE EDITS MATTER
==================================================

The template editor is only half-useful if it does not affect output.

You must wire the Quarterly Content Roadmap template system into actual deck rendering behavior.

Required minimum behavior:
- template layout/design changes saved in Templates for QCR must affect generated QCR output
- template changes must affect browser preview
- template changes must affect PPTX export as much as the current architecture reasonably allows

At minimum, the following template-controlled properties must flow through:
- header position
- header size
- header color / accent bar styling
- text box positions
- text box sizes
- font size
- font weight
- text color
- alignment
- visible/deleted elements
- basic shape blocks used in the template

If there are architectural limits that prevent full one-to-one PPTX layout mirroring, do not hide that.
Instead:
- implement the maximum real alignment possible
- explicitly list what now flows through to PPTX
- explicitly list what still does not
- explain why

Do not leave the template editor as a disconnected planning toy.
It must drive real output.

==================================================
PART 3 — CLEAN UP AIRTABLE TYPE DISPLAY
==================================================

The Anchored Tides output currently shows Type = Other for all 14 deliverables because Airtable Credit Type values do not match the expected vocabulary.

You must address this in one of these acceptable ways:

Option A:
Map the real Airtable values to clean client-facing labels through a normalization layer.

Option B:
If the Airtable values are too inconsistent, choose a better client-facing field for the Type column.

Option C:
If neither is possible safely, replace the Type column in QCR with a more reliable field that provides useful client-facing differentiation.

What is not acceptable:
- leaving every row as “Other” if there is a better available solution
- exposing ugly internal values directly to the client without cleanup

Explain exactly what you chose and why.

==================================================
PART 4 — KEEP TEMPLATES IN THE SIDEBAR AND IMPROVE THEM SAFELY
==================================================

Templates must remain visible in the left sidebar.

Do not remove or hide Templates while doing this work.

Preserve:
- template list page
- template editor routes
- QCR template entry
- existing simple editors for other template types unless there is a safe reason to improve them

If you touch template save behavior, do it safely.
The current implementation accepts any string as templateType. That is a latent risk.
Tighten validation so:
- only allowed template keys can be saved
- malformed payloads do not poison template_config.json
- existing saved template behavior still works

==================================================
PART 5 — NON-REGRESSION SAFETY
==================================================

Do not break:
- existing report generators
- existing PPTX exports
- existing save/load behavior
- existing routes
- existing registry behavior
- existing sidebar navigation
- existing templates for biweekly, monthly, qbr, or anything else

Use additive or tightly scoped changes only.

If you must touch shared PPTX generation utilities, prove that the other deck types still export correctly.

==================================================
PART 6 — REQUIRED REAL-WORLD TEST CASE
==================================================

Use Anchored Tides Recovery as the proof client.

Generate a real Q2 2026 Quarterly Content Roadmap after your fixes.

That proof case must include:
- title slide
- quarter overview
- April strategy
- April production table
- May strategy
- May production table
- June strategy
- June production table

I want the strategy text populated from real source data or clearly documented deterministic derivation from real source data.
I do not want placeholder filler.

==================================================
PART 7 — REQUIRED ARTIFACTS
==================================================

You must give me real inspectable output, not just claims.

Required deliverables:
1. a regenerated Anchored Tides Q2 2026 QCR preview
2. a regenerated PPTX artifact for Anchored Tides Q2 2026
3. a direct easy-to-download link to that PPTX
4. if possible, a JSON artifact or debug dump showing the mapped monthly strategy source fields
5. a concise list of exactly what changed between the old broken version and the corrected version

If a direct download link cannot be produced in your environment, say exactly why, and provide the nearest equivalent concrete artifact instead. Do not hand-wave.

==================================================
PART 8 — RESPONSE FORMAT
==================================================

Your next response must be an audit-style implementation report, not a vague summary.

Use this structure:

A. Executive verdict
- Is QCR production-ready now: yes or no
- If no, why exactly

B. Files changed
For each file changed:
- path
- new or modified
- exact purpose
- exact functions/components changed
- why the change is isolated

C. QBS fix
- exact schema fields used now
- exact adapter logic
- exact fallback logic
- exact month distribution logic
- proof with Anchored Tides

D. Template-to-output fix
- what template properties now affect browser preview
- what template properties now affect PPTX
- what still does not affect PPTX
- why

E. Airtable table cleanup
- what field/normalization logic is used now for the Type column
- proof with Anchored Tides

F. Non-regression proof
- what existing features were rechecked
- what was manually tested
- what was verified via build/type/runtime
- what still remains unverified

G. Anchored Tides sample output
Paste the actual generated content in readable form:
- title slide text
- quarter overview text
- April strategy bullets
- April table rows
- May strategy bullets
- May table rows
- June strategy bullets
- June table rows

H. Artifact links
- direct PPTX download link
- any other useful artifact links

I. Remaining risks
- anything still imperfect
- anything I should inspect first
- whether I can safely use this with clients right now

==================================================
IMPORTANT RESPONSE RULES
==================================================

- Do not say “complete” unless the QBS strategy gap is actually fixed
- Do not say template editing works unless it actually changes output
- Do not call a fallback placeholder a successful strategy population
- Do not hide architectural limits
- Do not give me a high-level summary instead of evidence
- Be explicit, concrete, and honest