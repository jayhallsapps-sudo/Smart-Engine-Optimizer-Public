You are working on SmartEO. The QBR Prep report is currently under-producing opportunities (empty Content/Technical/CRO sections). Fix the generator so it reliably produces a full actionable backlog using the connected data sources: Screaming Frog imports, Google Search Console, GA4, and SEMrush Site Audit.

Core problem to solve:
The logic is filtering too aggressively and/or failing to join datasets (URL normalization/mapping), so opportunities never get surfaced. I need deterministic opportunity generation with fallbacks so every run returns meaningful items.

Non-negotiables:
1) Do not return empty categories when data exists. If a category has 0 items after scoring, output a “Why empty” diagnostic AND then re-run using relaxed thresholds (fallback mode) to populate at least a minimum set (defaults below).
2) Always include CRO opportunities for “Verify Insurance” and “Contact” intent pages if they exist on the site (even if conversions are missing); those are always candidates.
3) Always produce at least:
   - Content opportunities: min 8
   - Technical opportunities: min 8
   - CRO opportunities: min 5
   - Tracking opportunities: min 3
   - Local/GBP opportunities: min 3 (if GBP connected)
   - Authority opportunities: min 2 (if SEMrush/Ahrefs connected; otherwise clearly mark unavailable and skip)
These are minimums in fallback mode; normal mode can exceed them.

Data ingestion requirements (fix these first):
A) Screaming Frog:
- Accept and parse BOTH:
  - Reports > Crawl Overview export (summary)
  - Internal > All export (row-level evidence)
- Normalize URL fields consistently: lowercase host, remove trailing slash (except root), strip fragments, preserve query only where needed, canonicalize http/https to final canonical if known.
- Store per-URL fields at minimum: status code, indexability, canonical, meta title, meta description, H1 count/text if available, word count, crawl depth, inlinks count, outlinks, response time (if available), directives (noindex/nofollow), pagination tags, structured data presence (if available), image alt issues (if available).

B) Google Search Console:
- Pull page performance for the past window: clicks, impressions, CTR, avg position by page.
- Pull query performance: top queries + associated pages if possible.
- Normalize URLs using same canonicalization as SF.
- Ensure you can map GSC page URL -> SF URL row (fix mismatch due to trailing slashes, uppercase, http/https).

C) GA4:
- Pull landing page metrics for organic traffic: sessions, engaged sessions, engagement rate, key events/conversions (forms), conversion rate.
- Pull device split for key pages (mobile vs desktop conversion rate).
- Normalize landing page paths to full URLs using the site’s primary domain, then map to SF rows.

D) SEMrush:
- If Site Audit is connected, pull top issues list with counts + affected URLs (or categories summary if URLs unavailable).
- Normalize URLs to match SF/GA4/GSC where possible.

IMPORTANT: Add a “coverage diagnostics” section (internal, not in the final client-facing doc unless a debug flag is on) that prints:
- Count of URLs from SF Internal All
- Count of pages from GSC
- Count of landing pages from GA4
- Match rate %: GSC->SF, GA4->SF
- For each category: number of candidates found before filtering, after filtering, after fallback

Opportunity generation: implement deterministic rules + fallback rules.
Create two passes:
PASS 1 (strict thresholds)
PASS 2 (fallback thresholds if minimums not met)

CONTENT OPPORTUNITIES (use GSC + GA4 + SF; SEMrush optional):
Generate candidates and score them. Include URL and evidence.
Rules (Pass 1):
1) High impressions, low CTR:
   - GSC impressions >= 75th percentile AND CTR <= 25th percentile OR CTR < 1% with impressions > 500
   - Avg position <= 20
   - Opportunity: rewrite title/meta; add TLDR/FAQ; improve snippet; align intent
2) Striking distance:
   - Avg position 4–15 AND impressions trend strong (top 25% impressions) AND clicks below expected (CTR below median)
   - Opportunity: refresh content, add internal links, expand sections, add schema/FAQ
3) High traffic, low conversions:
   - GA4 organic sessions in top 20 AND conversion rate below site median by >= 30%
   - Opportunity: CTA optimization; trust blocks; intent alignment; add internal links to money pages
4) Thin/low-value content (SF word count):
   - Word count below threshold for its type:
     - Service pages: < 900 words
     - Blog posts: < 1200 words (unless clearly short-form by design)
     - Location pages: < 700 words
   - Opportunity: expand coverage, add intent sections, add FAQs, add proof blocks
5) Meta/title quality issues (SF):
   - Missing title, duplicate title, title too long/short, missing meta description, duplicate meta description
   - Opportunity: write unique titles/descriptions with primary intent terms and conversion hook
6) AEO structure gaps (SF + heuristics):
   - Missing H2 structure (too few headings), multiple H1, no FAQ section indicators, no summary/TLDR indicator
   - Opportunity: restructure for answer-first, add FAQ, add table where appropriate
7) “Freshness” opportunities:
   - Pages with declining clicks QoQ/QTD and last-modified old (if available) OR no recent update signals
   - Opportunity: refresh, update stats, revalidate claims, republish

Fallback rules (Pass 2 if fewer than min 8):
- Lower thresholds: impressions >= median, avg position <= 30, CTR below median
- Include any page with:
  - impressions > 200 and CTR < 1.2%
  - position 8–25 with impressions > 200
  - GA4 sessions top 50 and conversions = 0
- If GSC is missing, generate content opportunities purely from SF signals (metadata, word count, headings, indexability, internal linking).

TECHNICAL SEO OPPORTUNITIES (use SF + SEMrush + GSC Indexing):
Rules (Pass 1):
1) Indexability blockers:
   - noindex pages that should be indexable (exclude staging/admin)
   - canonical points off-page or to non-200
   - robots blocked for key templates
2) Status code issues:
   - 4xx internal pages
   - 3xx chains or excessive redirects (>=2 hops) especially on money pages
3) Duplicate/conflicting directives:
   - canonical + noindex contradictions, inconsistent hreflang if present
4) Internal linking issues:
   - crawl depth > 3 on important pages OR inlinks < 5 on important pages
   - orphan pages if detected
5) Heading/HTML problems:
   - missing H1, multiple H1, duplicate H1 across many pages
6) Image/accessibility/SEO basics:
   - missing alt text on template images; oversized pages; missing dimensions (if SF captures)
7) Performance proxies:
   - slow response times in SF (if available) OR very large HTML size
8) Sitemap/Index coverage:
   - GSC coverage errors; submitted URLs not indexed; spikes in “Crawled - currently not indexed”

Fallback rules (Pass 2 if fewer than min 8):
- Include top 8 technical issues by count from SEMrush audit even if URL list is partial
- If SEMrush not connected, use SF issue counts and list top affected example URLs (up to 5 per issue type) plus “estimated affected count”.

CRO OPPORTUNITIES (use GA4 + Call Tracking + page heuristics):
Rules (Pass 1):
1) Always include:
   - Any URL containing patterns: /verify-insurance, /insurance, /contact, /admissions, /call, /get-help (if exists)
   - Generate CRO items even if no GA4 conversion data; use heuristic “high-intent page”
2) High traffic, low conversion:
   - Top 20 organic landing pages with conversion rate below median by 30%+
3) Device split problems:
   - Mobile conversion rate significantly lower than desktop (>=40% gap) on key pages
4) Engagement high, conversion low:
   - Engagement rate in top 25% but conversions below median
5) Call behavior:
   - If call tracking available: high call volume but low answered/qualified rate; recommend ops + tracking improvements

Fallback rules (Pass 2 if fewer than min 5):
- Include at least 5 high-intent pages based on URL patterns and SF page attributes (indexable, in main nav depth <=2, etc.) with generic but specific recommendations:
  - above-the-fold CTA, sticky call button on mobile, trust badges, FAQ, reduce friction, track click-to-call.

AUTHORITY/LINK OPPORTUNITIES (SEMrush/Ahrefs only; skip if not available under plan credits):
- Identify top 5 money pages by conversions/intent and flag if referring domains/backlinks are low relative to other key pages OR if there’s link decay (lost links) if data exists.
Fallback:
- If you cannot access authority data, output “Authority data not available via integrations; skipped” and do not fabricate.

TRACKING/MEASUREMENT OPPORTUNITIES (GA4 + GSC + Call tracking):
Rules:
1) Missing/zero conversion events:
   - GA4 shows 0 conversions sitewide or on key pages while calls exist (or vice versa)
2) Organic calls up but GA4 forms down (or mismatch) beyond normal variance
3) GSC clicks/impressions drop sharply while GA4 organic sessions stable (or opposite)
4) Call tracking attribution gaps: calls not attributed to landing page/source where expected
Fallback:
- Always produce at least 3: GA4 conversion audit, call tracking source mapping audit, GSC property/sitemap coverage audit.

LOCAL/GBP OPPORTUNITIES (GBP + call tracking):
Rules:
1) Interactions down QoQ/QTD -> posting cadence, photos, services, categories
2) Reviews velocity/response gaps
3) GBP-sourced calls low relative to interactions
Fallback:
- If GBP not connected, output “GBP not connected; skipped” and do not fabricate.

Opportunity format (must match QBR Prep doc spec):
For each opportunity:
- Opportunity Title
- Priority (P0/P1/P2)
- Impact (High/Med/Low)
- Effort (S/M/L)
- KPI Affected
- URL(s)
- Evidence (source + metrics/deltas; if fallback, cite SF issue + counts)
- Problem
- Opportunity
- Why it matters
- Recommended next step

Scoring / Prioritization:
- P0: indexation blockers on money/high-intent pages; severe tracking break; 4xx on key pages; major conversion drop
- P1: striking distance; high impressions low CTR; high traffic low conversion; major duplicate metadata on key templates
- P2: minor improvements or low-traffic pages

Implementation deliverable:
Update QBR Prep report generator so it:
- properly joins SF/GSC/GA4/SEMrush datasets via normalized URLs
- generates opportunities using PASS 1 rules
- checks minimums and triggers PASS 2 fallback rules if needed
- returns a full doc-ready output every time
- includes internal diagnostics counts (toggleable) so we can see why something was empty

Start by fixing URL normalization and join logic, then implement the candidate generation passes and minimum enforcement.