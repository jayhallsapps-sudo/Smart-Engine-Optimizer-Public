node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Root-level generator proof screenshots and exports — keep out of repo
/*.png
/*.pdf
/*.pptx
/*.docx
*.zip

# Replit Agent pasted-prompt artifacts — keep out of repo
attached_assets/Pasted-*.txt

# attached_assets is Replit Agent's pasted-files cache — keep out of repo.
# Exception: HEADER_IMAGE is referenced at runtime by qbrPrepDocxGenerator.ts.
attached_assets/*
!attached_assets/.gitkeep
!attached_assets/HEADER_IMAGE_1773063127856.png
