The QBS DOCX export is failing QA.

Treat this as a hard export-parity defect.
Do not treat it as “formatting cleanup.”
Do not respond with a light styling pass.
Do not optimize for Word-only rendering.
Optimize for Google Docs survivability first, because that is where the export is visibly falling apart.

The attached QBS PDF and DOCX prove the issue:

the PDF preserves the report design language

the DOCX imported into Google Docs collapses into a weak, flattened text document

hierarchy, spacing, table treatment, and report-family polish are being lost

That is a failed export.

==================================================
OBJECTIVE

Rebuild the QBS DOCX export so it survives Google Docs import as a professional client-facing report.

The DOCX does not need to match the PDF pixel-for-pixel.
It does need to preserve the report family.

Required outcome:
When the exported DOCX is opened in Google Docs, it must still look like a deliberate report artifact, not a content dump.

==================================================
NON-NEGOTIABLE EXPORT STANDARD

The DOCX export must preserve, in a Google-Docs-stable way:

report title hierarchy

subtitle/meta hierarchy

section hierarchy

readable spacing rhythm

structured table hierarchy

label/value separation

clean callout/recommendation blocks

footer/header polish

naming consistency

overall QBS report-family professionalism

If Google Docs strips a styling technique, do not use that styling technique as a critical dependency.

==================================================
CURRENT FAILURE MODES THAT MUST BE FIXED

The current DOCX export is failing because it behaves like a semantic text dump instead of a designed document.

Observed failure modes:

title hierarchy collapses

section spacing collapses

tables lose shell/treatment

long text becomes cramped walls

labels and values blur together

section transitions are weak or invisible

Additional Opportunities looks structurally broken

naming appears inconsistent with the current QBS artifact naming

document feels like imported content, not a polished report

Do not tell me “Google Docs strips some formatting.”
That is known.
Your job is to export in a way that survives Google Docs.

==================================================
PRIMARY IMPLEMENTATION RULE

Use conservative, Google-Docs-resilient DOCX formatting only.

That means:

explicit paragraph styles

explicit spacing before/after paragraphs

stable tables

simple shading

simple borders

intentional cell widths

intentional cell padding where supported

consistent font sizing

bold/size hierarchy

restrained use of line breaks

block structure via tables/paragraphs, not fragile floating layout

Do not rely on:

fragile positioning

floating objects

complex nested layout tricks

styling that only looks right in Word desktop

anything that Google Docs commonly strips or mangles

==================================================
REQUIRED DOCX STYLE SYSTEM

Build an explicit DOCX style system for QBS.

At minimum define and use styles for:

ReportTitle

strong size

bold

clean spacing after

obvious primary hierarchy

ReportMeta

smaller than title

subdued but readable

consistent spacing

SectionHeading

clearly distinct from body

bold

stronger size

consistent top spacing

visible bottom spacing

SubsectionHeading

smaller than section heading

still clearly distinct from body

BodyText

stable readable size

consistent paragraph spacing

no cramped default Word/Docs behavior

LabelText

bold or semi-emphasized inline label treatment

SmallMeta/Footer

smaller, subdued, but readable

Do not rely on default Normal style for everything.
If most of the document still uses generic default styling, the task fails.

==================================================
TABLE EXPORT REBUILD — HARD REQUIREMENT

You must rebuild the DOCX table output for all major QBS tables.

At minimum address:

Account Manager Context block

Section 1: What Matters Most This Quarter

Section 2: Where Conversions Actually Happen

Section 6: What We Need to Do Next

Section 8: Additional Opportunities

For every major table:

set explicit column widths

set explicit header row styling

use readable shading on header rows

use intentional borders

use readable cell spacing/padding where supported

align top vertically for long narrative cells

prevent ugly column collapse where possible

avoid giant unreadable reason/note walls

If a table currently looks like raw rows pasted into a document, it fails.

==================================================
SECTION-SPECIFIC REQUIREMENTS

A. ACCOUNT MANAGER CONTEXT

must read like a clean report section, not plain pasted paragraphs

each sub-block must be visually separated

hypothesis, previous quarter assessment, and client sentiment must be easy to scan

B. SECTION 1 — WHAT MATTERS MOST THIS QUARTER

table must remain readable in Google Docs

goal type, goal, source, shift, and reason must remain visually distinct

long reasons must not collapse into cramped unreadable blocks

C. SECTION 2 — WHERE CONVERSIONS ACTUALLY HAPPEN

separate the “Top Converting Pages,” “Top Conversion Patterns,” and “Top Converting Sources” subsections clearly

preserve subsection hierarchy

keep evidence and notes readable

D. SECTION 6 — WHAT WE NEED TO DO NEXT

preserve action hierarchy

do not flatten it into a weak list if structured blocks are possible in a Google-safe way

E. SECTION 8 — ADDITIONAL OPPORTUNITIES
This section needs a hard rebuild.
Current state is unacceptable.

Requirements:

each opportunity must render as a clearly structured block

preserve opportunity title/type distinctly

preserve evidence distinctly

preserve recommendation distinctly

preserve rationale distinctly if present

do not render it as loose paragraphs with weak labels

do not render “Evidence:” and “Recommendation:” as orphaned text fragments

use a stable block pattern, likely a small 2-column table or a nested structured section per opportunity, but only if it survives Google Docs

If Additional Opportunities still looks broken in Google Docs after export, the task fails.

==================================================
NAMING CONSISTENCY — HARD RULE

The DOCX export must use the correct current report naming.

If QBS is the official artifact name for this flow, the DOCX must not revert to:

QBR Prep

QBR Snapshot

legacy naming
unless there is an explicitly approved business reason.

You must verify:

document title

internal headers

filename generation if affected

footer/header references if affected

==================================================
GOOGLE DOCS COMPATIBILITY HARDENING

You must explicitly design for Google Docs import stability.

Required approach:

Assume Google Docs is the real renderer.

Build the DOCX so Google Docs keeps the hierarchy.

Prefer simpler, resilient structures over richer but fragile Word styling.

Test import result visually, not just raw file generation success.

You are not done if:

the .docx file technically opens

Word desktop looks okay

but Google Docs still looks like a flattened document

Google Docs result is the QA target.

==================================================
PARITY CHECK REQUIREMENT

You must compare all four states:

app preview

PDF export

DOCX export in Word-compatible rendering

DOCX import in Google Docs

Then identify where visual hierarchy is being lost and fix those specific losses.

Do not stop at “DOCX generated successfully.”
That is not the success condition.

==================================================
IMPLEMENTATION REQUIREMENTS

You must do all of the following:

Audit the current DOCX generator structure and identify every section using weak/default rendering.

Refactor the DOCX generator to use shared QBS export styles and shared block builders.

Rebuild weak sections using stable paragraph/table primitives.

Remove or replace any DOCX styling that Google Docs drops and that is currently causing hierarchy loss.

Keep PDF generation untouched unless a shared refactor is truly required.

Preserve content accuracy while improving presentation.

Ensure no section regresses into missing content or broken ordering.

==================================================
STRICT QA ACCEPTANCE CRITERIA

Do not mark complete until every item below passes.

DOCX opens in Google Docs with clear title hierarchy

DOCX opens in Google Docs with clear section hierarchy

spacing rhythm is visibly improved and consistent

Section 1 table is readable and structured

Section 2 subsections are clearly separated and readable

Account Manager Context is cleanly structured

What We Need to Do Next remains structured and professional

Additional Opportunities no longer looks broken

labels and values do not blur together

long text blocks are readable, not cramped dumps

naming is consistent with current QBS naming

preview is unchanged

PDF export is unchanged or improved without regression

DOCX content ordering is correct

Anchored Tides DOCX feels like the same report family as the PDF, even if simplified

no Word-only fragile styling is used as a critical dependency

Google Docs import result is visually acceptable as a client-facing artifact

==================================================
REQUIRED TEST SCENARIOS

You must explicitly test and report:

Anchored Tides QBS preview

Anchored Tides PDF export

Anchored Tides DOCX export

Anchored Tides DOCX opened in Google Docs

Section 1 readability in Google Docs

Section 2 readability in Google Docs

Additional Opportunities readability in Google Docs

naming consistency across preview/PDF/DOCX

regression check for content order and section presence

==================================================
REQUIRED OUTPUT FORMAT

Return a formal proof report with these exact sections:

Root formatting failures found

exact places hierarchy was being lost

exact DOCX structures/styles that were too fragile for Google Docs

Files changed

every file changed

what changed in each

DOCX style system added

exact style names

what each style is used for

Table/export rebuilds completed

which QBS sections were rebuilt

what structure each now uses

Google Docs hardening changes

exact formatting choices made for Google compatibility

exact fragile techniques removed/replaced

Before/after proof

screenshots or render evidence for:

title area

Section 1

Section 2

Additional Opportunities

one closing/action section

QA scenarios tested

one line per scenario

pass/fail

observed result

Regression check

preview

PDF

DOCX content ordering

naming consistency

Final status

explicitly state whether the DOCX export is fully acceptable in Google Docs

==================================================
FINAL HARD RULE

If the exported DOCX still opens in Google Docs looking like a flattened text dump instead of a polished client-facing report, the task is not complete.