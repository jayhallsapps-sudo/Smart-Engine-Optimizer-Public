You are doing a full generalization pass on the QBS generator. This is not an Anchored Tides cleanup. This is a system-wide hardening pass so the QBS works correctly across all clients and does not rely on Anchored Tides-specific handling.

Do not give me reassurance.
Do not tell me “the logic is already mostly generalized.”
Do not tell me “the risk is low.”
I need you to remove the remaining client-specific behavior, close the confirmed generalization gaps, and prove that the fixes are system-safe.

PRIMARY OBJECTIVE

Take the findings from the generalization audit and convert them into actual system-wide fixes so the QBS generator behaves correctly across multiple clients, not just Anchored Tides.

This pass must:
- remove Anchored Tides-specific hardcoding
- fix the known cross-client logic gaps
- preserve the improvements that genuinely generalize
- avoid introducing regressions into the now-working Anchored Tides flow

==================================================
CONFIRMED GENERALIZATION PROBLEMS TO FIX
==================================================

1) REMOVE CLIENT-SPECIFIC HARDcoded URLS FROM LIVE PAGE VERIFICATION

Current problem:
PAGE_CHECK_GROUPS still contains Anchored Tides-specific paths:
- /admissions-and-alcohol-rehab-insurance/
- /about-anchored-tides

That is not acceptable in a multi-client system.

Required fix:
- remove Anchored Tides-specific paths from PAGE_CHECK_GROUPS
- replace them with generalized resolver logic or generalized candidate patterns
- do not leave account-specific URLs in shared generator code

Acceptance requirement:
- no Anchored Tides-specific URL remains hardcoded in PAGE_CHECK_GROUPS or equivalent shared resolution logic

2) GENERALIZE LIVE PAGE RESOLUTION WITHOUT CLIENT-SPECIFIC PATHS

Current problem:
The live HTTP verification mechanism is generic, but the candidate path pool is not fully generic.

Required fix:
- preserve the generic verifyLivePages() mechanism
- generalize the candidate path generation so it can resolve likely admissions, about, verify-insurance, contact, and Levels of Care pages across clients without relying on Anchored Tides paths
- use pattern-based candidate generation where appropriate
- keep this suitable for behavioral health clients broadly, not just one site structure

Important:
This should still work for non-standard nested URLs when reasonable.
Do not revert to weak generic guessing.
Do not reintroduce fabricated paths.

Acceptance requirement:
- candidate path generation is no longer dependent on Anchored Tides-specific slugs
- live-page verification remains effective across BH clients with varied URL structures

3) FIX CALL TRACKING GENERALIZATION GAP

Current problem:
The system detects CallRail, CallTrackingMetrics, and Nimbata, but only CallRail has a real query implementation.
That means CTM/Nimbata clients may appear supported while silently falling back to GA4/GSC-only behavior.

Required fix:
Choose one of the following and implement it cleanly:
- implement real CTM and Nimbata query functions, or
- remove / disable CTM and Nimbata detection until supported, with honest fallback behavior and explicit reporting

Not acceptable:
- detecting unsupported providers as though they are active data sources
- silently implying call tracking is incorporated when it is not

Acceptance requirement:
- the system does not pretend unsupported call-tracking providers are functioning
- provider behavior is honest and technically correct across all clients

4) FIX CREATE-NEW VS CRO / REFRESH FOR ISSUES-FORMAT SF CLIENTS

Current problem:
When Screaming Frog is issues-format only and has no URL inventory, the system tends to over-recommend create-new content even when GSC already proves a page exists.

Required fix:
- when sfIsUrlCrawl is false, do not default blindly to create-new
- if GSC page data confirms a live ranking page already exists for the keyword target, prefer optimize-existing / CRO-update / refresh logic instead of create-new
- preserve create-new only where no credible existing-page evidence exists

Important:
A ranking GSC page is strong evidence of existence.
Do not ignore it just because SF lacks a URL column.

Acceptance requirement:
- issues-format SF clients no longer get systematic over-recommendation of net-new content
- create-new vs existing-page decisioning uses the best available evidence across integrations

5) REMOVE REMAINING USER-FACING “SERVICE PAGE” TERMINOLOGY LEAKS

Current problem:
The audit confirmed two user-facing strings still say “service page” in output branches that can render for other clients.

Required fix:
- replace the user-facing “service page” strings at the confirmed branches
- ensure those rendered reasons use the correct Levels of Care framing where applicable
- search for any remaining user-facing output branches that still emit “service page” / “service pages” incorrectly

Important:
Do not do blind replacement in internal detection logic.
Only fix user-facing rendered copy where the terminology is wrong.

Acceptance requirement:
- no user-facing QBS output branch emits “service page” / “service pages” where Levels of Care framing is required

6) REMOVE CLIENT-NAME ACCUMULATION IN BRANDED SIGNALS

Current problem:
The branded signal regex is accumulating client names directly in code, including Anchored Tides and other clients.

That is not scalable.

Required fix:
- remove hardcoded client names from shared branded signal detection logic
- drive branded-term handling from client.brandTerms, client.name-derived values, or another proper client-configured source
- do not keep growing a hardcoded regex of client brands inside generator logic

Acceptance requirement:
- branded signal detection no longer depends on a manually accumulated list of client names in shared code

7) HARDEN SANITIZESTRING WITHOUT CLIPPING LEGITIMATE COPY

Current problem:
The main “already” bug was fixed, but the audit confirms remaining sanitize patterns may still clip valid sentence endings for other clients.

Required fix:
- audit the remaining sanitizeString fragment-removal patterns
- remove or tighten any pattern that can erase legitimate completed prose
- preserve protection against broken template fragments, but do not use aggressive tail-stripping that can eat valid copy

Important:
Favor safer, more targeted bad-fragment removal.
Do not use broad regexes that risk clipping real output.

Acceptance requirement:
- sanitizeString no longer carries obvious cross-client clipping risk from broad tail-fragment patterns

8) DEDUPLICATE SHARED SOURCE HELPERS

Current problem:
computeSharedSource() and related logic are duplicated across renderers.
They match now, but this creates future divergence risk.

Required fix:
- extract shared-source helper logic into a single shared utility module
- import and use that shared implementation across preview, print, HTML/PDF, and any other relevant renderer path

Acceptance requirement:
- shared-source logic has one source of truth
- renderer divergence risk is reduced

==================================================
PRESERVE WHAT ALREADY GENERALIZES WELL
==================================================

Do not break these genuinely generalized improvements:
- regex-based detox / detoxification / residential / inpatient / PHP / IOP / outpatient / dual diagnosis normalization
- generalized hybrid admissions+insurance classification pattern
- Tier diagnosis framework as scoped for Webserv behavioral health clients
- false-negative / manual QA handling behavior
- SF abbreviation behavior
- keyword topic clustering architecture, while improving only where truly needed
- PDF / preview alignment where already working

==================================================
MANDATORY IMPLEMENTATION STANDARD
==================================================

For every fix above:
- implement the actual code change
- do not leave it as a TODO
- do not hide behind “future enhancement”
- do not say “pattern-based enough”
- do not keep known client-specific residue in shared code

If a fix cannot be completed cleanly in this pass, say so explicitly and explain why.
Do not pretend it is solved.

==================================================
REQUIRED QA OUTPUT
==================================================

After implementation, provide a QA report with exactly these sections:

1. HARDcoded CLIENT-SPECIFIC LOGIC REMOVED
Show:
- which Anchored Tides-specific paths / names / assumptions were removed
- exact code locations changed

2. LIVE PAGE RESOLUTION GENERALIZATION
Show:
- how candidate path generation now works generically
- why it is no longer Anchored Tides-specific

3. CALL TRACKING PROVIDER HONESTY
Show:
- whether CTM and Nimbata are now truly supported
or
- whether they were intentionally disabled / made explicit as unsupported
- no silent fake support

4. CREATE-NEW VS EXISTING-PAGE DECISIONING
Show:
- how issues-format SF clients now avoid false create-new recommendations when GSC confirms a live page exists

5. USER-FACING TERMINOLOGY CLEANUP
Show:
- exact user-facing “service page” strings removed or corrected
- confirm no remaining incorrect user-facing usage in rendered output branches

6. BRANDED SIGNAL GENERALIZATION
Show:
- how branded detection now works without hardcoded client-name accumulation in shared code

7. SANITIZESTRING SAFETY
Show:
- which remaining regex patterns were tightened, removed, or left in place
- why the remaining behavior is safe

8. SHARED UTILITY DEDUPLICATION
Show:
- which duplicated helper(s) were extracted into shared utilities
- where renderers now import them from

9. REGRESSION CHECK
Confirm that none of the previously solved Anchored Tides behaviors regressed

==================================================
MANDATORY FINAL SUMMARY
==================================================

End with these 3 sections:

SAFE SYSTEM-WIDE NOW
List the fixes that are now genuinely safe across all clients.

STILL NEEDS WORK
List anything that remains partially generalized or not fully solved.

FINAL VERDICT
Choose one:
- Fully generalized for Webserv BH clients
- Mostly generalized with minor remaining risk
- Still mixed / not yet safe system-wide

You must justify the verdict plainly.

==================================================
VALIDATION REQUIREMENT
==================================================

Do not answer from memory.
Do not answer from intention.
Trace the actual implementation after your changes.

If something is still client-specific, say so plainly.
If something is only pattern-based but not truly universal, say so plainly.
Honesty matters more than a clean-looking summary.

FINAL INSTRUCTION

This is the generalization hardening pass.
Remove the confirmed Anchored Tides-specific residue, close the known multi-client gaps, preserve the good generalized logic, and prove that the QBS system is safer across all Webserv behavioral health clients.