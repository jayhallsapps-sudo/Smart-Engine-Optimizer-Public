You are doing one final micro-fix pass on the QBS generator and output. This is not a rebuild. This is not a logic overhaul. This is a precision cleanup pass to correct the last confirmed artifact-level mismatch and do a quick regression check.

Do not reopen solved logic.
Do not redesign the report.
Do not change the red QBS design language.
Do not change the table design system.
Do not alter working tier logic, page resolution logic, keyword clustering logic, or source-label behavior unless required to fix the exact issue below.

PRIMARY OBJECTIVE

Fix the last confirmed mismatch in the generated Anchored Tides QBS PDF and verify that no previously solved behavior regressed.

CONFIRMED ISSUE TO FIX

Section 7 — “About This List” mismatch

Current problem:
The Section 7 summary box says the report is “showing up to 8 keyword opportunities (2× monthly credit capacity of 4),” but the rendered keyword table below contains 10 rows.

This is not acceptable.
Any summary box describing a cap, count, or limit must match the actual rendered number of rows in that section.

Required fix:
- make the Section 7 “About This List” copy dynamically match the actual rendered keyword row count
- if the section renders 10 rows, the summary must say 10
- if the section renders 8 rows, the summary must say 8
- do not hardcode the count in summary text unless the table itself is hard-capped to the same number

Important:
The summary must remain concise and readable.
Keep the current red summary-box styling intact.
Do not regress the keyword grouping, slug behavior, or IOP correction already solved.

REGRESSION CHECK REQUIREMENTS

While fixing the count mismatch, verify that none of these previously solved issues return:

1. No fabricated generic paths in Section 2
- do not allow /programs/detox or /programs/residential to reappear unless those exact live URLs truly exist

2. No false “Manual entry needed” fallback in Section 4 for pages already resolved live

3. No Tier 1 false negatives in Section 5 for confirmed live pages

4. No “service page” / “service pages” terminology returning where Levels of Care framing is required

5. No regression of the solved IOP row in Section 7
- it must remain a CRO / supporting update on the confirmed live page, not a create-new recommendation

6. No regression of corrected terminology in Section 8
- “Levels of Care and VOB pages” must remain intact

7. No regression of corrected Tier 1 pass / Tier 2 gap framing in Section 9

8. No source-badge wrapping regression in Section 1

FINAL VALIDATION REQUIREMENT

Use Anchored Tides again as the validation account.

Required steps:
1. Generate a fresh Anchored Tides QBS using the live connected data and the same AM inputs already provided.
2. Export the actual PDF.
3. Provide a direct link/path to the generated PDF.
4. Provide a short QA note that covers only:
   - the Section 7 summary-count fix
   - confirmation that the rendered row count matches the summary text
   - confirmation that none of the listed regressions reappeared

FAILURE RULE

If the generated Anchored Tides PDF still has any mismatch between summary-box counts and actual rendered row counts, do not declare completion.

If any previously solved issue reappears, do not declare completion.

Instead, list:
- the exact section
- the exact regression or mismatch
- the exact fix still required

FINAL INSTRUCTION

This is a micro-fix pass.
Fix the Section 7 count mismatch, regenerate the Anchored Tides PDF, and confirm that no solved behavior regressed.
Do not send a summary-only reassurance without the PDF artifact link.