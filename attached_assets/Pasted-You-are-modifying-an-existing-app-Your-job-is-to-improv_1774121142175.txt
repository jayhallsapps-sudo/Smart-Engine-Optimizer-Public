You are modifying an existing app. Your job is to improve one specific module and its related integration reliability behavior without breaking, regressing, renaming, restyling, or destabilizing any unrelated feature.

NON-DESTRUCTIVE RULES
- Treat the current app as production-like.
- Make the smallest safe set of changes required to complete this module upgrade.
- Do not refactor unrelated modules.
- Do not rename, restyle, reroute, or restructure unrelated features.
- Do not change unrelated API contracts, env vars, database schemas, navigation, reporting flows, auth, or UI patterns unless strictly required for this module to work.
- If any required change could affect another feature, stop short of making that destructive change and instead document it at the end under:
  "Open Questions / Potentially Destructive Changes Not Applied"
- Preserve existing working workflow where it is already good.
- Before changing anything, inspect the current ACA / Ask Claude Anything / AMA implementation end-to-end and identify exactly why it is returning fake, fallback, incorrect, or access-failure answers.

PRIMARY GOAL
We are cleaning up the ACA module and its surrounding integration reliability logic so that:
1. The module is renamed and cleaned up in the UI
2. The answers are grounded, source-scoped, and truthful
3. Connected integrations remain reliably connected
4. False “not configured” / access failure states are eliminated
5. Real disconnections are surfaced clearly in the app
6. These rules are enforced consistently across all AI providers / model paths used in the app

MODULE CHANGES REQUIRED
We are renaming the ACA module to:
- Module title: AMA
- Expanded meaning: Ask Me Anything

Update the core module description text to exactly:
- ask the onboard AIs anything about clients, integrations, data, and get live data pulls.

UI / UX REQUIREMENTS
Keep:
- client selector
- sources selector
- current general workflow, if it is already good

Remove:
- the "data sources" header at the top of the AMA area

Voice-to-text:
- The microphone / speech-to-text control currently shows listening feedback but does not populate the prompt field afterward.
- Fix speech-to-text so that after recording completes, the recognized transcript is inserted into the prompt input/textarea.
- Keep visible listening feedback.
- Handle permission denied, no speech detected, timeout, unsupported browser, and recognition failure states gracefully with clear user feedback.
- Do not submit automatically after transcription. Only populate the prompt field.

CORE ANSWERING REQUIREMENT: REAL ANSWERS ONLY
Current problem:
- The AMA feature is producing nonsense, fake answers, fallback answers, incorrect answers, and false access / config errors.
- That is unacceptable.

Desired behavior:
- AMA must answer like a competent research agent.
- Once the user selects:
  1. a client
  2. one or more integrations / sources
- and asks a question,
  the system must answer ONLY from the selected client context and selected sources/integrations, plus any explicitly selected website/source context.

NO MADE-UP ANSWERS
- Do not hallucinate.
- Do not invent metrics, statuses, integration results, client facts, URLs, reports, time windows, or findings.
- If the selected sources do not contain enough evidence, the assistant must say so clearly and specifically.
- If no source returns enough data, respond with a transparent insufficiency message instead of a fabricated answer.
- Never fill gaps with generic SEO filler or fake summaries.

MANDATORY ANSWERING MODEL
For every AMA response, enforce this behavior:
1. Read the selected client
2. Read the selected integrations / sources
3. Retrieve actual relevant data from only those selections
4. Build the answer from retrieved evidence
5. Include a visible evidence section or source summary showing what was actually used
6. Clearly distinguish:
   - confirmed facts
   - inferred conclusions
   - unavailable / missing data

If possible within current architecture, every answer should follow a structure similar to:
- Answer
- Sources used
- What I could confirm
- What I could not confirm

GROUNDING RULES
- The selected client must scope the query.
- The selected sources must constrain retrieval.
- If a source is not selected, do not use it.
- If an integration is disconnected, stale, unauthorized, or empty, say that clearly.
- If live pulls are supported for the selected source, execute the live pull.
- If only cached data is available, clearly label it as cached and state timestamp / recency if available.
- If website crawling/page context is part of selected sources, retrieve from that actual website context, not from memory.

FALLBACK POLICY
Unacceptable fallback:
- generic SEO advice when user asked for client-specific data
- fabricated summaries
- placeholder claims
- “best guess” answers presented as fact
- “not configured” or “access failure” messages when the source is actually connected and healthy

Acceptable fallback:
- a precise response that says which selected source(s) returned insufficient evidence
- a partial answer grounded in the sources that did work
- a request to refine the question only if needed after attempting retrieval

NEW INTEGRATION RELIABILITY REQUIREMENTS
There is an ongoing issue where CallRail, GA4, and Airtable are reportedly showing as 100% connected / healthy in the system, yet AMA or related query paths still return access failure, not configured, or equivalent errors.

You must investigate and fix the root cause of this contradiction.

This is now a first-class requirement.

Your job is to determine:
1. Why CallRail, GA4, and Airtable can appear connected in the UI / integration status layer but fail in AMA or query execution
2. Whether the problem is caused by:
   - stale tokens
   - token refresh failure
   - per-provider auth mismatch
   - workspace / client scoping mismatch
   - env vs database credential mismatch
   - backend tool registration mismatch
   - source-to-tool mapping mismatch
   - AI-provider-specific tool invocation differences
   - disconnected internal state between “integration health” and “query execution”
   - cached status not matching live execution reality
   - missing account / property / workspace IDs despite healthy connection state
   - silent exception swallowing
   - provider-specific prompt / tool-call handling bugs
   - other root causes found in inspection

CONNECTED APIs MUST STAY CONNECTED
- If an integration has already been successfully connected, the system should keep it connected.
- The system must not force repeated daily reconnections unless there is a real underlying auth expiration or provider-side revocation.
- If re-authentication is actually required, explain exactly why and surface that reason clearly in the UI.
- If re-authentication is not required, do not show not-configured, disconnected, or access-failure responses.

TRUE CONNECTION-STATE MODEL
You must unify or reconcile integration state so the app reflects reality.

Required behavior:
- If a source is truly connected and queryable, the app must treat it as connected and use it successfully.
- If a source is truly disconnected, expired, unauthorized, or missing critical config, the app must show that clearly in the app UI.
- A source must never simultaneously appear:
  - “100% connected / healthy” in the integrations area
  - but “not configured / access failed” in AMA
unless the system also shows a precise warning that explains the exact mismatch.

REQUIRED DISCONNECT NOTIFICATION BEHAVIOR
If any connected API later becomes disconnected, expired, revoked, or otherwise unusable:
- show an in-app notification / warning / status indicator
- identify which integration is affected
- identify the reason if known
- indicate whether the issue is:
  - auth expired
  - token refresh failed
  - missing scope
  - provider-side revocation
  - workspace/property mismatch
  - config incomplete
  - temporary API outage
  - unknown failure requiring manual reconnect

If an integration is healthy, do not show “not configured” or generic access failure during AMA / query execution.

ALL AI PROVIDERS MUST FOLLOW THE SAME RULES
This app uses multiple AIs depending on:
- available credits
- provider availability
- task purpose

You must inspect all AI execution paths involved in AMA and ensure the same grounding and integration reliability rules apply across all model/provider paths.

That means:
- No provider path may bypass selected-source constraints
- No provider path may fabricate answers
- No provider path may emit generic “not configured” messages when the integration is actually connected
- No provider path may silently drop tool results and replace them with filler
- No provider path may have weaker source transparency than the others
- If one provider lacks a capability another uses, the app must handle that transparently instead of degrading into nonsense

Audit all relevant provider paths, including but not limited to:
- primary provider
- fallback provider(s)
- low-credit / failover provider path(s)
- model-specific tool invocation logic
- any provider adapters or orchestration layers

ARCHITECTURE / IMPLEMENTATION EXPECTATIONS
Audit and fix the full AMA request and integration-reliability pipeline:
- front-end request construction
- selected client propagation
- selected source propagation
- backend route / handler
- retrieval orchestration
- source filtering
- grounding / context assembly
- model prompt / system prompt for AMA
- AI provider routing / fallback logic
- provider-specific tool execution
- integration health check logic
- integration status persistence
- token refresh / auth handling
- source-to-tool mapping
- response rendering
- error handling
- logging / observability

Specifically verify:
- selected client id is actually passed through to the backend
- selected source ids / source types are actually passed through to the backend
- backend is not silently falling back to a generic answer path
- model prompt is not missing retrieved context
- empty retrieval results do not trigger fabricated assistant output
- stale mock data, dev fixtures, or placeholder responses are removed from production path
- any “fallback answer” code path is either removed or replaced with transparent insufficiency handling
- integration “connected” status is based on real, query-relevant health
- connection health and execution health are not being tracked in contradictory ways
- CallRail, GA4, and Airtable access paths are using the same truth source as the integration status layer, or a clearly reconciled version of it
- any required account/property/workspace/table IDs are actually present where query execution needs them
- provider-specific orchestration does not lose credentials, tool bindings, or source context

PROMPTING / MODEL BEHAVIOR FOR AMA
Update the AMA model instructions so the assistant behaves like a retrieval-grounded analyst, not a creative chatbot.

System behavior for AMA should explicitly enforce:
- answer only from selected client + selected sources
- never invent missing details
- prefer “I couldn’t confirm that from the selected sources” over guessing
- surface evidence
- cite source names / integration names in readable form
- when conflicting data exists, mention the conflict instead of picking one silently
- when a selected integration is connected but returns execution failure, explain the exact failure if known rather than saying “not configured”
- distinguish:
  - connected but temporarily unavailable
  - connected but missing execution scope/config detail
  - disconnected
  - insufficient data

DESIRED RESPONSE QUALITY
The answers should feel similar in quality to a strong research agent:
- thorough
- client-specific
- source-aware
- correct
- explicit about uncertainty
- capable of answering questions about:
  - clients
  - integrations
  - data freshness
  - live pulls
  - performance context
  - source coverage
  - website/source findings
so long as those are supported by the selected sources

DO NOT OVERBUILD
- Do not redesign the whole module.
- Do not change working workflow unnecessarily.
- Do not add speculative features unless needed to support grounding, traceability, reliability, or truthful connection-state reporting.
- Prioritize correctness, source-scoping, provider consistency, connection persistence, and transparency.

VISIBLE UX IMPROVEMENTS TO INCLUDE
If feasible without disrupting layout, add lightweight source transparency in the AMA result area:
- selected client name shown in context
- selected sources used shown in context
- optional freshness / timestamp labels if available
- clear empty-state if no sources are selected
- clear warning if selected sources return no relevant data

Also add or improve integration-state visibility:
- if an integration is disconnected, show that in-app
- if an integration is healthy, do not surface misleading failure language in AMA
- if there is a mismatch between health check and execution check, surface a precise warning until fixed

ACCEPTANCE CRITERIA
The task is only complete if all of the following are true:

1. Branding / label changes
- ACA label is replaced by AMA
- description reads exactly: "ask the onboard AIs anything about clients, integrations, data, and get live data pulls."

2. UI cleanup
- “data sources” header is removed
- client selector remains
- sources selector remains
- current workflow remains intact unless a change was required for correctness

3. Voice-to-text
- microphone shows listening state
- spoken text populates the prompt field after capture
- errors are handled visibly and safely
- no silent failure
- no auto-submit after transcription

4. Grounded answers
- AMA answers are constrained by selected client + selected sources
- no fabricated answers
- no fake fallback summaries
- insufficient data is stated clearly

5. Retrieval integrity
- selected client and selected sources are verifiably used in the backend retrieval pipeline
- unselected sources are not used

6. Transparency
- responses visibly indicate which sources/integrations were actually used
- missing, stale, disconnected, or temporarily unavailable data is labeled accurately

7. Integration truthfulness
- CallRail, GA4, and Airtable do not report “not configured” / access failure when they are actually connected and healthy
- if they fail, the exact reason is investigated and surfaced truthfully
- connection state in UI matches execution reality

8. Connection persistence
- connected APIs remain connected unless there is a real auth / provider / scope failure
- repeated unnecessary reconnection is eliminated
- if reconnect is required, the UI explains exactly why

9. Provider consistency
- all AI provider paths used by AMA follow the same grounding, source-scoping, and truthful failure-handling rules

10. Regression safety
- no unrelated module regressions
- no broken routes, layouts, selectors, auth flows, reports, or integrations outside what was necessary

MANDATORY TESTING
Do not claim completion without evidence.

Run and report:

A. Code-path verification
- show the relevant request payload fields for selected client and sources
- show the backend receiving them
- show retrieval filtering using them
- show provider-routing logic used for AMA
- show where integration health is read
- show where execution health is validated
- show how contradictory health states were resolved

B. Functional tests
At minimum test these cases:
1. client selected + 1 valid source selected + answerable query
2. client selected + multiple valid sources selected + cross-source query
3. client selected + no sources selected
4. client selected + source selected but source has insufficient data
5. client selected + disconnected / expired / revoked source
6. client selected + source appears healthy in UI and must succeed in AMA
7. CallRail healthy-state execution test
8. GA4 healthy-state execution test
9. Airtable healthy-state execution test
10. provider A AMA query
11. provider B / fallback AMA query
12. voice-to-text happy path
13. voice-to-text permission denied / failure path

C. Root-cause tests for false failures
For CallRail, GA4, and Airtable, provide:
- what the UI status says
- what the backend health check says
- what the execution layer says
- whether credentials/tokens exist
- whether required IDs/scopes exist
- exact reason for any mismatch
- exact fix applied

D. Regression checks
- confirm module renders properly
- confirm selectors still work
- confirm submitting a query still works
- confirm no unrelated module changed unexpectedly
- confirm no unrelated integrations regressed

E. Proof of grounding
For at least 3 example AMA queries, provide:
- the exact selected client
- the exact selected sources
- the question asked
- the evidence retrieved
- the final answer returned
- why the answer is grounded
- confirmation that no fabricated content was inserted

F. Proof of provider consistency
For each AI/provider path used by AMA, provide:
- provider name/path
- whether tools/sources were available
- whether selected-source filtering was enforced
- how failure messaging behaves
- proof that no provider path degrades into nonsense or fake config errors

MANDATORY FINAL REPORT FORMAT
When you finish, do NOT give me a vague summary. Give me a proof package in this exact structure:

1. Executive Status
- Fixed / Partially Fixed / Blocked
- one-paragraph summary

2. What Changed
- file-by-file list
- what changed in each file
- why the change was necessary

3. Root Cause Analysis
- why AMA was producing nonsense before
- exact broken paths / fallback behavior / missing propagation / prompt issues found
- why CallRail, GA4, and Airtable were failing despite appearing connected
- whether the issue was provider-specific, integration-specific, execution-specific, state-sync-specific, or multiple

4. Evidence of Correctness
- request payload proof
- backend proof
- retrieval/source-filter proof
- provider-routing proof
- integration-health proof
- execution-health proof
- response rendering proof
- voice-to-text proof

5. Test Results
- each test case
- pass/fail
- concrete output / observed behavior
- screenshots or logs if available

6. Regression Check
- what unrelated areas were checked
- proof no regressions were introduced

7. Open Questions / Potentially Destructive Changes Not Applied
- anything you chose not to change because it might affect another feature
- why it was not changed
- recommended next step if needed

8. Definition of Done Check
- list every acceptance criterion above
- mark pass/fail with proof

CRITICAL REPORTING RULE
“Done” is not acceptable evidence.
“Tested” is not acceptable evidence.
“Implemented” is not acceptable evidence.

You must provide proof that each important claim is true:
- logs
- payloads
- screenshots
- DOM evidence
- route evidence
- example outputs
- before/after proof
- exact file diffs or summarized code-path evidence

If you cannot prove something, say it is unproven.

WORKING STYLE
- Inspect first
- Change only what is necessary
- Verify with evidence
- Report with proof
- Protect the rest of the app