Update the QBR Prep generator to align more closely with the QSSB spec, while keeping the current PDF output format intact for now. Do not change the final deliverable away from PDF yet.

Primary goals for this pass:
1. Rework Section 2 so it matches the QSSB structure more closely
2. Add explicit audit intake logic before Section 6
3. Add cross-sell / upsell preview plus AM confirmation flow inside Section 6
4. Respect content credit capacity using the existing Skill.md logic/source already used elsewhere in the project
5. Use the SEO Strategy Bank in Notion as the source for cross-sell / upsell opportunity matching
6. Do not break existing PDF export

Important constraints:
- Keep the current QBR Prep report generation working end to end
- Keep the visual style consistent with the current report unless a structural change is required
- Do not remove existing useful data unless it directly conflicts with the QSSB requirements
- Where there is a conflict between “showing more rows” and “following the QSSB structure,” prefer the QSSB structure
- PDF remains the output for now
- Do not overbuild a giant new audit feature; add only the intake and traceability needed for compliance
- Do not fabricate upsells, cross-sells, or opportunity matches when evidence is weak

SECTION 2 CHANGES — REQUIRED

Rebuild Section 2: “Where Conversions Actually Happen” to follow this structure and logic.

QSSB target:
- Top 2 converting pages
- Top 2 conversion patterns
- Top 2 converting sources

Current problem:
- The current report shows a broader “Top Converting Pages” output and a “Top Converting Sources” output
- It does not clearly break out “Top 2 conversion patterns” as its own required output
- It does not fully follow the required QSSB table shape
- It currently shows more than the required top 2 items

Implement Section 2 like this:

A. Top Converting Pages table
Use exactly this table structure:
| Type | Page / Pattern | Conversion Source | Notes / What We’re Learning |

Rules:
- Limit to exactly the top 2 page-level rows
- These must be actual pages, not generalized patterns
- Populate Conversion Source explicitly for each row
- Use the strongest available source attribution, such as CallRail, CTM, Nimbata, GA4, GSC, or Multi-source
- Notes should stay brief and analytical
- Do not let weak inference fill the table if stronger rows are available

B. Top Conversion Patterns table
Add a new table directly under Top Converting Pages:
| Pattern | Why It Matters | Evidence |

Rules:
- Limit to exactly the top 2 patterns
- Patterns are recurring conversion themes, not single URLs
- Examples include:
  - admissions or contact intent pages
  - verify insurance or VOB intent pages
  - high-intent service pages
  - informational pages assisting the admission path
- Evidence should explain why the pattern was identified from available inputs
- Do not invent patterns unsupported by the data

C. Top Converting Sources table
Use this table:
| Source | What’s Converting | Notes / What We’re Learning |

Rules:
- Limit to exactly the top 2 sources
- Prioritize the strongest conversion-relevant sources
- Notes should remain brief and analytical

D. Ranking logic for Section 2
When deciding the top 2 pages, top 2 patterns, and top 2 sources, follow this priority order:
1. Real conversion-backed evidence
2. Call-tracking landing-page evidence
3. GA4 conversion or high-intent engagement evidence
4. GSC-supported high-intent inference
5. Lower-confidence assisted-conversion inference

Additional ranking rules:
- Prefer fewer stronger rows over more weak rows
- Prefer rows tied to admits, VOBs, qualified calls, or clear admission-path actions
- Keep confidence weighting internal, but expose a clean caveat beneath Section 2 when attribution is incomplete

E. Confidence caveat behavior
If data is incomplete, keep the caveat, but rewrite it cleanly beneath Section 2 rather than bloating the tables.

Example style:
“Direct admit attribution remains partially inferred where tracking is incomplete, so some Section 2 conclusions are confidence-weighted rather than fully verified.”

SECTION 6 AUDIT INTAKE — REQUIRED

QSSB requires that before generating Section 6, the system must ask for or use a recent site audit screenshot or audit snapshot if one has not already been provided.

Implement this workflow:

A. Pre-Section-6 audit check
Before Section 6 is generated, determine whether recent audit input already exists in the report generation context.

Accepted audit inputs can include:
- a recent audit screenshot
- a recent audit summary
- structured audit findings already available in the generation pipeline
- known technical audit context already attached upstream

B. Missing audit behavior
If no audit input exists, do not silently proceed as though there is one.

Instead, surface a required input step asking the AM to either:
- upload a recent audit screenshot, or
- paste a short audit summary

Use simple wording such as:
“Before finalizing quarterly priorities, please provide a recent site audit screenshot or a short audit summary so the strategy can account for technical blockers, internal linking gaps, page quality issues, or structural weaknesses.”

C. Audit-aware generation
Once audit input exists, Section 6 must explicitly use it when forming priorities.

The system should be able to pull from audit evidence such as:
- missing titles or metadata issues
- thin or duplicate content
- indexation or crawl issues
- internal linking gaps
- structural or architecture issues
- page-quality weaknesses

D. Provenance and traceability
Do not make the audit logic invisible.
Where an initiative is meaningfully supported by audit findings, make that traceable in the Reason column using short language such as:
- “Manual audit confirms thin or duplicate content”
- “Audit flags internal linking weakness on key entry pages”
- “Audit findings show metadata or indexation issues on core pages”

E. Scope control
For this pass, do not build a separate audit appendix or standalone audit module.
The requirement is only:
- explicit audit intake before Section 6 when missing
- clear use of audit evidence inside Section 6 reasoning

SECTION 6 CROSS-SELL / UPSELL FLOW — REQUIRED

Add the QSSB-required cross-sell and upsell logic after the core Section 6 priorities are generated.

Source of truth for opportunity matching:
- Use the SEO Strategy Bank in Notion as the primary source for identifying relevant in-scope SEO work, cross-sells, and upsells
- Replit has access to that Notion content, so it should use it during opportunity evaluation
- Match opportunities against the current account snapshot rather than listing generic offers
- Only suggest items that are clearly supported by the report evidence and by relevant Strategy Bank entries

Workflow:

A. Generate the normal core priorities table first
Keep the main priorities table limited to 5–7 rows, or respect the current enforced report limit if one already exists elsewhere.

B. After core priorities are generated, evaluate whether any possible opportunities are strongly supported by the available snapshot data

Evidence sources may include:
- goals and goal shifts
- Section 2 conversion pages, patterns, and sources
- Section 3 traffic-driver patterns
- Section 4 site service coverage
- Section 5 tier diagnosis
- audit findings
- AM hypothesis or blended AM input
- content credit capacity from Skill.md logic
- relevant matching items from the SEO Strategy Bank in Notion

C. Only if signals are strong, generate a preview table for AM review
Use this preview table:
| Potential Opportunity | Why It Appears Relevant | Suggested Category |

Rules:
- Limit preview to 2–3 items maximum
- Suggested Category may be one of:
  - in-scope SEO work
  - cross-sell
  - upsell
  - not relevant
- Do not assume final scope simply because the system suggests a category
- Do not fabricate opportunities when evidence is weak
- Do not surface irrelevant Strategy Bank items just because they exist in Notion
- Opportunity descriptions should be specific and tied to the account condition

D. AM confirmation step is required
If preview items exist, pause and ask the AM to classify each one as:
- in-scope SEO work
- cross-sell
- upsell
- not relevant

Do not finalize the cross-sell or upsell subsection until the AM confirms classification.

E. Final subsection behavior
Only after AM confirmation:
- If at least one item is confirmed as cross-sell or upsell, add this subsection:

### Cross-Sell / Upsell Opportunities

Use this table:
| Recommendation | Type | Why It Makes Sense Now |

Rules:
- Include only AM-confirmed cross-sell or upsell items
- Do not include items marked in-scope or not relevant
- Keep recommendations brief, evidence-based, and directly relevant
- No vague sales language
- Wording should feel strategic, not pushy

F. If no strong opportunities exist
Do not force the subsection.
Either:
- omit it entirely, or
- store that no strong cross-sell or upsell was supported this quarter

CONTENT CREDIT CAPACITY — REQUIRED

Use content credit capacity from Skill.md and any existing project logic as the source of truth for pacing realism.

Rules:
- Section 6 priorities must be realistic within the account’s actual production capacity
- Do not recommend a quarterly roadmap that exceeds known execution bandwidth
- When choosing between equally valid priorities, prefer the ones most likely to drive admits, VOBs, qualified calls, or strong assisted-conversion outcomes within available capacity
- Use content capacity as an actual decision input, not a decorative field

DATA AND LOGIC GUARDRAILS

- Do not invent audit data
- Do not invent conversion patterns unless clearly supported
- Do not over-prioritize blog or informational expansion when Tier 1 or Tier 2 issues exist
- Follow the Treatment Center SEO Priority Framework
- Keep recommendations tied to admits, VOBs, qualified calls, or meaningful assisted-conversion behavior
- Avoid generic SEO filler language
- Prefer business-impact logic over traffic vanity
- Where tracking is weak, remain honest about inference level

UX AND FLOW REQUIREMENTS

- If Section 6 cannot be completed because audit input is missing, the user should receive a clear input request rather than a silently weakened report
- If cross-sell or upsell preview items exist, the report flow should support AM confirmation before final PDF completion
- Keep the flow usable and not clunky
- Store intermediate state where needed so the user can confirm classifications and continue generation without losing the rest of the report
- Do not create a confusing multi-step experience unless it is necessary for compliance with the above logic

TESTING AND ACCEPTANCE CRITERIA

1. Section 2 outputs:
- a Top Converting Pages table with exactly 2 rows
- a Top Conversion Patterns table with exactly 2 rows
- a Top Converting Sources table with exactly 2 rows

2. Top Converting Pages includes an explicit Conversion Source column

3. Section 6 cannot silently rely on hidden audit assumptions when no audit input exists

4. Section 6 can use provided audit input and reflect it in initiative reasoning

5. Cross-sell and upsell opportunities are previewed first, then AM-confirmed, then optionally added as a final subsection

6. The SEO Strategy Bank in Notion is used as the source for opportunity matching rather than hardcoded generic ideas

7. Content credit capacity from Skill.md is used as a real planning constraint

8. If no strong cross-sell or upsell opportunity exists, none is fabricated

9. Existing PDF generation still works end to end

After implementation, give me:
- files changed
- exact Section 2 ranking logic
- exact audit intake behavior
- exact Strategy Bank lookup and opportunity matching behavior
- exact cross-sell and upsell confirmation flow
- exact way content credit capacity influences recommendations
- whether PDF export still works
- any edge cases or unresolved limitations